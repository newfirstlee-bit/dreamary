# 운영·확장성 개선 작업 결과

> 2026-09-13 후속 작업: 테스트 Firebase·Netlify를 분리했고 비밀값·인증·규칙·색인 15개와 실제 서버 배포를 완료했다. 인증/페어 API 16개, 합성 기록 30+5 페이지 조회, iOS·Android 빌드가 통과했다. 최신 상태는 `STAGING_SETUP.md`를 우선한다. 아래의 테스트 환경 미생성/운영 DB 공유 설명은 분리 작업 전 기록이며 운영 배포는 별개로 남아 있다.


2026-09-16. **앱·서버 코드 보완과 로컬 자동 검증 완료. 스테이징 서버·OTA12는 반영됐고, 운영 인덱스 15개는 READY 확인을 완료했다. 운영 백업·알림 수신자·원격 CI는 외부 권한/설정이 남아 있다.** 아래 표의 ‘구현’은 현재 작업 폴더 기준이며 실제 운영 반영 여부는 외부 확인 결과와 함께 본다.

사용자 결정: R2는 비밀번호 변경 방식을 유지하고 임시 비밀번호 로그만 제거. R5는 출시 기능으로 수정. R13 제외. 페어 신규 등록 최대 5개. ImgBB와 현재 무료 범위 유지. OTA는 이후 별도 작업. 기존 5개 초과 페어를 삭제하지 않는다.

## 항목별 결과

| 항목 | 구현한 내용 | 남은 확인 / 제한 |
| --- | --- | --- |
| R1 채팅 ID | 난수 ID, 같은 전송 재시도 ID 유지, 중복 답장 재사용 | 양 플랫폼 실기기에서 네트워크 끊김/재전송 확인 |
| R2 비밀번호 | 임시 비밀번호 로그 제거. 메일 발송 성공 후에만 비밀번호 변경하도록 레거시·canonical 함수 순서 보완 | 운영 Resend 검증 발신 도메인과 실제 사용자 수신 확인 필요 |
| R3 사용량 | AI 전체/개인 하루 상한, 분당/동시 예약, 본문·메시지 크기, 기기·이미지·관리자 로그인 제한 | 무료 사용이나 총 지출을 보장하는 금액 한도는 아님. 실제 사용량/비용 경고 설정 필요 |
| R4 채팅 조회 | 최신 30개 조회/구독, 커서 더보기, 늦은 응답 폐기, 20개씩 서버 삭제 | 과거 createdAt 누락 자료 점검 필요 |
| R5 알림 | 하루 200명 고정 상한 제거. 매분 실행에서 작은 대상/기기 페이지, 임대·재시도·기기 성공 상태 기록. 해제도 20개 페이지 | APNs/FCM 운영 설정과 양 플랫폼 실제 도착 확인 필요. 외부 전송 응답 유실 시 중복 알림 가능성은 남음 |
| R6 배포/환경 | 타입·린트 검사 활성화, 회귀·demo 에뮬레이터 CI, demo 설정 앱 빌드, API 프로토콜 헤더와 호환 적용 순서 | 테스트 Firebase/서버 분리·스테이징 배포 완료. `.github/workflows/verify.yml`은 원격 저장소 push 후 1회 실행 필요 |
| R7 복구/관측 | 암호화 Firestore 내보내기/격리 복원 도구, 상태 표본 조회, 구조화 로그, 복구 절차 | **실제 운영 백업·보관 위치/키 관리·정기 실행·경고 수신 미설정**. Auth/이미지 원본은 별도 백업 필요 |
| R8 일기 정렬 | 작성 시각+문서 ID로 DB 정렬/커서 통일, 인덱스 파일, 옛 날짜 점검·보정 도구 | 운영 인덱스 15개 생성 및 READY 확인 완료. 옛 날짜 점검·보정은 별도 운영 작업 |
| R9 AI 중복/지연 | 처리 중 예약·결과 재조회·서버 45초 요청 제한·불완전 답장 저장 거부. 삭제 후 늦은 답변 저장 차단 | 실제 AI 장애/장시간 지연 시 사용자 경험 확인 필요 |
| R10 삭제/이전 | 페이지와 진행 상태를 함께 저장하는 서버 작업. 워커 임대, 실패 재시도, 탈퇴/이전 중 새 쓰기 차단 | 운영 예약 함수 배포 후 앱 종료 상태에서 완료 확인 필요 |
| R11 이미지 | ImgBB 서버 업로드·파일 검증·삭제 URL 보관, 탈퇴 시 삭제 대기, 디스크/메모리 캐시 상한. 운영 Netlify의 기존 키를 secret `IMGBB_API_KEY`로 전환하고 공개 `NEXT_PUBLIC_IMGBB_API_KEY` 제거 | ImgBB 외부 원본 삭제는 수동. 기존 이미지 삭제 URL·원본 백업은 별도 확인 필요 |
| R12 증가 관리 | 서버 트랜잭션/규칙/UI 최대 5개, 이전 시 합산 제한, 주제 단일 묶음 캐시, 시간당 제한된 임시 문서 정리 | 운영 주제 묶음 준비와 옛 만료 필드 점검 필요. 완료 작업 재전송 방지용 최소 기록은 보존 |
| 어드민 | 목록 20개·커서·주제 번역 페이지, 통계 캐시, 로그인 제한 | 현재 1인 비밀번호 방식 유지. 운영자 추가 전 개별 계정/MFA/행위 이력 필요 |
| R13 | 사용자 요청대로 변경하지 않음 | 이번 범위 제외 |

새로 시작하는 데이터 이전은 합산 5개를 초과하면 시작 전에 거부한다. 이전 버전에서 이미 대상 계정이 고정된 이전은 데이터가 중간에 남지 않도록 끝까지 재개할 수 있게 했다.

## 설정한 기본값

- AI: 사용자 하루 100회·분당 6회·동시 1건, 전체 하루 1,000회·동시 32건. 실패와 교정 재시도도 예산을 소비한다. 전체 한도는 16개 묶음에 나누므로 일부 묶음이 먼저 찰 수 있다.
- 이미지: 사용자 하루 30회, 파일 최대 2MiB. 디스크 캐시 50MiB/100개/30일, 메모리 캐시 50MiB/20개.
- 실제 예산이 미정이므로 보수적 호출 상한을 두었다. 서버 환경변수와 예상 DB 읽기·함수 실행 비용은 `OPERATIONS_RUNBOOK.md`에 기재했다. 유료 상품을 가입하거나 요금제를 변경하지 않았다.

## 실제 검증

| 검증 | 결과 |
| --- | --- |
| 회귀 테스트 | 92개 통과, 실패 0. 인증/소유권, 생성 상한 경합, 재시도, 선톡 ID, 삭제 중 AI, 작업 재개, 푸시 처리, 백업 훼손 거부, 비밀번호 메일 순서 포함 |
| demo Firestore 규칙 | 7개 통과, 실패 0. 본인 허용·타인/무인증 거부·서버 전용 필드/컬렉션 보호 |
| 실제 demo 백업 복원 | 1개 통과. 합성 문서 23개 복원과 DB 시각 정밀도 확인 |
| `npm run check:app` | 타입 검사와 최종 branch 앱 정적 빌드 통과 |
| `npm run build` | 최종 웹 빌드·배포 함수 진입점 검사·회귀 테스트 통과 |
| `npm run build:app:test` | demo 설정 앱 빌드 통과. 실제 테스트 Firebase 연결 환경은 별도 필요 |
| `npx cap sync` | iOS·Android에 OTA14 기준 branch 앱 결과물 동기화 완료 |
| iOS | 시뮬레이터 Debug 빌드 통과. 배포 서명 없이 컴파일 검증 |
| Android | Debug APK 빌드 통과 |
| Firestore demo emulator | 규칙 7개·암호화 백업 복원 1개 통과 |
| 정적 확인 | diff 공백 오류 없음. 클라이언트 번들의 알려진 서버 키 노출 검사 통과. OTA 추가 없음 |

기존 `any`/미사용 선언 등 일부 린트 경고는 남아 있다. 타입 오류·린트 오류를 무시하는 빌드 설정은 해제했고, 기존 스타일 부채 일부를 warning으로 분류했다. 빌드 성공은 실기기 UI/실제 푸시 수신/대규모 운영 부하 검증을 대신하지 않는다.

검증 기록: `artifacts/operations-2026-09-11/verification-2026-09-16.json`, `artifacts/operations-2026-09-11/firebase-indexes-2026-09-16.json`. 기존 사용자 기록을 쓰거나 삭제하는 운영 테스트는 하지 않았다. Firebase 설정은 읽기 전용으로 확인했으며 운영 복합 인덱스 15개가 모두 READY임을 확인했다. PITR 비활성, 백업 예약 조회 권한 부족 상태는 별도 기록으로 남아 있다.

## 다음 적용 순서

1. 운영 백업 확보, 인덱스 생성/READY, 옛 날짜 점검 및 주제 묶음 준비.
2. 서버 API/예약 함수와 ImgBB·푸시 환경 준비.
3. 새 앱/웹 전환과 규칙 적용을 호환되는 순서로 진행.
4. 양 플랫폼 실기기 확인, 알림·삭제/이전 완료 확인, 실제 오류/비용 경고와 정기 백업 설정.

상세 절차와 명령은 `OPERATIONS_RUNBOOK.md`를 따른다. 이번 변경에서는 운영 서버·규칙 배포를 완료하지 않았고, OTA13은 스테이징 게시·실기기 확인 후 기록한다. 운영 인덱스는 READY 상태로 확인했으며, 옛 날짜 점검·백업·알림·원격 CI는 후속 운영 설정으로 남아 있다.
