# Dreamary 개발 필수 규칙

이 파일의 규칙은 Dreamary의 모든 화면 개발과 수정에서 최우선으로 확인한다.

화면 디자인을 변경하기 전에는 루트의 `DESIGN_GUIDE.md`를 먼저 확인하고 그 수치와 공통 클래스를 따른다.

앱 변경을 전달하기 전에는 루트의 `APP_QA_CHECKLIST.md`와 `DEBUGGING.md`를 확인하고 최소한 `npm run check:app`과 대상 플랫폼 동기화·빌드를 수행한다.

## 탭 상단바 규칙 (필수)

- 하단 탭 최상위 화면의 상단바 콘텐츠 높이는 `DESIGN_GUIDE.md`에 정의된 **52px**로 고정한다.
- 기기 안전영역은 52px에 별도로 더하며 임의로 축소하지 않는다.
- 화면별로 상단바 높이나 상하 패딩을 덮어쓰지 않는다.

## 하단 탭 규칙 (필수)

- **풀페이지 팝업·풀페이지 작업 흐름에는 하단 탭을 절대 노출하지 않는다.**
- 풀페이지에는 온보딩, 안내 화면, 로그인·회원가입, 설정·편집처럼 기존 탭 화면을 완전히 덮는 흐름이 포함된다.
- 홈(`/`), 일기(`/diary`), 채팅(`/chat`), 마이페이지(`/mypage`)와 탭 안에서 계속 탐색하는 화면에서만 하단 탭을 노출한다.
- 하단 탭은 `src/lib/navigation.ts`의 `shouldShowBottomNav()`에 명시적으로 허용된 경로에서만 생성한다. 새 경로는 기본적으로 하단 탭 미노출이다.
- 앱 하단탭 배경은 항상 단색 흰색(`#FFFFFF`)이다. 하단탭에는 체크 패턴, 홈 배경 이미지, 그라데이션, 반투명 blur 배경을 적용하지 않는다.
- 화면 컴포넌트마다 별도의 경로 조건을 추가하지 않는다.
- 입력창·텍스트 영역 등에서 키보드가 열리는 모든 순간에는 하단 탭을 즉시 제거하고, 키보드가 닫힌 뒤 허용된 탭 화면에서만 복원한다.
- 탭 경로 안에서 풀페이지 오버레이를 띄운 경우에도 오버레이가 닫힐 때까지 하단 탭을 제거한다.
- Android 시스템 하단바와 iOS 홈 인디케이터가 CTA나 앱 하단 탭을 가리면 안 된다. 하단 고정 CTA는 `DESIGN_GUIDE.md`의 `.fixed-cta-footer` 규칙을 따른다.
- 비밀번호 확인·개인정보 수정 풀페이지 상단바의 콘텐츠 높이는 52px로 고정하고 기기 상단 안전영역은 별도로 더한다.
- Capacitor 정적 앱의 경로 끝 `/` 유무는 같은 화면으로 판정하며, 하단 탭 노출 여부는 반드시 중앙 경로 판정을 사용한다.
- 실제 ID가 들어가는 동적 화면은 직접 `/경로/${id}`로 이동하지 않고 `src/lib/navigation.ts`의 `buildStaticEntityRoute()`로 이동하며, 대상 화면은 `resolveStaticEntityId()`로 실제 ID를 읽는다.

## 앱 인증 시작 규칙 (필수)

- Firebase 로그인 세션 확인이 응답하지 않더라도 앱을 무한 로딩 상태로 두지 않는다.
- 세션 확인 제한시간이 지나면 비로그인 UUID로 앱을 사용할 수 있게 하고, 이후 로그인 세션이 복원되면 로그인 사용자 데이터로 자동 전환한다.
- 비로그인 앱 사용자의 데이터 기준 ID는 `src/lib/auth.ts`의 로컬 UUID(`dreamary_user_id`)이며, 광고식별자(IDFA/AAID)를 사용자 데이터 소유권 기준으로 사용하지 않는다.
- 로그인 사용자의 데이터 기준 ID는 Firebase `uid`다. 화면·저장·캐시 로직은 로그인 세션 유무만 확인하고, 로그인 정보가 없어도 비로그인 UUID로 홈/일기/채팅/마이페이지가 동작해야 한다.
- `CapacitorHttp.enabled`는 반드시 `false`로 유지한다. 전역 fetch/XHR 패치는 Firestore WebChannel 스트림을 잘라 읽기·쓰기를 무한 재시도하게 만든다.
- 네이티브 단발성 JSON API는 필요한 경우에만 명시적 `CapacitorHttp.request/post()`를 사용할 수 있다. Firestore와 스트리밍 채팅에는 사용하지 않는다.

- iOS·Android Firebase 연결 안정화를 위해 `src/lib/firebase.ts`의 Firestore long-polling 자동 감지 설정을 임의로 제거하지 않는다.
- `server.iosScheme`을 바꾸거나 Firebase 네트워크 설정을 바꾼 뒤에는 `npm run build:app` 후 `npx cap sync ios`로 iOS 프로젝트에 반영한다.
- 탭 이동 화면은 캐시 화면을 먼저 보여주고 Firebase 재조회로 화면 전환을 막지 않는다.
- 요약·뱃지·오늘 상태 확인에는 전체 채팅/전체 일기 목록을 읽지 말고 마지막 1개, 오늘 1개, 개수 조회처럼 필요한 범위만 읽는다.
- 캐릭터 생성·수정·삭제·소유권 이전 후에는 화면별 캐시뿐 아니라 `invalidateCharacterStore()`로 공용 캐릭터 캐시도 무효화한다.
- 네이티브 앱 광고 링크는 `@capacitor/browser`로 연다. 광고 대상 턴은 광고 웹페이지가 실제로 열렸을 때만 채팅·일기 답장을 생성한다.
- 광고 웹페이지가 열리지 않으면 입력값을 보존하고 답장 생성 API를 호출하지 않는다. 광고 호출 실패와 드리머리 서버/API 실패는 `src/lib/adDiagnostics.ts`로 서로 다른 로그를 남긴다.
- 채팅 광고 카운트는 AI 답장이 성공한 뒤에만 증가시킨다. 실패한 채팅 전송, 광고 미노출, 서버 오류는 광고 턴으로 계산하지 않는다.
- 채팅과 일기 임시저장 키를 공유하지 않는다. 채팅 실패 입력은 채팅 입력칸에만 보존하고 일기 입력칸으로 자동 복원하지 않는다.
- 앱 빌드의 비로그인 마이페이지에서는 UUID 원문 영역을 노출하지 않는다. 웹 비로그인 화면에서만 UUID 영역을 보여줄 수 있다.
- 채팅내용 백업은 비로그인 UUID 데이터에 대해 백업 난수코드를 생성하고, 로그인 후 코드 입력 시 서버 관리자 권한으로 로그인 `uid`에 데이터를 이전한다.
- 백업/이관 API는 네이티브 앱에서 `apiPostJson()`을 사용하여 호출하고, Firestore 보안 규칙에 막히지 않도록 Netlify 함수에서 `firebase-admin`을 사용한다.
- 선톡 완료 여부는 Firestore 채팅 문서가 기준이다. `hasPinged_*`를 API 성공 전에 저장하거나, 해당 값만으로 선톡 재시도를 막지 않는다.
- 선톡 요청은 `src/lib/initialPing.ts`의 `ensureInitialPing()`만 사용하여 중복 요청·실패 복구·네이티브 CORS 우회를 중앙 관리한다.
- 선톡 생성 중에는 홈 화면 메시지 카드에 채팅방과 같은 입력중 점 3개 애니메이션을 표시하고, 완료 시 실제 선톡으로 교체한다.
- 풀페이지 최상위 요소에는 `.full-page`를 적용하여 상·하단 기기 안전영역을 반영한다.
- 풀페이지 CTA는 스크롤 영역 밖의 고정된 하단 영역에 두고, 기기 하단과 최소 여백을 둔다.
- Capacitor 정적 앱의 동적 상세 경로는 빌드용 `/1`과 실제 `entityId` query를 구분한다. 상세 화면 렌더에서 `window.location.search`를 한 번만 읽지 말고 `useSearchParams()`로 실제 ID 변경을 구독한다.
- iOS와 Android 상태바는 WebView 위에 오버레이하여 화면 배경이 노치·상태바 뒤까지 이어지게 한다. Android에서 상태바 배경색을 흰색 등 단색으로 고정하지 않는다.
- 화면 최상위 컨테이너에는 노치·상태바 배경 의도를 드러내는 클래스를 사용한다. 하단탭 서브메인은 `.status-surface-check`, 단색 풀페이지는 `.status-surface-solid`, 흰색 작업 화면은 `.status-surface-white`를 기준으로 한다. 홈 배경 이미지가 있는 경우 별도 단색 레이어로 덮지 않는다.
- 로그인 사용자 회원 탈퇴는 클라이언트에서 Firestore 문서를 직접 삭제하지 않고 `netlify/functions/account-delete.ts`를 통해 Firebase ID 토큰 검증 후 처리한다.
- 회원 탈퇴 시 로그인 `uid` 기준 `accounts`, `characters`, 캐릭터별 `users`, `chatMessages`, `diaries`, `reports`, 관련 백업 코드와 Firebase Auth 사용자를 삭제한다.
- Google Play 외부 계정 삭제 URL은 `/account-deletion`을 사용한다. 이 페이지는 앱 재설치 없이 웹에서 본인 인증 후 같은 `account-delete` 함수를 호출해 실제 계정과 데이터를 삭제해야 한다.
- AI 답변 신고는 `netlify/functions/report-create.ts`로만 저장하고, 신고 문서는 `reports` 컬렉션에 `status: new`로 쌓는다.
- 신고 알림은 미처리 신고가 5건 단위로 쌓일 때만 발송한다. 알림 수신 주소와 메일 발송 키가 없으면 신고 저장만 수행한다.
- 일기에서는 답변 완료 후 작은 회색 `신고` 텍스트를 사용하고, 채팅에서는 AI 캐릭터 답변에만 사이렌 아이콘 신고 버튼을 사용한다. 사용자 채팅에는 수정 버튼을 노출하지 않는다.

## 네이티브 조작 규칙 (필수)

- 네이티브 앱 뒤로가기는 `src/hooks/useNativeNavigation.ts`에서만 중앙 처리한다.
- Android 시스템 뒤로가기는 홈에서 앱 종료, 일기·채팅·마이페이지 탭 최상위 화면에서 홈 이동, 풀페이지에서 이전 화면 이동으로 동작해야 한다.
- iOS와 Android 왼쪽 가장자리 스와이프 뒤로가기도 `src/hooks/useNativeNavigation.ts`의 중앙 규칙을 따른다. 화면 컴포넌트마다 별도 제스처를 만들지 않는다.
- 광고 노출 모달 확인 버튼은 누른 즉시 진한 색상과 로딩 스피너를 보여 사용자가 버튼 입력을 인지할 수 있게 한다.
