# Dreamary 출시 전 업데이트 방식 비교

검토일: 2026-09-11. 현재 저장소와 Apple·Google·서비스 제공자의 공식 문서를 기준으로 작성했다.

## 먼저 결정에 필요한 결론

드림어리는 이미 **Netlify 서버리스 + 앱에 포함된 화면 코드** 구조다. AI 답변 생성 방식과 서버 저장 로직은 서버만 배포해 바꿀 수 있다. 반면 설치된 앱의 버튼·화면·클라이언트 버그는 현재 방식에서 새 앱 배포가 필요하다.

권장 조합은 **현재 서버리스 유지 + 자주 바꾸는 값의 원격 설정 + 필요한 경우 OTA 업데이트 추가**다. 기존 앱을 전면 재개발할 이유는 현재 검토에서 찾지 못했다. 다만 먼저 [운영 위험 검토](</Users/shinwonlee/Desktop/dreamary/RELEASE_OPERATIONS_REVIEW.md>)의 출시 전 항목을 정리하는 편이 좋다.

“서버리스”는 서버를 누가 관리하느냐의 문제이고, “심사 없이 앱 내용을 어떻게 바꾸느냐”와는 다른 축이다. 아래 방식들은 함께 사용할 수 있다. 사람들이 꼽는 방식이 정확히 무엇인지 명칭을 전달받지는 않았으므로, 현재 앱에서 현실적으로 선택할 수 있는 방법을 비교했다.

## 앱 변경을 전달하는 방법

| 방식 | 쉽게 설명하면 | 앱 재심사 없이 반영할 수 있는 범위 | 장점 | 단점·주의점 | 드림어리 적합도 |
| --- | --- | --- | --- | --- | --- |
| 서버 API 배포 | 앱이 요청하면 서버가 새 처리 방식으로 답함 | 서버의 AI 모델·프롬프트·저장·검증 로직. 기존 앱과 요청·응답 약속을 유지해야 함 | 현재 사용 중. 서버 배포로 반영하고 서버 버전을 되돌리기 쉬움 | 앱 안의 화면·키보드·버튼 코드까지 바뀌지는 않음. 실수하면 여러 앱 버전이 동시에 영향받음 | **계속 사용 권장** |
| 원격 설정 / 서버가 내려주는 콘텐츠 | 이미 만들어 둔 기능에 값·문구·주제 등을 공급 | 앱이 미리 지원하는 문구, 공지, 노출 조건, 제한값, 기능 스위치. 더 큰 화면 변경은 미리 만든 화면 조립 규격이 있어야 가능 | 작은 변경에 적합. 일부 사용자부터 적용하거나 기능을 끄기 쉬움 | 미리 구현하지 않은 기능은 만들 수 없음. 설정 캐시·기본값·호환 버전을 설계해야 함 | **우선 도입 권장**. 주제는 이미 DB에서 받고 있음 |
| OTA / Live Update | 앱이 새 HTML·CSS·JavaScript 묶음을 받아 다음 안전한 시점에 실행 | 호환되는 웹 화면·스타일·JS 수정. 정책상 허용되는 범위에 한함 | Capacitor 구조를 유지하면서 화면 버그 수정 속도를 높일 수 있음. 배포 대상 구분·복구 기능을 갖춘 서비스 이용 가능 | 업데이트 도구의 첫 탑재는 앱 배포 필요. 네이티브 플러그인·권한은 변경 불가. 배포 서명, 앱 버전 호환, 단계 배포, 실패 복구 운영 필요 | **화면 수정이 잦다면 가장 직접적인 후보** |
| 원격 웹 화면을 앱 WebView에서 열기 | 앱 내부에서 서버의 웹사이트를 매번 불러옴 | 서버에 있는 웹 화면과 JS. 이미 설치된 네이티브 기능 범위에 한함 | 웹 배포를 앱 화면에 빠르게 반영. 웹과 화면을 공유하기 쉬움 | 첫 화면부터 네트워크·서버 장애 영향을 받음. 오프라인·캐시·로그인·뒤로가기·플러그인 호환을 재설계해야 함. 단순 웹 래퍼는 심사상 검토 대상 | **전체 전환은 현재 비추천**. 필요한 안내 페이지 등에 제한적으로 검토 |
| 스토어 앱 업데이트 | 새 설치 파일을 App Store·Google Play로 전달 | 해당 없음. 스토어 배포 절차를 따름 | 네이티브 기능·권한·SDK·앱 아이콘까지 변경 가능. 설치된 코드 조합이 명확함 | 심사와 사용자 업데이트에 시간 필요. 이전 버전 사용자도 지원해야 함 | **기본 배포 경로로 계속 필요** |

원격 설정은 실제로 앱 다운로드 없이 동작·표시를 조정할 수 있지만 앱이 해당 값을 읽고 처리하도록 준비되어 있어야 한다. 비밀키나 보안 권한 판정을 공개 설정값에 넣으면 안 된다. [Firebase Remote Config](https://firebase.google.com/docs/remote-config/)

OTA의 네이티브 변경 불가, 기존 앱 버전별 전달, 실패 복구는 업데이트 서비스를 써도 직접 검증해야 한다. 예를 들어 Capgo와 Capawesome은 Capacitor용 후보이지만, 여기서는 계약·가격·실제 연동까지 검증해 특정 제품을 선정한 것은 아니다. 현재 설치된 `@capgo/capacitor-native-navigation`은 뒤로가기 플러그인이며 OTA 설치를 의미하지 않는다. [Capgo 호환성](https://capgo.app/docs/live-updates/compatibility/), [Capawesome Live Updates](https://capawesome.io/cloud/live-updates/)

Capacitor 공식 설정 문서는 `server.url`을 개발용 live reload 목적으로 설명하고 운영 용도로 의도하지 않았다고 명시한다. 따라서 설정 한 줄로 운영 웹을 연결하면 출시 구조가 완성된다고 판단하면 안 된다. [Capacitor 설정](https://capacitorjs.com/docs/config)

웹만 배포하고 홈 화면에 추가해 사용하는 **PWA**도 선택지다. 스토어를 통한 앱 배포와는 별도 경로이며 기존 네이티브 기능을 그대로 쓸 수 있다고 가정하면 안 된다. 업데이트에도 브라우저·서비스 워커 캐시 수명 처리가 필요하다. 현재 iOS·Android 앱을 이미 만든 상황에서는 주력 앱을 대체하기보다 웹 채널의 확장안으로 보는 것이 적절하다. [PWA 업데이트](https://web.dev/learn/pwa/update)

## 심사 범위는 별도로 판단해야 한다

OTA를 설치해도 “앞으로 모든 변경은 심사 없이 가능”해지는 것은 아니다. Apple 2.5.2는 기능을 도입하거나 바꾸는 다운로드 코드에 제한을 두고, Google도 Play 외부에서 앱 실행 파일을 교체하는 것을 제한한다. Google의 WebView JavaScript 등에 대한 예외도 다른 정책을 무시할 권한은 아니다. 앱 목적·핵심 기능의 중대한 변경, 권한·네이티브 변경은 스토어 업데이트로 처리하는 운영 기준을 권장한다. 개별 심사 통과를 보장하는 방식은 없다. [Apple 심사 지침](https://developer.apple.com/app-store/review/guidelines/), [Google Device and Network Abuse](https://support.google.com/googleplay/android-developer/answer/9888379?hl=en), [Capgo 정책 FAQ](https://capgo.app/docs/faq/)

## 실제 변경 예시로 고르기

| 하고 싶은 변경 | 현재 앱에서 필요한 전달 방식 | 원격 설정·OTA를 추가한 뒤 |
| --- | --- | --- |
| AI 말투 프롬프트·모델 교체 | 서버 배포 | 그대로 서버 배포. 품질·응답 시간·비용 검증 필요 |
| 오늘의 일기 주제 콘텐츠 수정 | DB의 주제 변경과 앱 캐시 갱신 | 동일. 데이터 편집이므로 스토어 앱 업데이트는 불필요 |
| 오류 안내 문구·버튼 색·웹 화면 간격 수정 | 앱에 포함된 코드이면 스토어 업데이트 | 미리 원격화한 값은 설정 변경, 그 외 호환되는 웹 수정은 OTA 후보 |
| 채팅 목록 조회 개수·페이지네이션 로직 수정 | 현재 클라이언트 구현이므로 스토어 업데이트 | 웹 코드 범위라면 OTA 후보. 서버·DB 인덱스도 함께 맞춰야 함 |
| 사용자별 하루 AI 사용량 제한 | 서버 검증 추가는 서버 배포. 새 안내 화면은 앱 업데이트 | 서버 제한 + 원격 안내. 원격 설정만으로 강제 제한을 대신하지 않음 |
| 장애 발생 시 AI 생성만 일시 중지 | 현재 별도의 운영 스위치가 확인되지 않음 | 서버 측 중지 스위치 + 앱의 준비된 안내. 저장된 기록은 계속 열람 가능하게 설계 |
| 새 카메라·결제·푸시 플러그인, OS 권한 변경 | 스토어 업데이트 | 여전히 스토어 업데이트 |
| 앱 아이콘·네이티브 설정·Capacitor 버전 변경 | 스토어 업데이트 | 여전히 스토어 업데이트 |
| 앱의 핵심 용도나 제공 서비스를 크게 변경 | 새 심사 범위 검토 + 스토어 업데이트 | OTA 가능 여부만으로 판단하지 않음 |

## 서버 운영 자체를 비교하면

다음은 위 표와 별개의 선택이다. 어느 서버 방식을 쓰든 서버 코드만 변경한다면 앱 스토어 심사와 직접 연결되지 않는다.

| 서버 방식 | 특징 | 장점 | 단점·비용 성격 | 현재 판단 |
| --- | --- | --- | --- | --- |
| 함수형 서버리스 — 현재 Netlify Functions | 요청이나 예약 실행마다 함수를 실행하고 공급자가 인프라를 관리 | 초기 서버 관리 부담이 작고, 짧은 API 작업에 적합 | 실행 시간·요청 크기 제약, 외부 서비스 대기·호출 증가에 따른 비용, 공급자 의존. 자동 확장은 비용도 함께 늘릴 수 있음 | **현재 유지**. 조회·요청 제한·긴 작업 분리가 먼저 |
| 관리형 컨테이너 — 예: Cloud Run | 실행 환경을 컨테이너로 묶고 플랫폼이 배포·확장을 관리 | 서버 처리 환경을 더 유연하게 구성. API와 작업 처리 프로세스를 정리하기 좋음 | 이미지·자원·동시 처리 설정 운영이 추가됨. 최소 실행 인스턴스·사용량 등에 따라 비용 발생 | 장기 작업·처리 제어가 실제 제약이 될 때 검토 |
| 직접 관리하는 서버 / VM | 빌린 서버에 프로그램을 계속 실행 | 운영체제·프로세스·연결을 직접 통제. 일정 부하에서는 고정 용량 비용 예측이 쉬울 수 있음 | 보안 패치·증설·장애 복구·백업 책임이 큼. 유휴 서버 비용과 별도 이중화 비용 발생 | 현재 인력·요구에서는 우선순위 낮음 |

컨테이너와 서버리스는 반대말이 아니다. Cloud Run처럼 컨테이너를 서버리스로 운영하는 제품도 있다. 위 표는 운영 책임과 배포 단위를 비교한 것이다. [Cloud Run 개요](https://docs.cloud.google.com/run/docs/overview/what-is-cloud-run)

Netlify 공식 문서에는 기본 동기 함수 60초, 예약 함수 30초, background 함수 15분 제한이 안내되어 있다. 긴 작업이 필요하면 타임아웃 숫자만 키우기보다 작업을 나눠 이어서 처리할 구조를 먼저 선택해야 한다. 실제 적용 런타임·계약 설정은 전환 설계 시 대조해야 한다. [Netlify Functions 설정](https://docs.netlify.com/build/functions/configuration/)

## 드림어리에 제안하는 도입 순서

| 단계 | 제안 | 선택 기준 / 완료 조건 |
| --- | --- | --- |
| 출시 준비 | 현재 서버리스 유지. 서버·DB·앱 버전 호환, 비용 제한, 배포 검사 정리 | 서버를 바꾸지 않고도 현재 확인된 운영 위험을 해결할 수 있음 |
| 자주 바꾸는 값 정리 | 공지·점검 안내·일부 노출값을 원격 설정으로 분리 | 어떤 값이 원격인지 목록화. 통신 실패 시 기본값, 캐시 갱신 주기, 설정 되돌리기 검증 |
| OTA 도입 여부 결정 | 화면 수정이 잦고 스토어 대기가 실제 병목이면 작은 화면 수정으로 먼저 검증 | 첫 설치 앱에 업데이트 플러그인 포함. Capacitor 8 호환, 양 OS 확인, 서명·배포 대상·복구 시험 완료 |
| 운영 단계 | 테스트 기기 → 일부 사용자 → 전체 사용자 순서로 변경 배포 | 설치 앱 버전·웹 묶음 버전·서버 버전을 함께 추적. 이전 앱과 새 서버도 정상 동작해야 함 |

OTA 업데이트를 일기 작성 중 즉시 재시작시키면 입력이 사라질 수 있다. 다운로드는 뒤에서 처리하더라도 적용은 입력·광고·인증·이전 작업이 끝난 시점이나 다음 실행으로 미루고, 실패 시 이전의 정상 묶음으로 돌아오게 설계한다. 자동 복구는 모든 종류의 오류를 잡아주지는 않는다. [Capgo 복구](https://capgo.app/docs/live-updates/rollbacks/), [호환성 검증의 한계](https://capgo.app/docs/live-updates/compatibility/)

월 비용이 확정되지 않았으므로 특정 유료 OTA 플랜 구매를 권하지는 않는다. 선택 시 월 활성 설치 수, 업데이트 묶음 크기, 배포 횟수, 전송량·보관량, 팀 권한·이력·복구 기능을 같은 조건으로 견적 비교하면 된다.

## 현재 구조를 확인한 근거

| 근거 파일 | 확인 내용 |
| --- | --- |
| [capacitor.config.ts](</Users/shinwonlee/Desktop/dreamary/capacitor.config.ts:7>) | `out`의 웹 파일을 앱에 포함. 운영 웹을 직접 여는 `server.url` 없음 |
| [next.config.mjs](</Users/shinwonlee/Desktop/dreamary/next.config.mjs:16>) | 앱 빌드는 정적 export |
| [app-build-targets.json](</Users/shinwonlee/Desktop/dreamary/app-build-targets.json:1>) | 테스트 앱은 PR #2 API, 운영 앱은 운영 API. 두 대상의 Firebase 프로젝트는 같음 |
| [netlify.toml](</Users/shinwonlee/Desktop/dreamary/netlify.toml:1>) | 웹과 서버 함수 배포 구조 |
| [채팅 서버](</Users/shinwonlee/Desktop/dreamary/netlify/functions/chat.ts:200>) | AI 요청·모델 선택은 서버 코드 |
| [주제 조회](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:174>) | 주제 콘텐츠는 DB에서 가져옴 |

이번 문서는 비교·검토 결과다. OTA 설치, 서버 전환, 설정 변경, 새 배포는 수행하지 않았다.
