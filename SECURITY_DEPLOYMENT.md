# 인증 보완 / 배포 전 점검 (2026-09-10)

**현재 판정: 로컬 구현 및 양 플랫폼 빌드 검증 완료, 배포 보류. 보안 규칙과 인증 연동 코드는 준비했으나 운영 규칙/서버에는 아직 반영하지 않았다. 실환경 검증은 남아 있다.**

## 2026-09-10 추가 구현

- `firestore.rules`: 본인 데이터만 허용하며 클라이언트의 소유자 변경·일기 생성/삭제·인증키/백업/푸시/신고 컬렉션 직접 접근은 금지한다. 주제만 공개 읽기를 허용한다. 로컬 에뮬레이터에서 본인 허용/타인·무인증 거부를 검증했다.
- `dataSession.ts` + `dataFirestore.ts`: UI 로그인 세션은 유지하고 별도 Firebase 앱의 메모리 인증으로 DB에 접근한다. 서버는 검증된 UID/비로그인 UUID만 Firebase custom token에 넣는다. 비로그인은 Firebase Auth 내부 ID `guest:{UUID}`를 사용하되 실제 데이터 소유자 키는 기존 UUID 그대로다. 익명 로그인 공급자를 켜거나 매번 사용자 인증 화면을 띄우지 않는다.
- DB 첫 원격 작업 때만 인증을 준비하고 같은 세션의 동시 요청은 공유한다. 인증용 API는 15초 제한을 두고, 늦은 이전 계정 응답은 폐기한다. SDK의 다른 계정 캐시를 섞지 않도록 원격 읽기는 서버 결과를 사용하고 기존 앱 소유자별 캐시를 즉시 표시용으로 유지한다. 구독의 캐시 스냅샷/다른 계정 응답은 전달하지 않는다.
- 채팅·선톡 저장도 인증된 Admin API로 바꿨다. 규칙 강화로 서버의 Web SDK 쓰기가 막히는 회귀를 방지하고, 요청 ID 중복 조회를 본인/캐릭터 범위로 제한한다. 생성 실패의 부분 답변은 저장하지 않는다. iOS/Android 선톡의 직접 네이티브 POST에도 인증 헤더를 붙였다.
- 관리자 쿠키 `admin_auth=true`를 신뢰하지 않는다. 별도 서명키로 발급한 1시간 세션을 검증하고 관리자 데이터 API 자체에서도 확인한다. 관리자 주제 수정/삭제/count 및 페어 정보는 서버 경로로 이동했다. 기존 IP 제한은 보조 방어로만 유지한다.
- 캐릭터 삭제는 인증된 서버가 자식 일기·채팅을 20개 페이지로 지우고 마지막에 프로필/캐릭터를 지운다. 삭제 중 새 일기·채팅 저장을 차단한다.
- 회원 탈퇴는 중단된 비로그인 이전의 원본까지 포함해 20개 페이지로 정리한다. 신규 백업/시도 기록과 `guest:{UUID}` Auth 사용자도 정리한다. 이전 UUID의 자격 문서에는 재등록 방지용 `{retired: true}`만 남기고 계정 UID·키 해시·본문은 제거한다. 원래 로그인 Auth 사용자 삭제는 기존 REST 경로를 유지한다.
- 신고는 본인 대상만 접수하고 본문은 서버의 실제 답변으로 가져온다. 비밀번호 초기화 서버의 Web SDK 조회는 Admin의 단일 결과 조회로 바꿨으며 임시 비밀번호 로그는 제거했다.

## 실제 확인한 출시 차단 문제

Firebase Rules REST API에서 현재 `cloud.firestore` 릴리스를 읽기 전용으로 확인했다. 현재 규칙은 다음과 같다.

```text
match /{document=**} {
  allow read, write: if true;
}
```

- 이는 로그인·본인 여부에 관계없이 모든 컬렉션의 직접 읽기/수정/삭제를 허용한다. 일기 API를 거치지 않고 우회할 수 있다.
- 새 `guestCredentials`, `guestBackupCodes`, `guestBackupAttempts`도 이 규칙 아래에서는 클라이언트가 열람·변조할 수 있다. 따라서 이 문서의 선행 조치 없이 새 인증 코드를 운영에 올려도 안전하다고 판단하면 안 된다.
- 특정 경로에 `allow ...: if false`를 추가하는 것만으로는 넓은 `allow ...: if true`를 취소할 수 없다. 광범위 허용 자체를 제거해야 한다.
- 확인 과정에서 운영 문서 본문 조회·데이터 수정·규칙 변경·서버 배포는 하지 않았다. 침해 사고가 있었다는 증거는 수집하지 않았으며 실제 침해 발생을 단정하지 않는다.

## 이번에 구현한 범위

- 로그인: Firebase ID 토큰의 서명·발급자·프로젝트·유효기간·인증 시각을 확인하고 요청 UID 및 문서 소유자를 대조한다. 토큰 검증 자체에는 Firestore 읽기가 없다. 기본 검증은 즉시 토큰 취소/계정 정지 여부를 별도 조회하지 않는다.
- 비로그인: 기기에 256-bit 난수키를 보관하고 서버에는 해시만 저장한다. 최초 등록 이후 다른 키로 덮어쓸 수 없다. 15분 인증증을 메모리에 재사용하며 자동 갱신은 다음 실제 동작 시에만 한다. 등록 응답 유실 시 같은 키로 재시도한다.
- 기존 비로그인 데이터: 사용자가 승인한 **출시 전·실사용자 없음·ID를 아는 타인 없음** 전제 아래 최초 연결을 한시적으로 허용한다. 최초 연결은 과거 소유권을 암호학적으로 증명하는 절차가 아니며 먼저 등록한 기기를 신뢰한다. 상용 운영에서 계속 열어두면 안 된다.
- 일기 작성/수정/삭제/중복 응답: 검증된 소유자만 처리한다. 저장·변경 트랜잭션 안에서도 소유자와 비로그인 이전 상태를 확인한다. 캐릭터 소유권이 AI 생성 중 바뀌어도 이전 소유자로 저장하지 않는다.
- 당일 삭제: 클라이언트의 `todayDateString`을 신뢰하지 않는다. 서버 시각과 검증된 범위의 기기 시간대(-14~+12시간 오프셋)로 현재 날짜를 계산한다. 시간대는 기기 입력이므로 인접 날짜의 허용 경계는 달라질 수 있지만 임의의 과거 날짜를 오늘로 위조할 수는 없다.
- 백업/이전: 백업 발급은 비로그인 인증증이 필요하고, 이전은 로그인 인증증과 기기 인증증 또는 백업 코드가 모두 필요하다. 8자리 코드는 암호학적 난수로 생성하고 해시만 저장한다. 발급은 ID당 하루 3회, 잘못된 코드 검증은 로그인 UID당 하루 10회까지다. 이는 분산 계정/IP 공격 전체를 막는 전역 제한은 아니다.
- 이전 대상 계정은 트랜잭션으로 고정한다. 중단 후 같은 계정만 재개할 수 있고, 다른 계정으로 변경할 수 없다. 일기 → 채팅 → 캐릭터 순서로 한 번에 20개씩 옮긴다. 이전 후 같은 날의 일기를 새 UID 문서 ID로 중복 생성하지 않는다.
- 앱의 직접 `userId` 변경 방식은 제거하고 공통 서버 이전 API로 통일했다. 이전 완료 시 해당 기기의 기존 비로그인 UUID를 은퇴시켜 다음 로그아웃 때 새 UUID를 만든다. 웹/Next와 Netlify, iOS와 Android 모두 동일한 인증 코드 경로를 사용한다.

## 반드시 끝내야 할 선행 조치

1. **DB 규칙 운영 반영 전 확인:** 별도 데이터 인증·클라이언트 호출 연동·규칙은 로컬 구현했고 Rules Emulator 테스트를 통과했다. 실제 Firebase custom-token 로그인 및 서버 환경과 함께 검증한 뒤 운영 규칙을 반영해야 한다. 현재 운영은 여전히 광범위 허용 상태다. 단순히 로그인 조건만 먼저 배포하거나 신규 인증 컬렉션을 공개 상태로 사용하면 안 된다.
2. **탈퇴 실환경 회귀:** 새 메타데이터와 중단된 이전 정리는 로컬 구현/테스트 완료. 운영 계정을 실제 삭제하는 검증은 하지 않았다. 테스트 전용 계정에서 최종 승인 후 확인한다. 기본 ID 토큰 서명 검증은 즉시 취소 여부를 조회하지 않으므로 토큰 만료 전 본인 계정에 대한 잔여 접근 가능성은 별도 세션 취소 정책 검토 항목이다.
3. **서버 환경값 준비:** 아래 값은 서버에서만 설정한다. 이번 작업에서 실제 값 생성·업로드·환경 변경을 하지 않았다.

| 설정 | 용도 / 주의 |
| --- | --- |
| `FIREBASE_SERVICE_ACCOUNT_KEY` | 서명 검증 대상 프로젝트와 Admin DB/최초 UUID의 Firebase 계정 여부 확인. 신규 권한 부여는 하지 않음. |
| `GUEST_SESSION_SECRET` | 암호학적 난수 32바이트 이상의 hex 값. `NEXT_PUBLIC_` 금지, 저장소·로그·채팅에 원문 금지. 누락 시 비로그인 작업은 안전하게 거부. |
| `ADMIN_SESSION_SECRET` | 관리자 쿠키 서명용 별도의 암호학적 난수 32바이트 이상 hex 값. 기존 `admin_auth=true` 쿠키는 폐기되고 관리자 재로그인이 필요. |
| `GUEST_LEGACY_CLAIM_UNTIL` | 기존 테스트 데이터 최초 연결을 허용할 짧은 기간의 ISO 8601 종료 시각. 기본값은 비허용. 테스트 기기 연결 후 제거/만료 확인. 실사용자 배포 전 반드시 닫기. |

4. **앱·서버·규칙 동시 전환:** 구버전 앱은 인증 헤더/시간대를 보내지 않아 새 API가 거부한다. 새 앱은 `/api/guest/session`과 `/api/data/session`이 필요하므로 구버전 서버에서는 개인 데이터 로딩/비로그인 저장/이전이 동작하지 않는다. 실사용자가 없다는 전제에서 기존 테스트 앱을 중지하고 서버 환경/코드 → 새 규칙 → 새 테스트 앱 사용 순서로 전환한다. 규칙을 닫기 전 신규 인증키를 등록해 사용하지 않는다. 기존 8자리 `backupCodes`는 새 인증 경로에서 받아들이지 않으므로 새로 발급한다.
5. **Node 22 Netlify 실제 패키징과 런타임:** 기존 `firebase-admin/auth` ESM 장애 이력이 있어 최초 기기 등록의 동적 import, Google 공개키 조회, Auth `getUser`, CORS/오류 응답, API 경로 충돌 여부를 배포 미리보기에서 확인한다. 로컬 Node/Next 빌드 통과는 Netlify 실배포 검증을 대체하지 않는다.
6. **악용/비용 보호:** 게스트 등록·AI 생성 등 고비용 API의 플랫폼/IP 요청 제한과 모니터링을 검토한다. 인증된 사용자의 무제한 재시도·다수 기기 등록까지 차단 완료했다고 볼 수 없다.
7. **기기 회귀:** iOS/Android에서 기존 비로그인 키 최초 연결, 신규 비로그인 작성, 로그인/가입 이전, 백업 코드 이전, 중단/재개, 로그아웃 후 새 UUID, 같은 날 중복 작성, 알림·광고 복귀를 검증한다. 회원가입 도중 이전 API 장애가 발생한 경우 이미 생성된 Firebase 계정으로 로그인하여 이전을 재개하는 흐름도 확인한다.

## Firestore 읽기 예산 (트랜잭션 재시도/인덱스 과금 별도)

| 동작 | 읽기 영향 |
| --- | --- |
| 로그인 ID 토큰 검증 | 0. 공개 서명키 네트워크 조회/캐시만 사용. |
| 비로그인 최초 연결 | 기본 5회(자격 문서 2 + 기존 데이터 유무 3개의 limit(1)). 한시 연결 기간에는 자격 문서 2회. 별도로 Firebase Auth 사용자 존재 확인 1회, Firestore 과금 아님. |
| 비로그인 인증증 갱신 | 실제 기능 요청 시 약 14분 캐시 이후 자격 문서 1회. 화면/탭 진입 타이머 조회 없음. |
| 별도 DB 세션 준비 | 앱 런타임의 첫 개인 데이터 원격 작업/계정 전환 시 로그인은 Firestore 0회, 비로그인은 자격 상태 1회. 인증증이 없으면 위 등록/갱신 비용도 발생. 동일 세션은 재사용. 별도로 Firebase Auth 통신이 추가되어 최초 접근 지연은 실측 필요. |
| 새 클라이언트 보안 규칙 | 소유자 필드가 있는 읽기는 인증 때문에 추가 get 없음. 프로필 읽기/쓰기는 부모 캐릭터 검증 1회, 비로그인 쓰기는 자격 상태 검증 1회, 채팅 생성은 캐릭터 검증 1회 추가 가능. 규칙 내부 get의 실제 과금/캐시는 요청 단위로 달라진다. |
| 일기 수정/삭제 | 로그인: 기존과 같은 일기 1건. 비로그인: 자격 상태 1건 추가, 합계 2건. |
| 일기 신규 작성 | 로그인: 기존 조회/저장에 캐릭터 소유권 확인 2회 추가(생성 전/저장 트랜잭션). 비로그인: 여기에 자격 상태 2회 추가. 이전된 캐릭터만 당일 기존 일기 limit(1) 확인 추가. |
| 백업 발급 | 자격 상태·발급 횟수·코드 충돌 확인 3회. |
| 기기키로 이전 | 요청당 예약 확인 1 + 상태 확인 1 + 해당 단계 최대 20문서. 비어 있는 조회도 과금 가능. |
| 백업 코드로 이전 | 위 값에 코드/시도 횟수 및 예약 트랜잭션 코드 재확인 3회 추가. |

추가 조회는 소유권·동시 이전 검증에 필요한 고정 문서/제한된 페이지이며 전체 컬렉션 fallback은 없다. **“인증 전체가 DB 읽기 0”이라고 안내하면 안 된다.**

## 로컬 검증

- 2026-09-10 사용자 테스트 배포의 환경변수 4KB 초과에 대응해 보조파일 2개를 `netlify/shared/`로 분리했다. 실제 Netlify 로컬 함수 검색/패키징 결과는 API v2 함수 23개, 구형 함수 0개다. 빌드 사전 구조 검사와 회귀 테스트 3개를 추가했다. 환경변수 값·DB 권한은 변경하지 않았다. 수정된 소스를 올린 다음 원격 테스트 배포 성공 여부를 별도 확인해야 한다. 상세 원인과 절차는 `DEBUGGING.md` 최상단을 따른다.
- `npm run test:security`: 실제 서명과 실제 TypeScript 핸들러, 메모리 DB를 사용하는 보안 회귀 테스트. 운영 DB/AI 호출 없음.
- `npm run test:performance`: 기존 캐시·저장 응답·알림 후처리 회귀 테스트.
- 2026-09-10 최신 코드로 `npm run check:app`(타입 검사 + 정적 앱 빌드), `npm run build`(웹/서버 빌드) 통과. iOS/Android `npx cap sync` 완료.
- iOS Simulator Debug `xcodebuild` 종료 코드 0. 실기기 서명/설치 검증은 아니다.
- Android는 사용자 재시도 승인 후 2026-09-10 11:42 KST `npm run check:app` → `npx cap sync android` → `assembleDebug`를 실행해 `BUILD SUCCESSFUL`을 확인했다. 기존 Gradle deprecated 설정 경고는 남아 있다. APK: `android/app/build/outputs/apk/debug/app-debug.apk` (9,758,457 bytes, SHA-256 `21e543faa1996112b371834bd2e47ed203f24f930133e3a4ee58caef2f988afa`). 기기 설치나 서버 배포는 하지 않았다.
- 이번 수정 경로의 `git diff --check` 통과. 기존 Next 설정 `experimental.allowedDevOrigins` 및 앱 정적 export 관련 경고는 남아 있다.
- 보안 단위 테스트 18개, 성능 회귀 15개, 실제 로컬 Firestore Rules Emulator 테스트 6개 통과. 에뮬레이터 프로젝트는 `demo-dreamary-security`, 접속은 `127.0.0.1:8080`으로 고정했다.
- Rules 재실행 예: `firebase emulators:exec --only firestore --project demo-dreamary-security --config firebase.test.json "npm run test:rules"`. `firebase.test.json`은 로컬 테스트 설정이며 배포 지시가 아니다.
- 검증에 사용한 로컬 에뮬레이터는 정상 종료했다. 기기 설치·스토어 업로드·운영 서버/규칙 배포·실제 인증키 설정은 하지 않았다.
- 실기기 E2E, Netlify Node 22 패키징/미리보기, 실제 서버 환경값 검증은 미완료다. 로컬 검증 완료를 운영 반영 완료로 해석하지 않는다.
