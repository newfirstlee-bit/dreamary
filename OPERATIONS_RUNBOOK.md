# 운영 반영·복구 절차

## 2026-09-16 운영 배포 사전 확인 — 최신 상태

- 활성 운영 배포: `6aaa99386e0661bc90373005`(ready). 배포 전 되돌릴 기준은 `6aa2c1b853c3e9000882a0ef`(ready, 커밋 `2b9d257feef43b77690ed029313d5a9acbb94d16`)이다.
- 운영 인덱스 15개 READY, 기존 규칙 `709158b6-195d-44c4-8cfd-bf9843a1eab1`을 재확인했다. 원본 규칙을 `artifacts/production-2026-09-16/previous-firestore.rules`에 보관했다. 새 규칙 적용은 호환 서버·웹 게시와 함께 진행한다.
- 일기 367·채팅 6,320·캐릭터 135·계정 25개는 전체 count와 양수 숫자 `createdAt` 조건 count가 같았다. 사용자 본문을 읽거나 날짜를 수정하지 않았다.
- 기존 주제 185개를 20개씩 읽어 `topicCatalog/current` 1개를 생성하고 재조회 확인했다. 기존 주제·사용자 기록은 변경하지 않았다.
- 운영 서버 게시 후 호환 Firestore 규칙 `032b0b1b-7015-4a5a-8969-695dc8ff419c`을 활성화했다. 합성 계정으로 로그인·본인 접근/타인 및 비인증 거부·동시 페어 5개 제한·멱등 재시도·이미지 업로드·선톡·채팅·일기 저장을 확인하고 합성 데이터를 정리했다.
- 비밀번호 찾기 canonical 경로의 운영 404는 Next API 연결을 추가한 hotfix로 수정했다. 배포 `6aaa99386e0661bc90373005`에서 OPTIONS 204와 미존재 합성 계정의 JSON 404를 확인했으며 실제 메일은 보내지 않았다.
- 스토어용 iOS build 6 준비 중 타깃 최소 버전이 프로젝트 공통 설정 15.0을 덮어쓴 26.0임을 확인했다. Capacitor 8 의존성 기준과 공통 설정에 맞춰 앱 타깃도 15.0으로 통일해 지원 기기를 불필요하게 제한하지 않는다.
- Google Play 첫 제출용 업로드 키는 저장소 밖 `~/Library/Application Support/Dreamary/AndroidSigning/dreamary-upload.jks`에 두고, 무작위 비밀번호는 macOS 키체인의 `com.dreamary.android.upload` 서비스로만 보관한다. `create-android-upload-key.sh`는 기존 키를 덮어쓰지 않으며, `sign-android-bundle.sh`는 원본 unsigned AAB를 보존하고 별도 파일만 서명한다. Play 최초 업로드 전에 키 파일의 별도 암호화 사본을 확보한다.
- 회귀 97개, Rules Emulator 7개, 합성 백업 복원 1개 통과. 실제 운영 백업 전체 복원은 아래의 7,315문서 확인 기록을 따른다.
- 운영 설정으로 분리한 `/private/tmp/dreamary-prod-release-KIjl3s`에서 Netlify 서버·웹 빌드 통과. 비밀 환경파일·기존 빌드·백업파일은 배포 소스에 포함하지 않았다. 후보 소스 해시와 공개 파일의 로컬 비밀값 검사 결과는 `artifacts/production-2026-09-16/release-candidate.json`에 기록했다. 아직 게시하지 않았다.
- **ImgBB 후속:** 개별 조회 404로 누락을 확인한 뒤 사용자가 기존 운영 키의 운영 재등록을 명시적으로 허용했다. 동일 키를 production `IMGBB_API_KEY`에 등록했다. Netlify 무료 요금제가 특정 scope 지정에 403(`Upgrade your Netlify account to set specific scopes`)을 반환하여 기본 전체 scope를 사용한다. `NEXT_PUBLIC_` 공개 변수로 만들거나 클라이언트 코드에 넣지 않는다. 실제 이미지 업로드는 신규 배포 후 점검한다.
- 메일은 기존 사용자 결정대로 `onboarding@resend.dev`를 유지한다. 일반 사용자 발송 준비 완료를 의미하지 않는다.
- 증거: `artifacts/production-2026-09-16/`(비밀값 및 사용자 본문 없음).

> 2026-09-16 상태: 테스트 Firebase·Netlify 분리와 스테이징 서버/OTA12 배포는 완료했다. 비로그인 UUID·인증키 보조 저장 코드와 회귀 검사를 반영했다. 운영 Firestore 대기 작업은 0건이었다. 운영 복합 인덱스 15개를 생성했고 모두 READY 상태를 확인했다. 최신 외부 설정 상태는 아래 표와 `TODO.md`를 함께 본다.


2026-09-11. 코드 개선 상태는 `OPERATIONS_IMPLEMENTATION.md`를 기준으로 한다. 이 문서의 설정·배포·운영 백업 명령은 준비된 절차이며 자동 실행한 기록이 아니다.

## 현재 외부 설정 확인

| 항목 | 확인 결과 |
| --- | --- |
| DB | `dreamary-1a9af`, 서울 `asia-northeast3`, Firestore Native |
| 복합 인덱스 | `firestore.indexes.json` 기준 15개가 운영에 등록되었고 모두 READY 상태로 확인 |
| 시점 복구(PITR) | 비활성 |
| 관리형 백업 예약 | 현재 서버 인증에 조회 권한이 없어 **미확인**. 백업 없음으로 단정하지 않음 |
| 별도 테스트 Firebase | `dreamary-staging`으로 분리 완료. 합성 테스트는 `demo-dreamary-security` 로컬 에뮬레이터도 사용 |
| 로컬 서버 환경 | Firebase Admin·ImgBB·메일 설정 존재 여부를 확인. 운영 Netlify에서는 기존 ImgBB 키를 서버 전용 `IMGBB_API_KEY`(builds/functions, secret)로 전환하고 `NEXT_PUBLIC_IMGBB_API_KEY`를 제거했다. APNs 키 묶음은 등록되어 있으나 실제 도착은 실기기 확인 필요 |
| 실제 요금제·사용량·알림 수신자 | 사용자는 무료 범위 사용 중이라고 답변. 사용량은 수동 확인으로 결정. 콘솔별 알림 설정은 사용하지 않음 |

설정 조회는 읽기 전용으로 수행했다. 사용자 일기·채팅 본문을 조회하거나 운영 DB를 수정하지 않았다. 증거는 `artifacts/operations-2026-09-11/firebase-settings-readonly.json`이다.

## 서버 사용량 기본값

예산이 미정이므로 화폐 금액 대신 **호출 횟수와 동시 실행 수**를 제한한다. 무료 사용 보장이나 서비스 전체 지출 한도는 아니다. 기본값은 사용량을 본 뒤 서버 환경변수로 조정한다.

| 환경변수 / 정책 | 기본값 | 적용 의미 |
| --- | --- | --- |
| `AI_GENERATION_ENABLED` | 미설정이면 허용 | `false`로 새 AI 생성 중지. 저장된 데이터의 직접 열람은 유지 |
| `AI_DAILY_ATTEMPT_LIMIT` | 1,000 | UTC 하루 전체 AI 시도 예산 |
| `AI_USER_DAILY_ATTEMPT_LIMIT` | 100 | 사용자별 하루 시도 상한. 일기는 언어 교정 재시도까지 2회분 선예약 |
| `AI_USER_MINUTE_LIMIT` | 6 | 사용자별 분당 새 요청 |
| `AI_GLOBAL_CONCURRENCY_LIMIT` | 32 | 전체 처리 중 예약 상한. 사용자별 동시 생성은 1건 |
| `MIN_CLIENT_PROTOCOL` | 0 | 기존 앱 API 호환 유지. 새 앱은 1 전송. 전체 배포 후 필요 시 1로 올려 이전 API 클라이언트의 변경 요청 차단 |
| 이미지 업로드 | 사용자별 30회/UTC 하루, 파일 2MiB | JPG/PNG/GIF/WebP 시그니처·본문 크기 검사 후 ImgBB로 전송 |
| 비로그인 신규 연결 | 네트워크별 20회/일, 전체 500회/일 | 기존 키 갱신은 신규 연결 수에 포함하지 않음. IP 미제공 환경은 공용 보수적 버킷 |
| 알림 등록 | 사용자별 30회/일, 활성 기기 5대 | 기존 5대 초과 자료는 발송 페이지로 계속 처리. 새 기기는 상한 검사 |
| 관리자 로그인 | 네트워크별 30회/일 | 서명 세션·기존 비밀번호 방식 유지 |
| 삭제·이전 작업 생성 | 사용자별 50건/일 | 같은 작업의 페이지 재개는 추가 차감 없음. 회원 탈퇴는 이 상한에서 제외 |

전체 AI 예산·동시 예약은 사용자 해시로 나눈 16개 묶음에 고정 분배한다. 따라서 합계 상한에 이르기 전에도 특정 묶음이 먼저 찰 수 있다(기본 하루 62~63회/묶음, 동시 2건/묶음). 여유분을 다른 묶음에서 빌려 쓰지 않는 보수적 방식이다. 실패도 예약된 시도 예산을 소비한다. 본인 요청 ID에 이미 저장된 답장이 있으면 재생성하지 않는다.

AI 공급자 요청은 45초, 저장 예약은 55초로 제한한다. 연결이 끊기거나 공급자가 완료 표시 없이 종료하면 부분 답변을 성공으로 저장하지 않는다. Netlify modern API의 스트리밍 실행 한도는 60초, 예약 함수는 30초다. [Netlify API](https://docs.netlify.com/build/functions/api/), [함수 한도](https://docs.netlify.com/build/functions/configuration/)

Netlify의 코드 기반 IP 제한은 무료 요금제에서도 지원되지만, 적용 지연이 있어 DB 트랜잭션 제한을 함께 사용한다. [공식 제한 안내](https://docs.netlify.com/manage/security/secure-access-to-sites/rate-limiting/)

## 읽기·쓰기 비용 변화

- 채팅 진입: 첫 페이지 최대 30개 + 실시간 구독 최초 최대 30개. 과거 더보기는 한 번에 30개. 캐릭터/프로필·인증·규칙 평가·인덱스 읽기는 별도다. 100/1,000/10,000개 합성 기록에서 동일한 조회 상한을 테스트했다.
- 주제: 클라이언트와 서버 각각 캐시 만료 시 `topicCatalog/current` 1개 조회. 캐시는 5분. 최대 1,000개·600KB를 넘으면 저장을 거부하며 묶음 분리 작업이 필요하다.
- AI: 정상 로그인 채팅의 서버 사전 조회·예약·저장·예약 해제는 대략 14개 문서 읽기, 서버 7회 쓰기다. 사용자 메시지 1회 쓰기와 클라이언트 구독/규칙 읽기, 트랜잭션 재시도는 별도다. 보호 기능 자체에도 비용이 있다.
- 지속 작업: 매분 예약 함수 한 개에서 삭제·이전과 푸시를 처리한다. 빈 상태라도 하루 약 2,880회 최소 쿼리 읽기(2개 × 1,440회), 30일 약 43,200회 함수 실행이다. 실제 작업·재시도 비용은 추가된다.
- 메타데이터 정리: 매시간 5개 컬렉션에서 최대 20개씩 확인(시간당 최대 100개 후보). 빈 상태 하루 약 120회 최소 쿼리 읽기, 월 약 720회 함수 실행이 추가된다. 실제 삭제 전에는 현재 상태를 다시 읽는다.
- 백업: 전체 문서 읽기와 다운로드가 발생한다. 자동 무료 백업 기능으로 오해하지 않는다. 일반 CLI 기본 문서 상한은 1,000개이며, 대화형 운영 스크립트는 2026-09-16 집계 7,315개를 반영해 10,000개다. 시작 전 count 집계로 상한을 점검하며, 진행 중 초과에도 실패 처리하고 불완전 파일을 제거한다.

Firestore 무료 기준은 일 읽기 50,000·쓰기 20,000·삭제 20,000, 저장 1GiB다. 관리형 백업·PITR·TTL 삭제는 무료 사용에 포함되지 않는다. 따라서 이번에 유료 기능을 켜지 않았다. 직접 SDK 쓰기·조회, 함수 실행/전송량, AI 가격은 위 요청 상한 밖의 비용도 있으므로 콘솔 사용량을 함께 봐야 한다. [Firestore 공식 과금](https://firebase.google.com/docs/firestore/pricing)

## 적용 순서

1. 변경 전 커밋·Netlify 배포 ID·활성 규칙·환경변수 이름·인덱스를 보관한다. 실제 운영 백업을 확보한다. 잘못된 삭제 때 코드 되돌리기만으로 데이터가 복구되지는 않는다.
2. `firestore.indexes.json`의 필요한 인덱스를 생성하고 READY를 확인한다. 기존 인덱스가 추가됐으면 삭제하지 않고 병합한다. 운영 15개 인덱스는 2026-09-16에 READY 확인을 완료했다.
3. 날짜 사전 점검과 주제 묶음을 준비한다. **새 앱을 먼저 설치해 테스트하면 미배포 API/주제 묶음/인덱스 때문에 실패할 수 있다.**
4. Netlify에 서버 API·예약 함수를 배포한다. 운영 ImgBB는 서버 전용 `IMGBB_API_KEY`로 등록하고 공개 `NEXT_PUBLIC_IMGBB_API_KEY`는 제거했다. APNs·FCM 설정과 실제 테스트 기기 토큰을 확인한다.
5. 페어 생성은 서버 API를 사용하는 새 앱/웹으로 전환한 뒤 규칙을 적용한다. 새 규칙은 클라이언트 직접 페어 생성을 거부하므로 이전 앱과 동시에 쓰면 생성이 실패한다. 이전 앱을 계속 허용하려고 5개 제한을 우회하는 규칙을 다시 열지 않는다.
6. 앱 branch는 PR #2 API를 사용하므로 해당 API 배포도 같은 코드여야 한다. 운영 앱은 `npm run build:app:release`로 만든다. 새 프로토콜 헤더를 실제 기기에서 확인한 뒤 필요 시 최소값을 올린다.
7. 테스트용 계정/기기로 로그인·비로그인, 페어 5/6번째, 선톡, 웹 스트림·네이티브 JSON, 실패 재전송, 이전/탈퇴, 이미지 업로드, 양 플랫폼 실제 알림 도착을 확인한다. 예약 함수는 게시된 배포에서 실행되므로 미리보기 성공만으로 운영 스케줄 성공을 판정하지 않는다.

서버/규칙/인덱스/새 앱은 서로 다른 배포물이다. 되돌릴 때도 서로 호환되는 묶음으로 판단한다. 보안 규칙을 예전 전체 공개 상태로 되돌리지 않는다. AI 문제는 먼저 `AI_GENERATION_ENABLED=false`로 확산을 멈추고 로그와 저장 결과를 확인한다. 삭제 작업 오류는 `dataJobs`의 pending 상태와 함수 로그를 확인하며, 삭제 작업을 완료로 조작하거나 임대 필드를 임의로 지우지 않는다.

## 사전 데이터 점검 도구

다음 명령은 기본 읽기 전용이다. `--apply`를 붙였을 때만 DB를 변경한다. 프로젝트 ID가 로컬 서버 인증의 프로젝트와 다르면 실행을 거부한다.

```sh
node scripts/operations/prepare.cjs --project dreamary-1a9af --action catalog
node scripts/operations/prepare.cjs --project dreamary-1a9af --action dates --collection diaries --max-docs 1000
node scripts/operations/status.cjs --project dreamary-1a9af
```

- 날짜 점검 대상: `diaries`, `chatMessages`, `characters`, `accounts`. 20개씩, 상한까지 읽고 다음 커서를 출력한다. `--cursor`로 이어서 읽는다. 본문은 가져오지 않는다.
- 숫자 createdAt이 없을 때 기존 Timestamp, 13자리 시간 ID, 일기 날짜의 한국 0시 순서로 복구값을 제안한다. 마지막 방법은 원래 작성 시각 복원이 아니라 날짜 정렬을 위한 보정이므로 `createdAtRecoveredFrom`을 남긴다. 근거 없는 항목은 unresolved로 남기며 날짜를 임의로 만들어 넣지 않는다.
- 주제 묶음은 개수·크기를 출력하고 전체를 읽었을 때만 적용한다. 편집을 잠시 멈춘 상태에서 실행해 생성 중 변경과 충돌하지 않게 한다.
- 상태 도구는 대기 작업·이미지 삭제 대기를 각각 최대 5개 표본으로 본다. 출력의 `sampled`는 전체 개수가 아니다.
- 이전 메타데이터 중 만료 필드가 없거나 숫자만 있는 자료, `nextNotifyAt`이 누락된 옛 알림 대상은 자동 정리/예약 쿼리에 잡히지 않는다. 출시 전 별도 표본 점검과 필드 보정이 필요하다. 정상 사용자 기록을 보존 기간만으로 삭제하지 않는다.

## 백업과 복원

`backup.cjs`는 현재 Dreamary의 최상위 컬렉션 문서 전체를 20개씩 내보내 gzip + AES-256-GCM으로 암호화한다. 키는 20자 이상 `BACKUP_PASSPHRASE` 환경변수에서 읽는다. 비밀 문구를 채팅·명령 인수·저장소에 넣지 않는다. 기존 파일은 덮어쓰지 않고 새 경로를 쓴다. 하위 컬렉션이 발견되면 불완전한 백업을 성공 처리하지 않고 중단한다.

```sh
bash scripts/operations/backup-interactive.sh
# 직접 실행하는 경우 암호문구는 환경변수로만 전달하며 먼저 문서 수/읽기 예산을 확인한다.
node scripts/operations/backup.cjs --project dreamary-1a9af --action export --file /안전한경로/날짜.drmbkp --max-docs 10000
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 node scripts/operations/backup.cjs --project demo-dreamary-security --action restore-demo --file /안전한경로/날짜.drmbkp
```

대화형 스크립트는 암호문구를 두 번 비공개 입력받고 종료 시 환경변수에서 제거한다. CLI 내보내기는 파일 생성 후 GCM·복호화·프로젝트·형식·문서 수를 자동 검증하며 검증용 평문 파일이나 DB 쓰기를 만들지 않는다. 처리 개수와 실패 분류를 출력하지만 비밀값과 본문은 출력하지 않는다. 이 로컬 파일 검증은 아래의 실제 격리 DB 복원 시험과는 별개다.

**2026-09-16 실제 운영 백업 생성과 로컬 파일 검증 완료:** `backups/dreamary-1a9af-20260916T080558Z.drmbkp`, 7,315문서, 2,502,671바이트. 사용자 터미널의 `verified:true` 출력과 로컬 파일 존재·0600 권한을 확인했다. SHA-256은 `e8f328caefc7aaab089589d07eb6456932ff113a32ac2cc499de2bdb4afdd9b6`다. 실제 운영 사본도 2026-09-16 21:46 KST에 loopback demo DB로 7,315개 전부 복원·재조회·내용 대조를 통과했다(`verifiedEveryDocument: true`, 원본 SHA-256 일치). 결과는 백업 옆 `.restore-check.json`에 있다. Google Drive `Dreamary 운영백업` 폴더에 암호화 파일 업로드 완료를 확인했고, 일반 액세스는 제한됨·소유자만 접근 가능하다. 파일 ID는 `103RbfkLkVmxtxxJtvYPo-dcrZun7flXW`, 폴더는 https://drive.google.com/drive/folders/1zJVkNcR-4y8TkB90ZeYWL2aWTaNxjj8O 이다. 암호문구는 업로드하지 않았다.

다음 명령은 기존 파일을 다시 백업하지 않고 로컬 전용 임시 Firestore(127.0.0.1:8186, `demo-dreamary-backup-drill`)에 복원해 모든 문서를 재조회·내용 대조한다. 암호문구는 터미널에서 비공개 입력한다. 운영 인증 환경변수를 제거하고 클라우드 DB에는 연결하지 않는다. 시험이 끝나면 에뮬레이터를 종료하고 임시 로그·평문을 정리한다. 결과는 백업 옆 `.restore-check.json`에 문서 수·체크섬만 기록하며 원본 암호화 파일은 보존한다.

```sh
bash scripts/operations/restore-interactive.sh backups/dreamary-1a9af-20260916T080558Z.drmbkp
```

이 대화형 명령 전체 흐름은 합성 23문서로 통과했고, 운영 도구 회귀 8개도 통과했다. 복원 도구는 전체 파일의 인증 태그가 맞기 전까지 쓰지 않으며, 운영 프로젝트 복원은 거부한다. 운영 복구는 별도 대상/범위를 검토한 후 준비해야 한다.

이것은 동시 쓰기를 포함하는 시점 일관성 백업이 아니다. 일관된 사본이 필요하면 유지보수 시간 또는 관리형 백업이 필요하다. Firebase Auth 계정/비밀번호 해시·인증 설정·보안 규칙·인덱스·서버 환경변수·ImgBB 원본 이미지 파일은 이 백업에 포함되지 않는다. Auth 내보내기와 복원용 해시 설정은 프로젝트 관리자 권한으로 별도로 확보해야 한다.

운영 정책 제안은 하루 1회 암호화 사본, 배포 전 추가 사본, 최근 7일+최근 4주 보관, 개발 PC 외 별도 보관, 월 1회 격리 복원 확인이다. 사용자는 Google Drive 보관과 본인의 암호문구 관리를 선택했다. 주기·보관 기간의 실제 자동 실행이나 외부 업로드는 아직 설정하지 않았다. 이 정책을 실제로 설정하기 전에는 R7 완료로 판정하지 않는다.

## 이미지 삭제와 운영 확인

ImgBB 공식 API는 업로드와 삭제 URL 반환을 설명하지만 자동 삭제 API를 문서화하지 않는다. `delete_url`은 서버 전용으로 보관한다. 회원 탈퇴 때 `imageDeletionQueue`로 옮겨 관리자가 삭제 URL을 통해 원본 삭제를 마친 후 해당 대기 문서를 제거한다. 삭제 URL은 공개 링크로 공유하지 않는다. 기존에 저장하지 않았던 이미지의 삭제 URL은 복구할 수 있다고 보장하지 않는다. ImgBB 계정에서 별도 확인이 필요하다. [ImgBB API](https://api.imgbb.com/)

원본 이미지 파일의 외부 유실·보관 기간·공개 URL 접근 특성은 ImgBB를 유지하는 동안 남는다. 기기 캐시나 URL 목록은 이미지 원본 백업이 아니다. 새 업로드의 캐시는 디스크 50MiB/100개·30일, 메모리 50MiB/20개로 제한한다.

출시 후 최소 하루 한 번 Netlify `ai_request_finished`, `operations_queue`, `data_job_failed`, `metadata_cleanup` 로그와 Firebase 사용량을 확인한다. AI 실패 증가, 10분 이상 대기 작업, 당일 알림 실패 증가, 백업 미생성은 조사 대상으로 한다. 프로젝트 관리자에게 오류/사용량 경고 수신을 연결하고 시험 알림을 확인해야 한다. **알림 수신자·실제 오류/비용 알림은 아직 설정하지 않았다.** 로그 기록만으로 자동 장애 알림 완료라고 하지 않는다.

관리자는 현재 1인 공용 비밀번호 체계를 유지한다. 로그인 제한·통계 캐시·목록 상한은 반영했다. 운영자를 추가하기 전 개별 계정·MFA·권한/행위 이력을 갖춘 인증으로 전환해야 한다. R13 신고 접수/처리 작업은 사용자 요청대로 변경하지 않았다.
