# 앱 성능·호출 구조 검토 — 2026-09-09

## 1차 수정 현황 — 2026-09-09

검토 이후 사용자의 수정 요청에 따라 다음을 **로컬 코드에 반영**했다. 아래 원래 조사 내용은 수정 전 상태의 기록이다.

- 프로필·주제 캐시 및 진행 중 요청 공유, 캐릭터 조회 중복 방지와 실패 전파, 일기 목록의 공용 페어 재사용/주제 동시 준비.
- 저장된 일기를 API 응답으로 바로 표시(소유자·페어·날짜 확인), 구독 오류/빈 오프라인 캐시로 결과가 지워지는 경로 차단.
- 사진 단일 로딩 경로·제한된 재시도·비동기 캐시 저장·새 프로필 512px 압축 시도.
- 알림 OFF 시 1문서만 확인, ON 시 작성한 페어만 갱신. 개발/운영 완료 API의 공통 함수 통합.
- AI 생성·언어 재생성·DB 저장·API 왕복을 구분하는 개인정보 없는 시간 기록.

자동 테스트 **15개**, `npm run check:app`, iOS·Android 동기화 및 Debug 빌드 통과. 서버/스토어 미배포이며 실기기 속도 개선율은 아직 측정하지 않았다. 상세 원인·예상 읽기 수·검증 한계는 [디버깅 기록](/Users/shinwonlee/Desktop/dreamary/DEBUGGING.md)에 작성했다.

**아직 남은 항목:** 최신 일기 정렬/인덱스와 과거 필드 확인, 일기 본문 캐시와 상세의 부가 정보 분리, 채팅 전체 내역 조회/구독, 이미지 저장소·프리로드 메모리 상한, API 인증, 환경 설정값 대조, AI/DB/실기기 속도 측정. AI 모델과 품질 검수, 인프라 설정은 바꾸지 않았다.

## 비개발자를 위한 결론

개선 여지가 크다. 현재 앱은 캐시와 조회량 제한을 일부 갖추고 있지만, 화면마다 정보를 불러오는 방식이 달라 같은 정보를 다시 요청하거나 여러 요청이 끝날 때까지 화면을 기다리게 하는 구간이 남아 있다.

사용자가 느끼는 “저장이 느림”도 작업별로 원인이 다르다. 일기 수정은 DB 읽기·수정과 서버 통신이 중심이다. 새 일기 작성은 AI 답변 생성·품질 검수·DB 저장이 하나로 묶여 있어, 이 전체 시간을 저장 시간처럼 느끼게 한다. 프로필 사진은 DB에서 사진 주소를 받은 뒤 외부 이미지 서버에서 실제 파일을 내려받는 별도 과정이다.

우선순위는 **일기·프로필 중복 조회 및 화면 대기 제거 → 이미지 로딩 정리 → AI 생성·DB 시간을 분리해 측정하고 최적화**다. 알림 후처리의 과다 조회와 채팅의 전체 내역 구독도 함께 정리할 필요가 있다.

## 조사 범위와 증거 수준

- 현재 작업 폴더의 홈·일기 작성·일기 목록·상세·수정·마이페이지·채팅 및 공용 캐시, 이미지 처리, Netlify 일기/알림 함수, Next API 연결을 읽었다.
- `DEBUGGING.md` 전체를 먼저 확인했다. 기존 ImgBB 지연, Firestore 한도 초과 기록은 과거 사례이며 현재 장애의 실측 결과로 취급하지 않았다.
- 아래의 호출 순서, 캐시 누락, 오류 처리, 환경별 코드 차이는 현재 소스에서 확인한 사실이다.
- 이번에는 실제 iPhone/Android의 네트워크 기록, 운영 함수 로그, DB 지연 지표, 배포된 앱과 현재 소스의 버전 일치를 확인하지 않았다. 따라서 특정 구간이 실제로 몇 초를 차지하는지와 성능 개선율은 아직 확정할 수 없다.
- `tsc --noEmit --incremental false`는 통과했다. 타입 검사 통과가 성능이나 실기기 동작 정상의 증거는 아니다.
- 검토 문서만 작성했다. 앱 코드·배포·DB 데이터는 변경하지 않았고, 진단을 위한 원격 DB 조회나 AI 생성도 실행하지 않았다.

## 1. 일기 목록·상세: 받은 정보를 다시 읽고, 부가 정보까지 기다림

**소스에서 확인한 내용**

- 목록은 `페어 조회 → 주제 전체 조회 → 일기 10건과 프로필 조회`의 세 단계로 진행된다. 페어와 주제는 공용 스토어가 있어도 이 화면에서는 직접 다시 읽는다.
- 목록에서 더보기를 누를 때도 같은 페어의 프로필을 다시 읽는다.
- 상세는 일기 1건을 먼저 읽은 뒤 프로필·캐릭터·이전/다음 일기·전체 주제를 기다리고 나서 본문을 표시한다. 이전/다음 버튼용 조회가 늦어도 본문이 같이 늦어진다.
- 목록·상세에는 화면 간에 공유하는 일기 캐시가 없다. 목록에서 받은 일기를 상세에서 재사용하는 흐름도 없다.
- 일기 작성 탭은 모든 페어의 오늘 상태를 확인한 뒤 작성 화면 또는 목록으로 이동한다. 목록으로 이동하면 다시 페어·주제를 읽는 경로가 생긴다.

**개선 방향**

1. 페어·주제·프로필·일기를 사용자별 공용 캐시에서 재사용한다. 동일 조회가 진행 중이면 진행 중인 요청도 공유한다.
2. 목록에서 받은 일기로 상세 본문을 먼저 표시하고, 이전/다음 버튼과 부가 정보는 따로 갱신한다.
3. 독립적인 조회는 함께 시작하되, 현재 보이는 페어부터 로드한다. 모든 페어를 미리 읽는 방식으로 병렬화하지 않는다.
4. 수정·작성 후 캐시 전체 삭제보다 바뀐 일기와 요약 상태를 갱신한다. 로그아웃·소유권 이전·삭제 시 무효화는 계속 지킨다.

근거: [목록 초기화](/Users/shinwonlee/Desktop/dreamary/src/app/diary/history/page.tsx:33), [상세 초기화](/Users/shinwonlee/Desktop/dreamary/src/app/diary/history/[id]/page.tsx:96), [작성 탭 초기화](/Users/shinwonlee/Desktop/dreamary/src/app/diary/page.tsx:390), [캐시 유효기간 5분](/Users/shinwonlee/Desktop/dreamary/src/lib/appCache.ts:1).

**정확성도 함께 보완할 부분:** 목록 조회는 DB에서 날짜순으로 정렬하지 않고 `limit(10)`으로 자른 뒤 가져온 결과만 화면에서 정렬한다. 따라서 현재의 “10개”는 최신 10개라고 보장되지 않는다. 서버의 날짜 내림차순 정렬과 그 순서에 맞는 커서·인덱스를 사용해야 한다. 날짜 필드가 없는 과거 문서 처리도 확인해야 한다. [목록 쿼리](/Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:172)

## 2. 프로필 사진: 주소 조회 지연 + 큰 원본 + 중복 로딩 경로

**소스에서 확인한 내용**

- 사진을 표시하려면 먼저 `users/{characterId}`에서 주소를 받아야 한다. 일기와 홈은 초기화 코드와 별도 프로필 effect 양쪽에서 같은 프로필을 조회할 수 있다.
- 마이페이지는 모든 페어의 프로필 요청을 묶어 기다린 뒤 한꺼번에 적용한다. 한 프로필 요청이 느리면 다른 프로필도 늦게 표시된다.
- `ResilientImage`는 먼저 원본 URL을 `<img>`에 넣고, 그 뒤 IndexedDB 캐시를 읽는다. 캐시가 없으면 별도 `fetch`도 시작한다. 원본 표시와 캐시 저장 경로가 분리돼 중복 요청이 생길 수 있다. 실제 중복 전송량은 브라우저 HTTP 캐시·요청 병합에 따라 달라진다.
- 별도 이미지 다운로드는 12초 제한으로 최대 3회 시도하며 재시도 대기가 합계 2초다. 네트워크가 응답하지 않는 경우 이 경로만 약 38초가 걸릴 수 있다. 원래 `<img>`의 대기 시간과 IndexedDB 대기는 별도다.
- 캐시를 채우는 다운로드가 성공해도 그 결과를 즉시 표시하는 연결은 없다. 원본 `<img>`가 실패했을 때 복구 함수가 별도로 동작한다.
- 프로필과 홈 배경 업로드는 같은 기본 압축값을 사용한다. 최대 1920px, 700KB 이하 파일은 조건에 따라 그대로 남으며, 프로필 전용 작은 이미지가 없다. GIF는 압축 대상에서 제외된다.
- 이미지 캐시는 30일 만료 판정만 있고 전체 용량·개수 상한과 만료 파일 정리는 없다. 배경 Blob URL 메모리 맵도 상한이 없어 장시간 사용 시 자원이 쌓일 수 있다.

**개선 방향**

- 프로필 정보를 공용 캐시로 통합하고 같은 문서의 동시 요청을 공유한다.
- 사진은 `캐시 확인 → 없으면 다운로드 1회 → 그 결과를 표시와 캐시에 함께 사용`하는 구조로 정리한다. 이전 사진·기본 이미지는 다운로드 중에도 유지한다.
- 프로필은 표시 크기에 맞춘 별도 작은 파일(예: 256~512px)을 만들고, 큰 배경 파일과 구분한다. 기존 업로드 이미지도 작은 파일이 없으면 계속 원본을 받으므로 단계적으로 변환해야 한다.
- 영구적인 404와 일시 네트워크 오류를 구분해 재시도를 제한한다. 캐시에는 용량 상한과 오래된 항목 정리를 둔다.
- 외부 이미지 호스팅 변경은 실제 실패율·파일 크기·다운로드 시간을 측정한 다음 판단한다. 이전 기록에는 ImgBB 404와 최대 약 9초 지연이 있지만 현재 상태를 실측한 것은 아니다.

근거: [프로필 중복 경로](/Users/shinwonlee/Desktop/dreamary/src/app/diary/page.tsx:535), [별도 프로필 effect](/Users/shinwonlee/Desktop/dreamary/src/app/diary/page.tsx:806), [이미지 표시](/Users/shinwonlee/Desktop/dreamary/src/components/ResilientImage.tsx:26), [다운로드·재시도](/Users/shinwonlee/Desktop/dreamary/src/lib/imageCache.ts:78), [압축 기본값](/Users/shinwonlee/Desktop/dreamary/src/lib/imgbb.ts:15), [배경 메모리 캐시](/Users/shinwonlee/Desktop/dreamary/src/lib/imagePreload.ts:4).

## 3. 일기 작성·캐릭터 답장: AI와 저장이 한 번의 대기로 묶임

현재 정상 신규 작성의 주요 순서는 다음과 같다.

`광고 대상이면 실제 광고 열림 확인 → 일기 API → 기존 일기 확인 → 요청 ID 확인 → AI 생성 → 한국어 검수 → 필요하면 AI 1회 재생성 → DB 트랜잭션 저장 → 최종 JSON 반환`

- AI 요청은 전체 답변을 한 번에 받는다. 한국어 검수에 걸리면 생성 작업을 다시 하므로 일부 요청만 유독 길어질 수 있다. 실제 재생성 비율은 아직 측정하지 않았다.
- 2초마다 보내는 공백은 연결 유지용이며 사용자가 읽을 답변을 전달하는 스트리밍이 아니다. 네이티브 앱도 최종 JSON이 올 때까지 기다린다.
- 서버는 DB 저장까지 완료한 뒤 `reply`와 `savedId`를 반환한다. 그러나 클라이언트는 이 답변을 화면 상태에 직접 넣지 않고 별도의 Firestore 실시간 구독이 반영하기를 기다린다. 구독이 먼저 올 수도 있지만, 늦거나 실패하면 API 성공 후에도 답변 표시가 지연될 수 있다.
- 클라이언트 일기 읽기 제한은 180초다. 제한시간을 늘리는 것 자체는 처리 속도를 개선하지 않는다.
- 같은 날 일기 문서 ID 고정과 트랜잭션은 저장 중복을 막지만, 서로 다른 동시 요청이 기존 문서 확인을 함께 통과하면 AI 생성은 중복될 수 있다.

**개선 방향**

1. 서버가 저장 확정한 일기 전체를 응답하고 클라이언트·캐시에 즉시 반영한다. 구독은 다른 기기의 변경 등을 동기화하는 역할로 유지한다. 이미 저장 성공한 결과를 보여주는 것이므로 저장 전 성공 표시와는 다르다.
2. 사용자 입력 저장과 캐릭터 답변 준비 상태를 구분한다. 서버에 입력을 먼저 보관하고 `답변 준비 중` 상태에서 재개 가능하게 만드는 방안은 실패 복구에 유리하지만, 기존의 하루 1건 규칙·광고 조건·재시도 설계를 함께 바꾸는 중간 규모 작업이다.
3. AI 생성 시간, 검수 재생성 비율, DB 저장 시간을 따로 기록한 뒤 한국어 품질을 유지하는 모델·공급자 설정을 비교한다. 품질 검수를 없애는 방식은 사용하지 않는다.
4. 동시 생성 제어가 필요하면 서버에서 날짜별 작업 소유권과 만료·복구를 관리한다. 단순 클라이언트 버튼 잠금만으로 여러 기기의 중복 AI 호출을 막을 수는 없다.

근거: [기존 일기 확인](/Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:47), [AI 생성·검수·저장](/Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:150), [응답 후 화면 처리](/Users/shinwonlee/Desktop/dreamary/src/app/diary/page.tsx:881).

## 4. 일기 수정 저장: 실제 DB·서버 지연 측정이 필요한 구간

- 일기 수정 API는 AI를 호출하지 않는다. 일기 1건을 읽고 소유자 문자열을 비교한 다음 필드 1개를 수정한다.
- 앱은 API 응답을 기다린 뒤 화면 내용을 바꾼다. 저장 중 스피너는 이미 있다.
- 따라서 수정에 수 초 이상 걸린다면 현재 소스만으로 “DB가 느리다”고 확정할 수 없다. 앱에서 서버까지의 통신, 서버 함수 시작, DB 읽기·쓰기 시간을 나눠 측정해야 한다.
- 서버·DB의 실제 지역과 운영 함수의 시작 지연은 확인하지 않았다. Firebase 공식 문서는 사용자·서버와 DB의 거리가 지연에 영향을 준다고 설명한다. [Firebase 성능 권장사항](https://firebase.google.com/docs/firestore/best-practices)

**개선 방향:** 단계별 시간을 기록해 오래 걸리는 단계부터 개선한다. 본문을 먼저 표시하고 `저장 중` 상태를 별도로 유지하는 방식은 체감 개선에 도움이 되지만, 실패 시 복원과 재시도를 보장하고 서버 확인 전에는 `저장 완료`로 표시하지 않아야 한다. 소유자 확인이나 저장 확정 절차를 없애서 속도를 높이지 않는다.

근거: [수정 서버](/Users/shinwonlee/Desktop/dreamary/netlify/functions/diary-edit.ts:35), [수정 화면](/Users/shinwonlee/Desktop/dreamary/src/app/diary/page.tsx:186), [저장 진행 표시](/Users/shinwonlee/Desktop/dreamary/src/components/DiaryInlineEditBox.tsx:27).

## 5. 일기 완료 후 알림 후처리: 페어 수에 비례하는 불필요한 조회

- 앱은 일기 작성 성공 후 로그인 사용자에 대해 알림 완료 API를 백그라운드 호출한다. 이 호출 자체를 기다리며 일기 저장 버튼을 붙잡고 있지는 않다.
- 서버는 알림이 켜져 있는지 확인하기 **전에** 해당 계정의 모든 페어와 전체 주제를 읽고, 페어마다 일기 개수·오늘 일기·프로필을 조회한다.
- 앱에서 전달한 다음 주제 정보도 현재 완료 핸들러는 활용하지 않는다.
- 페어가 N개이고 후보 재계산이 진행되면, 토큰 검증·쓰기·재시도를 제외해도 `페어/주제 2회 + 페어별 3N회 + 알림 대상 문서 1회`의 읽기·집계 호출 구조다. 페어 5개면 18회다. 이는 SDK 호출 수이며 실제 네트워크 횟수나 청구 문서 수와 같지 않다.

**개선 방향:** 알림 대상 여부를 먼저 확인하고, 알림이 꺼져 있으면 후보를 재계산하지 않는다. 켜져 있으면 이번에 작성한 페어의 후보만 서버에서 검증 가능한 정보로 갱신한다. 다음 주제 요약을 일기 저장 시 함께 유지하는 구조를 검토한다. 클라이언트가 보낸 주제 정보를 무검증으로 신뢰하지 않는다.

근거: [백그라운드 호출](/Users/shinwonlee/Desktop/dreamary/src/app/diary/page.tsx:936), [대상 확인 전 재계산](/Users/shinwonlee/Desktop/dreamary/netlify/functions/push-diary-complete.mts:33), [페어별 조회](/Users/shinwonlee/Desktop/dreamary/netlify/functions/push-shared.mts:136).

## 6. 전체적인 호출·안정성 문제

| 확인 항목 | 현재 상태 | 개선 방향 |
|---|---|---|
| 공용 캐릭터·주제 스토어 | 완료된 결과는 재사용하지만 진행 중 요청은 공유하지 않음 | 같은 소유자·인증 모드 요청은 Promise 공유, 무효화 후 이전 응답 차단 유지 |
| 캐시 정책 | 일부 탭은 5분 캐시, 목록·상세는 재조회, 수정 후에는 캐시 삭제 | 정보별 공용 캐시 및 변경 항목 갱신, 확인한 빈 상태와 아직 모르는 상태 구분 |
| 조회 실패 처리 | 오늘 일기 오류는 null, 개수 오류는 0, 캐릭터 오류는 빈 배열 반환 | 오류를 “데이터 없음”으로 저장하지 않고 기존 정상 화면·재시도 안내 유지 |
| 상세 화면 오류 | 일기·캐릭터 로드 실패 후에도 필수 상태가 없으면 스피너 유지 가능 | 실패 상태를 명시하고 재시도 제공 |
| 채팅 조회 | 최초 전체 메시지 읽기 후 전체 메시지 구독 | 최근 20~30개부터 로드, 과거 메시지는 더보기 |
| 채팅 구독 해제 | 초기화 완료 전 화면을 떠나면 나중에 생성된 구독이 해제되지 않을 수 있음 | 취소 여부를 확인해 늦게 생성된 구독도 즉시 해제 |
| 홈 날짜 기준 | 홈은 UTC 날짜, 일기는 기기 현지 날짜 | 동일한 날짜 정책으로 정리해 잘못된 오늘 상태·우회 조회 방지 |
| 빌드 검사 | 일반 Next 빌드에서 타입·린트 검사를 건너뜀 | 배포 파이프라인에서 별도 검사 강제, 기존 오류 단계적 정리 |

근거: [스토어](/Users/shinwonlee/Desktop/dreamary/src/store/useAppStore.ts:31), [오류를 빈 값으로 반환](/Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:231), [상세 스피너 조건](/Users/shinwonlee/Desktop/dreamary/src/app/diary/history/[id]/page.tsx:131), [채팅 초기화·구독](/Users/shinwonlee/Desktop/dreamary/src/app/chat/[id]/page.tsx:158), [전체 메시지 구독](/Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:406), [홈 날짜](/Users/shinwonlee/Desktop/dreamary/src/app/page.tsx:214), [빌드 설정](/Users/shinwonlee/Desktop/dreamary/next.config.mjs:13).

진행 중 구독이 남는 문제는 코드상 가능한 경로다. 현재 실기기에서 몇 개가 남았는지는 측정하지 않았다. 오래된 전체 일기 조회 함수는 존재하지만 이번 검색에서는 화면의 실제 호출을 찾지 못했으므로 현재 부하로 계산하지 않았다.

## 7. 개발·운영 동일 동작 점검

- 일기 작성·수정은 Next API가 Netlify의 동일 핸들러를 가져오므로 핵심 로직을 공유한다.
- 반면 알림 완료는 Next와 Netlify 구현이 따로 있다. Next는 후보 재계산 실패를 잡고 빈 후보로 후속 처리를 이어가지만, Netlify는 실패 시 API 오류로 종료한다. 소스상 실제 동작 차이다. 배포된 운영 함수가 현재 소스와 같은지는 별도 확인이 필요하다.
- `ENVIRONMENT_GUIDE.md`를 참조하는 규칙은 있으나 현재 루트에 해당 문서가 없다.
- 네이티브 API 주소가 설정되지 않으면 운영 주소로 돌아가는 기본값이 있다. 개발 DB와 운영 API가 섞이지 않도록 빌드 전에 Firebase 프로젝트와 API 대상 조합을 검증할 필요가 있다. 현재 실행 앱에 실제 혼선이 있다는 뜻은 아니다.
- `CapacitorHttp.enabled=false`와 네이티브 Firestore long-polling 자동 감지는 유지돼 있다. 과거 저장 장애를 일으킨 전역 HTTP 패치를 다시 켜는 방식으로 성능을 개선하지 않는다.

근거: [로컬 알림 완료](/Users/shinwonlee/Desktop/dreamary/src/app/api/push/diary-complete/route.ts:34), [Netlify 알림 완료](/Users/shinwonlee/Desktop/dreamary/netlify/functions/push-diary-complete.mts:35), [API 기본 주소](/Users/shinwonlee/Desktop/dreamary/src/lib/api.ts:3), [Firestore 설정](/Users/shinwonlee/Desktop/dreamary/src/lib/firebase.ts:20).

## 8. 성능 외 별도 우선 확인할 데이터 보호 문제

일기 수정 API는 클라이언트가 보낸 `userId`와 일기의 `userId`를 비교하지만, 요청자의 로그인 토큰을 검증하지 않는다. 일기 작성 API도 전달받은 사용자·캐릭터 정보에 대한 인증 검증이 없다. 프로젝트 미들웨어는 관리자 경로에만 적용된다. 현재 소스에서는 요청자가 주장하는 ID와 실제 요청자가 같은 사람인지 증명하는 절차가 빠져 있다.

성능 최적화와 함께 로그인 사용자는 Firebase ID 토큰 검증, 비로그인 사용자는 기존 UUID 사용 흐름을 유지하면서 서버가 검증 가능한 소유권 증명을 설계해야 한다. UUID 문자열 자체를 인증 수단으로 취급해서는 안 된다. 실제 타인 데이터 접근 시도나 원격 변경은 수행하지 않았다.

근거: [수정 API 권한 비교](/Users/shinwonlee/Desktop/dreamary/netlify/functions/diary-edit.ts:30), [작성 API 입력 처리](/Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:32), [미들웨어 적용 범위](/Users/shinwonlee/Desktop/dreamary/src/middleware.ts:33).

## 권장 실행 순서와 완료 기준

1. **측정 추가와 빠른 체감 개선:** API 응답으로 저장된 일기 즉시 반영, 프로필 중복 조회 제거, 일기 목록·상세 캐시 공유, 독립 조회 병렬화. 현재 조회량 이하에서 같은 계정·같은 화면 재진입의 대기가 줄어드는지 확인한다.
2. **사진 처리 개선:** 프로필 전용 작은 파일, 캐시 우선 단일 다운로드, 제한된 재시도, 용량 정리. 이미 본 사진은 네트워크가 느려도 캐시로 보이고, 실패 이미지 때문에 화면 전체가 기다리지 않아야 한다.
3. **불필요한 DB 부하 제거:** 알림 완료의 전체 페어 재계산 제거, 채팅 페이지네이션·구독 해제, 주제 캐시 공유. 데이터가 늘어도 첫 화면 읽기 수에 상한이 있어야 한다.
4. **실측 기반 서버·AI 개선:** 일기 수정의 DB 구간과 서버 시작 지연, AI 생성·재생성 비율, 서버/DB 지역을 비교한다. 중복 방지·품질 검수·소유권 검증은 유지한다.

데이터 보호 검증 누락은 위 성능 작업 순위와 별도로 우선 수정 대상으로 관리한다. 개발/운영의 알림 구현은 공통 핸들러로 통합하고 같은 회귀 테스트를 적용한다.

### 측정 계획

- 화면: 탭 선택 → 캐시 표시 → 최신 본문 표시 → 프로필 사진 표시 시간을 따로 측정한다.
- 수정: 클릭 → 서버 도착 → 일기 읽기 → 일기 수정 확정 → 앱 표시 시간을 나눈다.
- 작성: 광고 열림 대기, 서버 기존 문서 확인, 첫 AI 생성, 검수 재생성, DB 저장, 최종 응답, 구독 반영을 나눠 기록한다. 광고 시간은 AI/DB 시간과 섞지 않는다.
- 사진: 주소 준비 시간, 캐시 적중 여부, 파일 크기, 다운로드·디코딩 시간, 재시도 횟수를 기록한다.
- 플랫폼·환경·앱 버전·첫 실행/재진입·페어 수·네트워크 조건을 함께 구분한다. 초기 비교는 동일 조건으로 여러 차례 실시하고, 운영 지표는 표본 수와 함께 중앙값 및 느린 상위 구간을 본다.
- 일기 본문·이름·사진 URL·토큰은 성능 로그에 남기지 않는다. 측정용 DB 재조회나 전체 컬렉션 스캔을 추가하지 않는다.
- 개선율·완료 시간을 지금 숫자로 약속하지 않는다. 처리 시간 자체의 감소와 화면을 먼저 보여주는 체감 개선을 별도로 평가한다.

Firestore 캐시는 현재 Web SDK 기본 메모리 방식이며 앱 재시작 후 영구 캐시를 명시적으로 활성화하지 않았다. 공용 앱 캐시 정리가 먼저이며, 영구 캐시 도입은 iOS IndexedDB 동작과 로그아웃 후 정보 노출 방지를 검증한 뒤 별도 선택한다. [Firebase 오프라인 캐시 설명](https://firebase.google.com/docs/firestore/manage-data/enable-offline)
