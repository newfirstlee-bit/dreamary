# Dreamary 디자인 가이드

이 문서는 Dreamary 앱 화면을 추가하거나 수정할 때 적용하는 고정 UI 규격이다.

## 환경 표시

- 개발 웹·개발 앱 빌드에서는 모든 화면의 왼쪽 상단에 노란색 `개발` 딱지를 표시한다.
- 운영 빌드에는 개발 딱지를 절대 표시하지 않는다.
- 개발 딱지는 화면 확인용 오버레이이며 버튼·입력 등 사용자 조작을 가로막지 않는다.

## 탭 상단바

- 적용 화면: 교환일기, 채팅, 마이페이지처럼 하단 탭으로 진입하는 최상위 화면
- 상단바 콘텐츠 높이: **52px**
- iPhone 노치·상태바와 Android 시스템 안전영역은 52px 밖에 별도로 더한다. 안전영역을 줄이거나 제거하지 않는다.
- 제목 글자: 기본 20px, 세로 길이가 짧은 기기는 18px
- 좌우 여백: 기본 20px, 세로 길이가 짧은 기기는 16px
- 화면별 인라인 높이·상하 여백을 추가하지 않고 공통 `.tab-page > .header` 규격을 사용한다.
- 높이와 여백은 소수점 없이 정수 px 값만 사용한다.

## 풀페이지와 하단 탭

- 풀페이지 팝업·풀페이지 작업 흐름에는 하단 탭을 노출하지 않는다.
- iOS와 Android 네이티브 앱은 상태바를 WebView 위에 overlay로 띄우고, 화면 배경이 노치·상태바 뒤까지 이어지게 한다. 체크 배경 화면에서는 상단 상태바 영역도 같은 체크 배경이 보여야 하며, Android 상태바 배경색을 흰색 등 단색으로 고정하지 않는다.
- 노치·상태바 배경은 화면 종류별 최상단 배경을 따른다. 홈·일기·채팅·마이페이지 같은 하단탭 서브메인 화면은 `.status-surface-check`, 채팅 상세처럼 풀페이지 단색 화면은 `.status-surface-solid`, 흰색 작업 화면은 `.status-surface-white`를 사용한다.
- 홈 화면에 배경 이미지가 등록된 경우 노치·상태바 영역도 해당 이미지와 상단 그라데이션이 이어져야 하며, 별도 단색 레이어로 덮지 않는다.
- 하단 탭은 중앙 경로 허용 목록에 등록된 탭 화면에서만 생성하며, 새 화면은 기본적으로 미노출한다.
- 앱/웹 하단탭은 `rgba(255, 255, 255, 0.7)` 반투명 배경을 사용한다. 아이콘·텍스트에는 opacity를 적용하지 않는다.
- 하단탭은 `saturate(180%) blur(14px)`와 약한 상단 보더·그림자만 허용한다.
- 소프트웨어 키보드가 열리는 동안에는 모든 화면에서 하단 탭을 제거한다.
- 탭 화면 위에 풀페이지 오버레이를 띄운 경우에도 하단 탭을 제거한다.
- 비밀번호 확인·개인정보 수정 풀페이지의 상단바 콘텐츠 높이는 52px이며, 기기 상단 안전영역은 별도로 더한다.
- 풀페이지는 `.full-page`를 사용해 상·하단 기기 안전영역을 반영한다.
- 풀페이지 높이는 공통 `--app-viewport-height`만 사용한다. iOS에서 별도로 `-webkit-fill-available`을 적용해 화면 높이를 이중 제한하지 않는다.
- 풀페이지 CTA는 스크롤 영역 밖의 하단 영역에 두고 기기 하단과 여백을 둔다.
- 바텀시트는 하단 패딩에 `safe-bottom`을 포함하고, 작은 화면에서는 시트 내부가 스크롤되어 마지막 버튼이 기기 하단에 붙지 않게 한다.
- 앱 전역 모달·바텀시트는 스크롤 컨테이너 안에 두지 않고 `document.body` 포털과 공통 `.global-overlay`를 사용한다. dim은 헤더·본문·하단 탭을 포함한 앱 전체를 덮고, 시트만 최상단에 표시한다.

## 네이티브 뒤로가기

- iOS와 Android 앱에서는 화면 왼쪽 가장자리에서 오른쪽으로 스와이프하면 이전 화면으로 이동한다.
- 홈 화면에서 스와이프 뒤로가기는 앱을 종료하지 않고 아무 동작도 하지 않는다.
- Android 시스템 뒤로가기는 홈에서 앱을 종료하고, 일기·채팅·마이페이지 탭 최상위 화면에서는 홈으로 이동한다.
- 상세·수정·설정 같은 풀페이지에서는 뒤로가기와 스와이프가 이전 화면으로 돌아가야 한다.

## 모달 버튼 피드백

- 광고 노출 안내 모달의 확인 버튼은 누르는 순간 진한 보라색으로 바뀐다.
- 광고를 여는 동안에는 버튼 텍스트를 숨기고 작은 로딩 스피너만 표시한다.
- 로딩 중에는 같은 버튼을 중복으로 누를 수 없게 한다.
- 교환일기 푸시 알림 유도 바텀시트는 일기 답변 완료 후에만 노출한다.
- 교환일기 푸시 알림 유도 바텀시트와 알림설정 화면 진입 버튼은 앱 빌드에서만 노출한다. 웹 버전에는 알림 설정 UI를 노출하지 않는다.
- 푸시 알림 유도 바텀시트의 버튼은 취소 3, 주요 버튼 7 비율로 배치하고 주요 버튼은 보라색으로 표시한다.
- 푸시 알림 유도 바텀시트는 하단 안전영역을 포함한 패딩을 확보하고, 작은 화면에서는 시트 내부가 스크롤되어 버튼이 하단에 붙지 않게 한다.
- 푸시 알림 유도 바텀시트는 기존 앱 바텀시트처럼 오른쪽 상단 닫기 아이콘을 두지 않고, 시트 바깥 좌우 여백 없이 하단에 붙인다.
- 푸시 알림 유도 바텀시트의 일기장·포스트잇 장식 이미지는 타이틀·본문 텍스트 아래에 배치한다.
- 푸시 알림 완료·실패처럼 단일 확인 CTA만 있는 상태의 버튼 문구는 `확인`을 사용한다.
- 알림설정 화면은 풀페이지로 열고 하단 탭을 노출하지 않는다. 마이페이지의 앱 정보 영역에서 진입한다.
- 알림설정 화면의 `교환일기 알림` 행은 카드 테두리·좌측 아이콘·설명 문구 없이 텍스트와 토글만 배치한다.
- 기기 OS에서 드림어리 알림이 꺼져 있거나 아직 허용 전이면 `설정 > 알림 > 드림어리 > 알림을 켜주세요` 안내와 `알림설정 바로 가기` 버튼을 노출한다. 앱 내부 `교환일기 알림` 토글이 OFF여도 기기 권한 안내는 숨기지 않는다.
- 알림 설정 완료 상태는 Capacitor 권한값만 믿지 않고 Android `NotificationManagerCompat.areNotificationsEnabled()`와 iOS `UNUserNotificationCenter.getNotificationSettings()`로 확인한 실제 OS 알림 허용 상태가 켜져 있을 때만 표시한다.
- 신고 모달은 세로형 중앙 모달로 사용하고, 체크박스가 1개 이상 선택되기 전에는 `신고하기` 버튼을 회색 비활성 상태로 둔다.
- 기타 신고 입력은 최대 30자이며, 기타 선택 시 입력창을 즉시 노출한다.
- 회원 탈퇴 확인 모달은 삭제 버튼과 취소 버튼을 3:7 비율로 배치한다. 삭제 버튼은 연분홍 배경·빨간 텍스트, 취소 버튼은 회색 배경·진한 회색 텍스트를 사용한다.

## 앱 아이콘과 스플래시

- iOS 앱 아이콘은 1024px 정사각형 원본을 사용한다.
- Android 앱 아이콘은 적응형 아이콘 마스크에 잘리지 않도록 중앙 로고를 전체 캔버스의 72% 이하로 배치한다.
- Android `ic_launcher_foreground`에는 로고 안전 여백을 포함하고, `ic_launcher_background` 색상은 아이콘 배경색과 맞춘다.
- 스플래시는 iOS/Android 모두 원본 비율을 찌그러뜨리지 않고 기기별 해상도에 맞게 리사이즈한다.
- 스플래시 배경은 격자 패턴 없이 단색 `#FBFBFB`를 사용하고, 중앙에는 보라색 영문 Dreamary 로고만 배치한다.

## 신고와 회원 탈퇴 UI

- 일기 답변 완료 후 유저 포스트잇 하단 우측에는 연필 수정 아이콘을 둔다.
- 일기 답변 완료 후 AI 답변 하단 좌측에는 채팅 상세와 같은 사이렌 신고, 연필 수정, 휴지통 삭제 아이콘을 둔다. 기존 텍스트 `신고` 버튼은 사용하지 않는다.
- 일기 수정 모드는 포스트잇 전체를 textarea 수정 박스로 전환하고, 키보드가 열린 상태에서도 AI 답변·안내문구·일기 모아보기 버튼까지 스크롤로 확인 가능해야 한다.
- 일기 삭제 확인 모달은 삭제 버튼과 취소 버튼을 3:7 비율로 배치한다. 삭제 버튼은 빨간 배경·흰 텍스트, 취소 버튼은 회색 배경·진한 회색 텍스트를 사용한다.
- 채팅방에서는 사용자 메시지에 신고·수정 기능을 두지 않는다.
- 채팅방의 AI 캐릭터 답변에만 사이렌 아이콘 신고 버튼을 둔다.
- 한국어 일기 AI 답변은 저장 전 비한국어 문자 검수를 거친다. 영어 알파벳, 일본어, 중국어 한자, 기타 외국어 문자가 감지되면 한 번 재생성하고, 재생성 후에도 남아 있으면 저장하지 않는다.
- 개인정보 수정 화면의 회원 탈퇴 버튼은 화면 하단 영역에 연분홍 배경·빨간 텍스트로 표시한다.
- 외부 계정 삭제 페이지는 `/account-deletion` 단일 경로를 사용하고, 삭제 범위·본인 확인·최종 확인·완료 상태를 한 흐름에서 제공한다.
- 외부 계정 삭제 최종 확인 모달도 앱 회원 탈퇴 모달과 같은 3:7 버튼 비율과 위험 색상을 사용한다.

## 마이페이지 백업 안내

- 앱 빌드에서는 비로그인 UUID 원문 영역을 노출하지 않는다. 웹 비로그인 화면에서만 UUID 영역을 보여줄 수 있다.
- 비로그인 상태에서 생성한 캐릭터가 있을 때만 채팅내용 백업 버튼을 노출한다.
- 채팅내용 백업 버튼 아래에는 작은 안내 문구를 두며, 두 줄까지 허용한다.
- 백업 코드는 모달로 보여주고, 로그인 후 백업 코드 입력으로 기존 UUID 데이터를 로그인 uid로 옮긴다.

## 홈 메시지 카드

- 캐릭터 선톡 생성이 시작되면 홈 화면에도 채팅 카드가 즉시 노출되어야 한다.
- 선톡 작성 중에는 채팅방 입력중 상태와 같은 동그라미 3개 애니메이션을 표시한다.
- 선톡이 도착하면 입력중 애니메이션을 실제 메시지 첫 줄로 교체한다.
- 선톡 작성 중 카드도 누르면 해당 캐릭터의 채팅 상세로 이동한다.

## 사진 업로드와 완료 모달

- 홈 배경 사진 업로드 중에는 버튼 안에 회전 스피너를 표시하고, 중복 업로드 버튼 입력을 막는다.
- 홈 배경 사진은 업로드 전 앱에서 압축해 전송하고, 홈 화면에서는 페어별 배경 이미지를 미리 불러온다.
- 홈에서 페어를 전환할 때 새 배경 이미지가 준비되기 전에는 이전 배경을 유지해 흰 화면이나 배경 깜빡임이 보이지 않게 한다.
- 홈 배경 표시 속도 문제를 해결하기 위해 페어 전환마다 Firebase를 다시 조회하지 않는다. 이미 받은 이미지 URL을 기준으로 브라우저 이미지 프리로드를 사용한다.
- 업로드 중 사용자가 뒤로가기·앱 비활성화·화면 이탈을 하면 진행 중인 클라이언트 업로드를 중단하고, 이전 업로드의 늦은 완료 콜백은 화면과 DB에 반영하지 않는다.
- 배경화면 변경 완료 안내는 브라우저 기본 alert를 쓰지 않고 앱 모달을 사용한다.
- 배경화면 변경 완료 모달은 `홈으로`와 `편집 계속` 2버튼을 사용한다. 왼쪽 버튼은 연한 배경·진한 회색 텍스트, 오른쪽 버튼은 진한 회색 배경·밝은 회색 텍스트로 표시한다.

## 하단 안전영역

- Android 시스템 하단바와 iOS 홈 인디케이터가 CTA나 앱 하단 탭을 가리면 안 된다.
- 하단 고정 CTA는 화면별 인라인 `position: fixed; bottom: 0`을 직접 쓰지 않고 공통 `.fixed-cta-footer`를 사용한다.
- 하단 고정 CTA가 있는 풀페이지는 `.fixed-cta-page`와 `.fixed-cta-content`를 함께 사용한다.
- 앱 하단 탭과 고정 CTA는 `--bottom-ui-safe-gap`을 기준으로 하단 여백을 잡는다.
- Android 앱에서는 `--bottom-ui-safe-gap`이 최소 48px 이상 확보되어야 한다.
- 단, 소프트웨어 키보드가 열린 동안에는 Android 시스템 하단바 보정값 48px을 CTA 아래에 더하지 않는다.
- 키보드 입력 화면의 CTA는 키보드 바로 위에 노출하며, CTA와 키보드 사이 여백은 8px 수준으로 유지한다.
- Android는 키보드가 WebView 높이를 줄이므로 CTA에 키보드 높이를 다시 더하지 않는다. iOS의 `KeyboardResize.None`에서만 키보드 높이만큼 CTA를 올린다.
- 본문 스크롤 영역은 하단 고정 CTA 높이와 키보드 스크롤 여백을 함께 확보해 마지막 입력 영역이 CTA·키보드 뒤로 숨지 않게 한다.
- 고정 CTA가 있는 풀페이지 입력 화면은 CTA 위치용 `--keyboard-height`와 본문 스크롤용 `--keyboard-scroll-height`를 구분한다. Android는 CTA를 중복으로 올리지 않되, 본문은 키보드 높이만큼 더 스크롤 가능해야 한다.
- 채팅 상세 입력창은 `.chat-input-area`를 사용하며, 키보드가 닫혔을 때는 홈 인디케이터 안전영역만 반영하고 키보드가 열렸을 때는 8px 여백만 둔다.
- 채팅 상세 입력 textarea는 1줄에서 시작하고 최대 6줄까지만 높이를 확장한다. 6줄을 넘는 입력은 textarea 내부 스크롤을 사용한다.
- 일기 작성 textarea는 고정 높이 내부 스크롤을 쓰지 않고 내용 길이에 맞춰 세로로 확장한다. 키보드가 열린 iOS에서도 `.diary-content` 본문이 스크롤되어 작성 중인 긴 내용을 확인할 수 있어야 한다.
