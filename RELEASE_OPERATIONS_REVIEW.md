# Dreamary 출시 전 운영·확장성 검토

검토일: 2026-09-11. 기준 커밋: `2b9d257` 및 현재 작업 폴더. 코드 수정·운영 배포 없이 검토했다.

## 판단

현재 Next.js·Capacitor·Netlify·Firestore 조합을 전면 교체할 필요는 없다. 먼저 해결해야 할 것은 **사용량 상한, 오래 쌓인 데이터의 처리 범위, 동시 요청, 장애 복구와 배포 통제**다. 서버가 자동으로 늘어나는 것만으로 이러한 문제가 해결되지는 않는다.

사용자의 앱 정상 동작 확인은 기능 점검으로 반영했다. 다만 적은 데이터로 한 번 성공하는 것과 장기 사용·동시 사용자 증가에 견디는 것은 별도 검증이다. 이번 검토에서는 운영 DB 본문·사용자 목록 조회, 실제 AI 호출, 이메일 발송, 부하 테스트를 하지 않았다. 현재 요금제·실제 비용·트래픽·백업 설정·외부 방화벽 설정은 미확인이다.

기존 전체 공개 Firestore 규칙은 **2026-09-10에 교체 완료한 문제**다. 이번 보고서는 이를 현재 미해결 문제로 다시 분류하지 않는다. 아래는 현재 코드에서 추가로 확인한 위험과 운영 설정에서 확인할 항목이다.

## 우선순위와 의사결정

“출시 전”은 공개 사용자에게 제공하기 전에 개선하거나 명확한 운영 대안을 마련하자는 권고다. 실제 피해·장애가 발생했다는 뜻은 아니다. “코드 확인”은 해당 경로를 직접 읽었다는 뜻이며 실환경 재현과는 구분한다.

| 항목 | 시점 | 사용자가 겪을 수 있는 문제 / 운영 영향 | 권장 대응 | 변경 영역 |
| --- | --- | --- | --- | --- |
| R1. 채팅 ID 충돌 | 출시 전 | 여러 사용자가 같은 밀리초에 보내면 일부 전송 실패 | 시간값 대신 충분한 난수 ID, 재전송 시 ID 유지 | 앱 |
| R2. 비밀번호 재설정 | 출시 전 | 메일 확인 전 비밀번호 변경, 메일 실패 시 로그인 불가·문의 증가 | 만료되는 일회용 이메일 링크로 본인 확인 후 변경 | 서버·앱 |
| R3. AI·기기 등록 사용량 제한 | 출시 전 | 정상 과다 사용·자동 요청만으로 비용과 호출 한도 소진 | 서버에서 사용자별·전체 비용·동시 처리 상한, 요청 크기 제한 | 서버·운영 설정 |
| R4. 채팅 전체 조회·구독·삭제 | 출시 전 | 오래 쓴 사람일수록 느리고 읽기 비용 증가 | 최신 20~30개, 이전 기록 더보기, 제한된 구독·삭제 | 앱·서버 |
| R5. 알림 200명·직렬 처리 | 알림을 출시 기능으로 제공하기 전 | 당일 대상이 200명을 넘으면 일부 미발송. 대상이 적어도 시간 초과 가능 | 작은 작업 묶음·다음 묶음 처리·재시도·발송 상태 | 서버 |
| R6. 테스트/운영 격리·배포 검사·호환 | 출시 전 | 테스트가 실데이터에 영향, 구버전 앱 장애, 검사 실패 코드 배포 | 별도 테스트 DB, 자동 검증, 이전 앱 호환, 되돌리기 절차 | 빌드·서버·운영 |
| R7. 백업·복구·장애 알림 | 출시 전 설정 확인 | 오삭제·배포 사고를 늦게 알아차리거나 복구 불가 | 백업 정책·복원 실습·비용/오류 알림·대응 담당 | 운영 설정 |
| R8. 일기 목록 정렬·인덱스 | 출시 전 | 일기가 쌓이면 최신 일기가 첫 페이지에서 빠짐 | 서버 쿼리 정렬과 커서 일치, 인덱스 버전 관리 | 앱·DB 설정 |
| R9. AI 중복 실행·시간 초과 | 출시 전 최소 방어 | 답장 한 개에 AI 비용 여러 번, 계속 대기·재시도 | 처리 중 상태 예약, 시간 한도, 결과 재조회 | 서버·앱 |
| R10. 큰 데이터 삭제·이전 | 장기 데이터가 쌓이기 전 | 탈퇴/이전 도중 중단, 재실행을 반복해야 완료 | 서버 작업으로 이어서 처리, 진행 상태와 재개 보장 | 서버·앱 |
| R11. 이미지 원본·캐시 | 출시 전 저장 책임 결정, 용량 관리는 성장 전 | 원본 유실, 임의 업로드, 기기 저장 공간 증가 | 소유권 있는 저장소·업로드 검증·삭제 경로·캐시 상한 | 서버·앱·스토리지 |
| R12. 페어·주제·인증 메타데이터 증가 | 성장 전 | 진입 시 호출 수 증가, 사용하지 않는 문서 계속 축적 | 페어 정책·페이지 조회, 주제 묶음 캐시, 보존 기한 | 앱·서버·DB |
| R13. 신고 처리 | 출시 전 최소 수동 운영 확보 | 신고 접수 후 방치, 중복 신고·메일, 처리 상태 불명 | 담당자·처리 기한·처리 상태, 이후 관리 화면 | 운영·서버·어드민 |

## 코드에서 확인한 근거와 완료 기준

### R1. 채팅 메시지의 고유 번호가 사용자 전체에서 충돌할 수 있음

사용자 메시지는 `Date.now().toString()`을 ID로 만들고 모든 사용자 메시지를 공통 `chatMessages/{id}` 경로에 저장한다. 다른 사용자가 같은 시간값을 생성하면 같은 문서를 가리킨다. 현재 규칙은 타인의 문서 덮어쓰기를 거부하므로 대표적인 결과는 **전송 실패**다. 같은 사용자의 같은 대화방·여러 창에서 동일 ID로 저장되면 기존 메시지가 바뀔 여지도 있다. 충돌 빈도는 실제 전송 시각 분포를 측정하지 않아 추정하지 않았다.

권장: 난수 UUID 또는 Firestore 자동 ID를 사용하고, 같은 논리적 전송을 재시도할 때는 기존 ID를 재사용한다. 서버 답장의 결정적 ID와 사용자 메시지 ID를 함께 연결하면 실패 복구도 쉬워진다.

완료 기준: 시간을 고정한 채 다른 사용자·같은 사용자 여러 창에서 보내도 각각 저장된다. 동일 전송 재시도에는 중복 메시지가 생기지 않는다.

근거: [사용자 메시지 생성](</Users/shinwonlee/Desktop/dreamary/src/app/chat/[id]/page.tsx:286>), [저장 위치](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:452>), [현재 채팅 규칙](</Users/shinwonlee/Desktop/dreamary/firestore.rules:39>).

### R2. 비밀번호 재설정이 계정 접근 장애를 만들 수 있음

기존 재설정 화면은 `/.netlify/functions/reset-password`를 호출했으나 2026-09-15에 운영과 같은 `/api/auth/reset-password` 경로로 전환했다. 두 함수는 아이디·이메일 일치만으로 요청을 처리하고, 2026-09-15 수정으로 메일 발송이 성공한 뒤에만 비밀번호를 변경한다. 본인이 이메일을 열었다는 확인은 없으므로 타인이 두 정보를 알면 재설정을 요청할 수 있다. Resend가 메일을 거부하면 기존 비밀번호는 유지된다.

실제로 연결된 레거시 함수에는 `Math.random()` 임시 비밀번호 생성 코드가 남아 있다. 또한 메일이 성공적으로 접수된 뒤 Firebase 비밀번호 변경이 실패하면 사용자가 받은 임시 비밀번호가 즉시 사용되지 않을 수 있으므로, 장기적으로는 Firebase 만료 재설정 링크 방식으로 통합하는 것이 안전하다. 스테이징의 Resend 키는 테스트 모드여서 소유자 테스트 주소 외의 수신자를 거부한다는 점도 확인했다.

권장: Firebase의 만료되는 이메일 재설정 링크 등으로 통합한다. 사용자가 링크를 확인해 새 비밀번호를 정하기 전까지 기존 비밀번호를 유지한다. 호출 제한과 계정 존재 여부를 과도하게 알려주지 않는 응답을 적용한다. 이전 함수 경로도 함께 정리해 우회 경로를 남기지 않는다. [Firebase 사용자 관리](https://firebase.google.com/docs/auth/web/manage-users)

완료 기준: 메일 장애·반복 요청·만료 링크·사용한 링크로 기존 계정 접근이 임의로 바뀌지 않는다. 실제 개인 계정이나 메일을 이용한 공격성 검사는 하지 않는다.

근거: [실제 화면 호출](</Users/shinwonlee/Desktop/dreamary/src/app/(auth)/reset-password/page.tsx:28>), [운영과 동일한 함수](</Users/shinwonlee/Desktop/dreamary/netlify/functions/auth-reset-password.ts:58>), [레거시 함수](</Users/shinwonlee/Desktop/dreamary/netlify/functions/reset-password.mts:63>).

### R3. 인증은 있지만 비용을 제한하는 공통 장치가 부족함

채팅 API는 본인 여부와 캐릭터 소유권을 확인한다. 그러나 `messages`가 배열인지 확인한 뒤 길이·각 본문 크기·허용 role의 충분한 검증 없이 AI 공급자에 전달한다. 정상 앱은 최근 10개만 보내지만 서버에서 강제하는 상한은 아니다. 출력의 `max_tokens` 설정은 입력 비용이나 요청 횟수까지 제한하지 않는다. 일기는 사용자 본문 4,000자 제한이 있으나 캐릭터 설정·주제 등의 전체 요청 예산과 하루 생성 횟수 방어를 별도로 설계해야 한다.

비로그인 등록, AI API, 관리자 로그인 등에 공통 속도 제한·동시 처리 제한이 코드에서 확인되지 않았다. 외부 플랫폼에 별도 제한이 설정되어 있는지는 미확인이다. 백업 코드의 하루 발급·오입력 제한은 이미 구현돼 있으므로 전체가 무제한인 것은 아니다.

광고 카운트는 기기의 localStorage이고 AI 서버가 광고가 실제 열렸다는 증명을 검증하지 않는다. 현재 외부 광고 웹페이지의 열림 확인은 UI 흐름의 확인이며, 확실한 수익 발생 증명이 아니다. 광고 수익을 AI 비용의 안전장치로 계산하면 안 된다.

권장: 사용자·기기별 분당 요청, 하루 AI 예산, 처리 중 요청 수, 메시지/설정 크기를 서버에서 제한한다. 플랫폼 차단·App Check 등은 보조 수단으로 검토하되 기존 소유권 인증을 대체하지 않는다. 여러 서버 인스턴스가 공유하는 제한을 사용하고 모든 사용자의 요청을 한 문서에 집중시키지 않는다. 광고 수익 검증이 꼭 필요하면 제공자가 지원하는 검증 방식의 존재부터 확인한다.

완료 기준: 정상 UI 밖에서 요청해도 상한을 넘으면 AI 호출 전에 거부된다. 예산 도달 시 신규 AI 생성만 제한하고 기존 일기·채팅 열람은 가능한 상태로 유지한다.

근거: [채팅 검증](</Users/shinwonlee/Desktop/dreamary/netlify/functions/chat.ts:50>), [AI 입력 전달](</Users/shinwonlee/Desktop/dreamary/netlify/functions/chat.ts:195>), [일기 검증](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:52>), [게스트 등록](</Users/shinwonlee/Desktop/dreamary/netlify/functions/guest-session.ts:16>), [광고 카운트](</Users/shinwonlee/Desktop/dreamary/src/lib/adTracker.ts:74>).

### R4. 오래 사용한 채팅방은 과거 기록 전체를 반복해서 읽음

채팅방 진입 시 전체 기록을 한 번 조회하고, 같은 범위의 전체 실시간 구독을 다시 연다. 둘 다 `limit`이 없다. 화면도 누적 메시지를 모두 상태에 올려 정렬·렌더링한다. AI에 보내는 기록은 최근 10개뿐이므로 과거 전체를 매번 읽을 필요는 없다.

초기 전체 조회와 구독은 각각 비용을 만들 수 있다. 구독 중 메시지 한 건이 추가될 때마다 과거 전체가 매번 청구된다는 뜻은 아니다. 재연결·새 구독 과금도 캐시 설정에 따라 달라진다. 현재 래퍼의 일회성 조회는 서버 조회를 강제한다. [Firestore 과금](https://firebase.google.com/docs/firestore/pricing)

대화 삭제도 전체 조회 후 모든 삭제 요청을 `Promise.all`로 동시에 보낸다. 큰 대화방에서는 느려지거나 일부만 삭제될 수 있다. 또한 채팅 상세 초기화가 끝나기 전에 화면을 떠나면, 정리 함수가 아직 할당되지 않아 뒤늦게 생성된 구독이 남을 수 있는 경로가 있다. 공통 래퍼의 계정 확인이 있어도 이 화면의 늦은 구독 생성 자체는 해결하지 못한다.

권장: 최신 20~30개 구독, 과거 20~30개씩 더보기, 필요한 경우 화면에 보이는 부분만 렌더링한다. 화면 이탈 취소 여부를 초기화의 각 비동기 단계에서 확인한다. 대화 삭제는 권한 있는 서버에서 작은 묶음으로 이어서 처리한다.

완료 기준: 100개·1,000개·10,000개 기록에서 첫 진입 읽기 상한이 같고, 화면 왕복 후 활성 구독 수가 증가하지 않는다. 최신 메시지·과거 더보기·삭제 중 재접속도 확인한다.

근거: [조회·구독](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:394>), [삭제](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:460>), [상세 초기화·정리](</Users/shinwonlee/Desktop/dreamary/src/app/chat/[id]/page.tsx:180>), [서버 조회 래퍼](</Users/shinwonlee/Desktop/dreamary/src/lib/dataFirestore.ts:19>).

### R5. 교환일기 알림은 하루 최대 200개 대상까지만 처리함

예약 함수는 하루 한 번 20시에 실행되고 대상 쿼리에 `limit(200)`을 사용한다. 다음 페이지 반복·별도 후속 실행 예약이 없다. 당일 대상이 201명이면 첫 실행에서 적어도 한 명이 빠지고, 계속 1,000명이 대상이면 하루 처리량 200으로 매일 모두 알릴 수 없다. 이는 가입자 총수 대신 **해당 시각 알림 처리 대상 수** 기준이다.

대상 사용자를 순차 처리하고 iOS 기기도 하나씩 전송한다. 따라서 200명 이하여도 외부 응답 지연으로 실행 시간이 부족할 수 있다. Android 전송 등의 예외가 사용자 루프 밖으로 나오면 뒤의 대상도 처리되지 않는다. 모든 기기 발송이 실패해도 다음 알림 시각을 다음 날로 넘기는 흐름이라 일시 장애 복구가 약하다. 공식 예약 함수 제한은 30초다. [Netlify 예약 함수](https://docs.netlify.com/build/functions/scheduled-functions/)

권장: 20시는 작업을 시작하는 시각으로 사용하고, 실제 발송은 작은 묶음을 계속 처리하는 작업 대기열로 분리한다. 날짜+사용자 기준 중복 방지, 제한된 동시 전송, 일시 실패만 재시도, 실패 대상만 다시 처리할 수 있는 기록을 둔다. 숫자 200만 크게 바꾸는 것은 해결이 아니다.

완료 기준: 발송 대역으로 201명·1,000명을 처리해 누락이 없고, 중간 실패·재실행에도 중복과 다음 대상 누락이 없다. 실제 대량 푸시로 테스트하지 않는다.

근거: [일정과 한도](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary-push-scheduler.mts:16>), [대상 조회](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary-push-scheduler.mts:161>), [전송 및 다음 날짜 처리](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary-push-scheduler.mts:228>).

### R6. 배포와 앱 버전 증가를 견딜 운영 장치가 필요함

테스트 앱과 운영 앱은 API 주소가 다르지만 Firebase 프로젝트가 같다. 출시 후 테스트 기능·DB 규칙·테스트 데이터 정리가 실제 사용자에게 영향을 줄 수 있다. 테스트 DB 분리는 개발·운영 로직을 다르게 만드는 것이 아니라 **같은 코드와 규칙을 다른 데이터에 적용하는 것**이다.

Netlify의 `npm run build`는 Next 빌드를 사용하고, Next 설정은 타입·린트 오류를 빌드에서 무시한다. prebuild에 함수 구조와 인증 런타임 검사는 있지만 보안·성능·Rules 전체 검사가 연결돼 있지는 않다. GitHub 외부의 필수 검사나 보호 설정은 확인하지 않았다. 수동으로 `check:app`을 실행해도 이후 모든 서버 배포가 자동 보호되는 것은 아니다.

현재 코드에서는 공통 최소 지원 앱 버전·API 계약 버전·운영 기능 중지 스위치를 찾지 못했다. API와 DB 스키마를 변경하면 새 앱뿐 아니라 업데이트하지 않은 이전 앱을 함께 지원해야 한다. 앱은 DB를 직접 읽기도 하므로 API 버전만 도입해도 모든 호환 문제가 해결되는 것은 아니다.

권장: 독립 테스트 Firebase 프로젝트, 배포 전 타입·기존 회귀·demo Rules 테스트, 운영 앱 대상 확인, 이전 앱과 새 서버 조합 검사, 인덱스/규칙 버전 관리를 자동화한다. API 필드는 우선 추가하고, 이전 클라이언트 전환을 확인한 뒤 제거한다. 최소 버전 안내는 긴급 수단이지 호환성 유지의 대체가 아니다.

완료 기준: 검사가 실패한 커밋은 운영 배포되지 않고, 테스트 앱으로 만든 데이터가 운영 DB에 쌓이지 않는다. 이전 서버로 되돌렸을 때 현재 DB를 읽을 수 있는지까지 확인한다.

근거: [빌드 대상](</Users/shinwonlee/Desktop/dreamary/app-build-targets.json:1>), [무시 설정](</Users/shinwonlee/Desktop/dreamary/next.config.mjs:23>), [현재 검사 연결](</Users/shinwonlee/Desktop/dreamary/package.json:7>), [빌드 대상 검증](</Users/shinwonlee/Desktop/dreamary/scripts/build-app.cjs:5>).

### R7. 장애 복구와 비용·오류 알림은 설정 확인이 필요함

저장소에는 주로 로컬 진단 기록과 서버 콘솔 로그가 있고, 일기 API의 단계별 시간 기록이 구현돼 있다. 그러나 자동 장애 호출·비용 임계치·복원 실습 기록은 이번 확인 범위에서 찾지 못했다. 클라우드 콘솔에서 별도로 활성화했을 가능성이 있으므로 “백업이 없다”라고 단정하지 않는다.

앱의 “채팅내용 백업 코드”는 데이터를 다른 계정으로 옮기는 접근 수단이다. DB가 삭제되거나 오염됐을 때 과거 상태를 되살리는 운영 백업은 아니다. DB 백업만으로 Firebase Auth 계정, 이미지 원본, 서버 설정까지 모두 복구되는 것도 아니다.

권장: 허용할 데이터 손실 시간과 복구 목표 시간을 정하고 DB 정기 백업·시점 복구를 검토한다. 예를 들어 “최대 하루치 손실, 몇 시간 내 복구”는 선택할 운영 목표이지 현재 보장값이 아니다. 격리 환경에서 실제 복원해 일기·캐릭터·로그인 연결을 확인하고, 코드/DB/이미지/인증 설정을 각자 복구할 절차를 마련한다. [Firestore 백업](https://firebase.google.com/docs/firestore/backups), [시점 복구](https://firebase.google.com/docs/firestore/use-pitr)

운영자가 볼 최소 지표는 채팅·일기 성공률, 응답 시간, AI 요청 수·토큰·비용, Firestore 읽기/쓰기, 알림 미처리 건수, 앱 버전별 오류다. HTTP 200 안에 오류 JSON이 반환되는 스트림도 있으므로 상태 코드만으로 성공을 집계하면 안 된다. 비용 알림은 자동 지출 상한이 아니어서 서버 사용 제한과 함께 둬야 한다. [Firebase 요금·예산 알림](https://firebase.google.com/docs/projects/billing/firebase-pricing-plans)

근거: [성능 기록](</Users/shinwonlee/Desktop/dreamary/src/lib/performanceTrace.ts:1>), [스트림 내부 오류](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:302>), [백업 코드 발급](</Users/shinwonlee/Desktop/dreamary/netlify/functions/backup-generate.ts:18>).

### R8. 일기 더보기의 정렬이 DB 조회 순서와 다름

일기 페이지 쿼리는 `limit`과 커서를 쓰지만 날짜 정렬을 지정하지 않는다. 화면이 이미 받은 일부 문서만 최신순으로 정렬한다. Firestore 기본 순서는 문서 ID 기준이므로, 이 방식으로 전체 기록의 최신 10개를 보장할 수 없다. 현재 일기 ID는 `daily_...날짜` 구조여서 오래된 날짜가 먼저 조회될 수 있고, 이전된 문서와 옛 ID도 섞일 수 있다. [Firestore 기본 정렬](https://firebase.google.com/docs/firestore/query-data/order-limit-data)

최신 채팅 쿼리 실패 시의 30개 fallback도 정렬 없이 30개를 받은 다음 최신을 고르므로 “그 30개 중 최신”일 뿐 전체 대화의 최신이 아닐 수 있다. 읽기 폭주를 막는 장점은 있지만 정확성 문제가 남는다.

권장: 날짜/생성 시각과 ID로 안정적인 정렬·커서를 만들고 실제 복합 인덱스를 코드와 함께 관리한다. `orderBy`를 추가하기 전에 옛 문서의 정렬 필드 유무를 확인해야 한다. 전체 조회 fallback으로 해결하지 않는다.

완료 기준: 35개 이상 일기, 생성 시각이 같은 문서, 이전된 문서가 섞여 있어도 첫 페이지가 최신이며 더보기에서 누락·중복이 없다. 현재 운영 인덱스는 이번에 조회하지 않았다.

근거: [일기 페이지 쿼리](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:203>), [화면 정렬](</Users/shinwonlee/Desktop/dreamary/src/app/diary/history/page.tsx:36>), [일기 ID](</Users/shinwonlee/Desktop/dreamary/src/lib/diaryIdentity.ts:6>), [채팅 fallback](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:421>).

### R9. 중복 저장 방어와 중복 AI 비용 방어는 다름

채팅과 일기는 이미 저장된 요청을 확인하고 마지막 저장을 트랜잭션으로 처리한다. 이 보호는 유지해야 한다. 하지만 두 요청이 저장되기 전에 동시에 들어오면 둘 다 AI 생성을 시작할 수 있다. 최종 문서는 하나여도 AI 호출 비용은 두 번 발생할 수 있다. 채팅 화면 재시도는 새로운 requestId를 만든다.

AI fetch 자체에 명시적인 시간 제한·외부 요청 취소 신호가 없다. 앱의 `apiPostJson`에는 제한이 있지만, 현재 일반 채팅 화면은 `apiFetch` 스트림을 직접 사용하고 이 경로에는 제한이 없다. `isNativeChat` 변수도 선언만 되어 있어 문서의 “네이티브 일반 채팅은 JSON” 설명과 현재 코드가 다르다. 사용자가 정상 동작을 확인한 상태에서 이를 현재 장애라고 단정하지 않지만, 지연·연결 중단 시험이 필요한 지점이다. 응답 중 공백 전송은 연결 유지를 돕지만 함수의 최대 실행 시간을 없애주지 않는다.

추가로 OpenRouter 스트림 파서는 완료 표식·오류 이벤트·finish reason을 엄격히 확인하지 않고 비어 있지 않은 내용을 저장한다. 공급자가 일부 내용 후 오류 이벤트를 보내고 정상 EOF로 연결을 닫는 경우를 실패로 구분하는 검증이 필요하다.

권장: 생성 전에 논리적 요청 ID의 처리 권한과 만료 시각을 예약한다. 재요청은 기존 작업 상태·결과를 읽고, 실패/시간 초과 시 같은 ID로 안전하게 재개한다. 짧은 서버 제한과 상태 재조회를 우선 도입하고, 긴 생성은 실제 지연 자료에 따라 작업 대기열로 분리한다. 여러 탭·기기·서버 인스턴스에서도 동작해야 한다.

완료 기준: 동시 중복 요청, 클라이언트 연결 끊김, 공급자 429/5xx, 응답 중단, 완료 후 응답 유실을 대역으로 시험해 저장과 비용 발생 횟수를 구분한다.

근거: [채팅 조회 후 AI 호출](</Users/shinwonlee/Desktop/dreamary/netlify/functions/chat.ts:77>), [일기 생성 전 조회](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:69>), [서버 AI fetch](</Users/shinwonlee/Desktop/dreamary/netlify/functions/diary.ts:184>), [화면 스트리밍 경로](</Users/shinwonlee/Desktop/dreamary/src/app/chat/[id]/page.tsx:334>), [API 래퍼](</Users/shinwonlee/Desktop/dreamary/src/lib/api.ts:15>), [스트림 파서](</Users/shinwonlee/Desktop/dreamary/netlify/functions/chat.ts:13>).

### R10. 페이지 크기가 작아도 작업 전체 시간이 제한되는 것은 아님

회원 탈퇴는 20개씩 삭제하지만 한 HTTP 요청 안에서 비어질 때까지 반복한다. 오래 쓴 계정에서는 함수 제한에 걸려 일부 데이터만 삭제된 상태가 될 수 있다. 재시도 가능한 부분은 있으나 서버가 독립적으로 끝까지 이어가는 작업·진행 상태가 없다.

캐릭터 삭제와 백업 이전은 요청별 범위를 제한하고 재개하도록 작성돼 있어 상대적으로 낫다. 다만 이전 클라이언트는 한 번에 최대 100페이지를 순차 호출한 뒤 다시 시도하라고 안내한다. 단계 전환용 요청을 제외하면 약 2,000문서 내외부터 한 번에 끝나지 않을 수 있다. 20회 채팅을 한 사용자에게 하루 40개 채팅 문서가 생긴다면 수개월이 아니라 수십 일의 기록에서도 고려할 문제다.

권장: 삭제·이전 작업의 상태를 서버에 남기고 화면을 닫아도 작은 묶음이 이어서 실행되게 한다. 삭제 시작 이후 새 쓰기를 막는 계정 상태도 공통 경로에서 확인한다. 중단·재시도·완료 실패·인증 만료별 복구 절차를 둔다.

완료 기준: 10,000문서 계정의 삭제/이전 중 앱 종료·작업 시간 초과·재로그인을 거쳐도 타인 데이터에 영향 없이 완료되며 사용자에게 진행 상태를 표시한다. 합성 데이터와 격리 환경에서 검증한다.

근거: [탈퇴 삭제 반복](</Users/shinwonlee/Desktop/dreamary/src/lib/server/accountCleanup.ts:3>), [탈퇴 함수 연결](</Users/shinwonlee/Desktop/dreamary/netlify/functions/account-delete.mts:79>), [이전 반복 한도](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:19>), [서버 이전 페이지](</Users/shinwonlee/Desktop/dreamary/netlify/functions/backup-migrate.ts:66>), [캐릭터 삭제](</Users/shinwonlee/Desktop/dreamary/netlify/functions/character-delete.ts:16>).

### R11. 이미지 저장과 캐시의 책임이 불명확함

클라이언트에 포함되는 ImgBB 업로드 키로 외부 이미지 서버에 직접 업로드한다. 반환된 응답에서 URL만 저장하며 원본 복구·삭제 식별자 관리 경로는 보이지 않는다. 공개 Firebase 설정과 달리 공유 이미지 업로드 키는 임의 업로드에 사용될 수 있는 권한이므로 발급·제한·회수 정책이 필요하다. 키 원문은 조회하지 않았다.

30일 이미지 캐시는 시간이 지난 항목을 사용하지 않을 뿐 실제로 제거하지 않는다. 저장 용량·항목 수 상한도 없다. 배경 이미지 미리보기는 Blob URL을 계속 Map에 보관하며 해제·퇴출 경로가 없다. 사진을 자주 바꾸는 장기 사용자에게 저장 공간·메모리 부담이 쌓일 수 있다.

권장: 사업자가 원본과 삭제를 통제할 수 있는 저장소, 사용자별 경로·인증·용량 제한·파일 검증·압축을 갖춘 업로드 경로를 검토한다. 기존 이미지를 한꺼번에 다시 옮기기 전에 보존·삭제 정책과 소량 이전부터 확인한다. 로컬 캐시는 바이트/개수 상한, 오래 쓰지 않은 항목 제거, 만료 삭제, Blob 해제를 적용한다. 기기 캐시는 운영 백업으로 취급하지 않는다.

완료 기준: 업로드 실패·외부 원본 404·계정 삭제·캐시 용량 초과·수백 장 사진 변경을 시험한다. 개인 이미지에 URL을 아는 누구나 접근해도 되는지도 제품 정책으로 결정한다.

근거: [직접 업로드·URL 저장](</Users/shinwonlee/Desktop/dreamary/src/lib/imgbb.ts:165>), [클라이언트 키](</Users/shinwonlee/Desktop/dreamary/src/lib/imgbb.ts:188>), [캐시 만료 판정](</Users/shinwonlee/Desktop/dreamary/src/lib/imageCache.ts:44>), [배경 메모리 보관](</Users/shinwonlee/Desktop/dreamary/src/lib/imagePreload.ts:4>).

### R12. 페어·주제·일시 인증 문서에도 증가 정책이 필요함

사용자의 캐릭터 목록과 주제 목록은 전체 조회다. 주제는 5분 캐시가 있고 프로필도 캐시돼 있어 매번 전체 조회하는 구조는 아니지만, 캐시가 없는 첫 사용과 런타임 변경 때 비용이 생긴다. 푸시 후보 재계산에는 해당 사용자의 페어별 조회·집계가 추가된다. 페어 최대 개수와 주제 묶음 전략이 없다면 가입자 수가 적어도 일부 사용자의 큰 데이터가 느려질 수 있다.

백업 코드·시도·발급 횟수 기록은 계속 만들어진다. 코드의 숫자형 `expiresAt`은 인증에서 만료를 확인하지만 자동 삭제를 의미하지 않는다. Firestore TTL은 날짜/시간 필드와 정책이 필요하므로 필드 이름만 있다고 정리되는 것이 아니다. 완료된 이전의 재개 보장, 재연결 방지용 retired 문서는 임의로 삭제하면 안 된다. [Firestore TTL](https://firebase.google.com/docs/firestore/ttl)

권장: 페어 수 정책을 정하고 목록은 페이지화한다. 자주 쓰는 주제는 버전 있는 묶음·서버 캐시를 검토한다. 일시 기록은 재개에 필요한 기간을 고려해 정리하고, 개인정보·이미지·탈퇴 기록별 보존 정책을 명시한다. 사용자의 정상 일기·채팅을 임의로 오래됐다는 이유만으로 삭제하지 않는다.

근거: [캐릭터 목록](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:89>), [주제 목록](</Users/shinwonlee/Desktop/dreamary/src/lib/db.ts:174>), [푸시 후보 계산](</Users/shinwonlee/Desktop/dreamary/src/lib/server/pushShared.ts:92>), [임시 코드 생성](</Users/shinwonlee/Desktop/dreamary/netlify/functions/backup-generate.ts:20>).

### R13. 신고 접수 이후 처리할 운영 절차가 필요함

신고는 본인 답변을 검증하고 저장하므로 기본 접수 경로는 있다. 그러나 같은 대상에 반복 신고해도 새 문서가 생기고, 접수마다 미처리 전체 count를 계산한다. 알림 상태 확인→메일→상태 저장이 원자적이지 않아 동시에 신고가 들어오면 중복 메일이 생길 수 있다. 상태 읽기가 실패하면 신고는 저장됐어도 요청 전체는 실패해 재신고로 이어질 수 있다.

관리자 알림은 5건 단위이고 메일 설정이 없으면 알림 없이 저장한다. 신고가 1~4건만 있어도 필요한 조치를 할 수 있도록 별도 정기 확인이 필요하다. 현재 메일 안내는 Firebase Console을 보도록 되어 있어, 접수 이후 누가 어떤 기준으로 처리하고 종결하는지 운영 절차가 중요하다.

권장: 출시 시에는 수동 처리여도 담당자·확인 주기·처리 기한·상태 변경·긴급 사안 대응을 확정한다. 이후 중복 접수 제어와 알림 작업 분리, 담당자·처리 이력 관리 화면을 추가한다. 관리자 화면을 예쁘게 만드는 작업보다 실제 처리 절차가 우선이다. AI 유해 답변·연령·개인정보 취급에 관한 정책 검토도 별도 필요하며, 이번 검토는 그 전체 적합성을 인증한 것이 아니다.

근거: [신고 저장·count](</Users/shinwonlee/Desktop/dreamary/netlify/functions/report-create.ts:110>), [메일 임계치·상태](</Users/shinwonlee/Desktop/dreamary/netlify/functions/report-create.ts:43>).

## 어드민은 다음 순서로 정리

| 대상 | 확인 내용 | 우선순위 / 대응 |
| --- | --- | --- |
| 대시보드 통계 | 전체 본문 조회 대신 count를 사용한 것은 적절함. 매 조회마다 4개 전체 집계 | 낮음. 데이터가 많아지고 자주 열면 짧은 통계 캐시·요약 문서 검토. count도 항상 무료·상수 비용은 아님 |
| 사용자 목록 | 최대 20개 페이지와 사용자별 count | 낮음. 현재 구조 유지하며 실제 사용 빈도·응답 시간을 먼저 측정 |
| 사용자 상세 | 해당 사용자의 모든 캐릭터를 읽고 각 캐릭터의 집계 2개와 프로필 조회를 동시 실행 | 중간. 페어 수가 늘면 큰 요청이 됨. 페어 20개와 더보기·통계 지연 로딩 |
| 주제 관리 | 서버가 20개씩 읽지만 한 요청 안에서 최대 100페이지를 누적 | 낮음. 주제가 늘면 화면 페이지와 요청 페이지를 일치시키고 일괄 번역은 별도 작업 |
| 관리자 권한·업무 이력 | 서명 세션·HttpOnly 쿠키는 구현. 공용 비밀번호 기반이며 세션에 운영자 식별자 없음 | 운영자가 늘기 전 개선. 개별 계정·다중 인증·역할 제한·조회/수정 이력. 1인 운영도 로그인 요청 제한은 확인 |

근거: [통계](</Users/shinwonlee/Desktop/dreamary/src/app/api/admin/stats/route.ts:20>), [사용자 목록](</Users/shinwonlee/Desktop/dreamary/src/app/api/admin/users/route.ts:38>), [사용자 상세](</Users/shinwonlee/Desktop/dreamary/src/app/api/admin/user-detail/route.ts:38>), [주제 누적](</Users/shinwonlee/Desktop/dreamary/src/lib/server/adminTopics.ts:4>), [관리자 로그인](</Users/shinwonlee/Desktop/dreamary/netlify/functions/admin-login.ts:17>), [세션](</Users/shinwonlee/Desktop/dreamary/src/lib/server/adminSession.ts:8>).

## 사용자 규모별로 무엇이 늘어나는가

사용자 규모와 예산이 아직 정해지지 않아 다음을 **설명용 가정**으로 계산했다. 서비스 처리 가능 인원이나 요금 견적이 아니다.

- 일 활성 사용자 1명당 하루 채팅 20회, 일기 1회.
- 채팅 1회는 사용자 메시지 1개 + AI 답장 1개, 일기 1회는 문서 1개.
- 채팅방 기록은 사용자당 1,000문서, 하루 새 진입 2회. 전체 일회성 조회와 새 전체 구독을 각각 한 번 수행한다고 가정.
- 트랜잭션·권한·프로필·주제·인덱스·재연결·추가 구독 변경·관리자·사진 비용은 아래 숫자에서 제외.

| 지표 | 일 사용자 100명 | 일 사용자 1,000명 | 일 사용자 10,000명 |
| --- | ---: | ---: | ---: |
| 기본 AI 생성/일 | 2,100회 | 21,000회 | 210,000회 |
| 신규 채팅·일기 문서/일 | 4,100개 | 41,000개 | 410,000개 |
| 같은 사용량의 30일 신규 문서 | 123,000개 | 1,230,000개 | 12,300,000개 |
| 현재 전체 채팅 진입 조회의 문서 읽기 예시/일 | 400,000회 | 4,000,000회 | 40,000,000회 |
| 두 초기 경로를 각각 30개로 제한한 경우의 같은 읽기 예시/일 | 12,000회 | 120,000회 | 1,200,000회 |
| 이들이 모두 당일 알림 대상일 때 현재 1회 실행에서 처리할 수 없는 최소 대상 | 0명* | 800명 | 9,800명 |

`*` 100명이어도 직렬 처리 시간 초과나 전송 장애 가능성은 별도다.

읽기 계산은 `사용자 수 × 하루 진입 2회 × 조회 경로 2개 × 기록 수`다. 실제 SDK 동작·재연결·인덱스에 따라 청구량은 달라지므로 정확한 비용 계산은 계측이 필요하다. 표의 개선 수치는 페이지 상한을 적용했을 때의 비교이며 아직 구현하지 않았다.

AI 기본 호출은 `사용자 수 × (채팅 20 + 일기 1)`이다. 일기 언어 재생성과 동시 중복 실행은 별도 추가 호출이다. 모델의 입력·출력 길이가 다르므로 “호출 한 번의 가격”을 고정하면 부정확하다.

월비용은 `AI 입력 토큰 비용 + 출력 토큰 비용 + DB 읽기/쓰기/삭제/저장/전송/인덱스 + 함수 실행 + 이미지 저장/전송 + 메일 + 백업/모니터링 + 선택한 OTA 비용`으로 나눠 본다. 실제 모델 가격·공급자·지역·요금제와 하루 평균·피크 사용량을 확인한 뒤 금액을 산정한다. 예산 알림만 설정하고 무제한 유료 사용을 허용하는 것은 지출 한도가 아니다. [Firestore 과금](https://firebase.google.com/docs/firestore/pricing), [Firebase 예산 안내](https://firebase.google.com/docs/projects/billing/firebase-pricing-plans)

동시 실행은 전체 가입자 수보다 **초당 새 요청 × 답장에 걸리는 시간**에 민감하다. 예를 들어 피크에 초당 5개의 AI 요청이 들어와 평균 20초 걸리면 약 100개가 동시에 처리 중일 수 있다. 이는 예시이며 실제 측정값은 아니다. 먼저 측정하고 동시 처리·대기열·공급자 제한을 맞춘다.

## 구현 순서 제안

| 묶음 | 작업 | 완료 판단 |
| --- | --- | --- |
| 1. 계정·비용·데이터 보호 | R1 ID, R2 재설정, R3 서버 사용 제한. R7 백업·오류 알림 확인 | 충돌·메일 장애·과다 요청·복원 절차가 검증됨 |
| 2. 장기 사용과 배포 안정성 | R4 채팅 페이지, R8 일기 정렬, R6 테스트 DB·배포 검사·구버전 호환 | 10,000개 합성 기록과 이전 앱 조합에서 읽기 상한·정확성 유지 |
| 3. 비동기 처리 | R5 알림, R9 생성 재시도, R10 삭제·이전 | 시간 초과·화면 종료 뒤에도 중복·누락 없이 이어서 완료 |
| 4. 저장·운영 정리 | R11 이미지, R12 보존 정책, R13 신고 처리. 어드민 세부 최적화 | 데이터 보존/삭제 책임과 운영자 처리 흐름이 명확함 |

R5 알림을 출시 핵심 기능으로 유지한다면 3번 묶음이어도 출시 전에 해야 한다. R11 이미지 보관 정책과 R13 최소 수동 신고 처리는 어드민 고도화보다 먼저 결정한다. 이 순서는 작업 의존성과 중요도에 대한 권고이며 개발 기간 견적은 아니다.

## 이번 검토에서 한 검증과 하지 않은 검증

현재 보안·성능·앱 로그인·Netlify 구조·Firebase 인증 런타임의 기존 자동 테스트 **43개가 모두 통과**했다. 실행 명령은 다음과 같다.

```sh
node --no-experimental-require-module --test scripts/test-security.cjs scripts/test-performance.cjs scripts/test-app-login.cjs scripts/test-netlify-functions.cjs scripts/test-server-runtime.cjs
```

이 테스트는 위 신규 위험을 모두 다루거나 부하·스토어 심사를 검증하지 않는다. 이번에는 앱 소스를 바꾸지 않아 앱 재빌드·기기 재설치·새 배포를 수행하지 않았다. 기존 9월 10일 운영 인증/본인 허용/타인 차단 검증과 사용자의 기기 정상 확인을 재사용했다.

출시 전에 별도로 확인할 운영 설정은 다음과 같다.

| 확인 대상 | 현재 증거 상태 | 필요한 확인 |
| --- | --- | --- |
| Firebase·Netlify·AI·메일 요금제/한도 | 이번에 콘솔 미조회 | 하루/월 비용, 제한 도달 시 동작, 경고 수신자 |
| 서버·DB 위치 | 코드에 명시적인 함수 지역 설정 없음. 실제 지역 미조회 | 주요 사용자와 DB/함수 간 지연 측정. 지역 변경 필요성 판단 |
| DB 인덱스·백업·TTL·복원 | 저장소에서 운영 설정 전체를 재현할 자료 미확인 | 콘솔 설정 대조 후 버전 관리·복원 실습 |
| API 외부 차단·앱 인증·최소 버전 | 코드의 공통 방어 범위는 위에 기록. 외부 설정 미조회 | 외부 제한 여부, 구버전 지원 기간, 긴급 기능 중지 방법 |
| 사고·신고 대응 | 일부 로그/5건 단위 신고 메일만 코드에서 확인 | 담당자, 알림 수신, 처리 상태와 복구 절차 |
| 실제 부하·장기 데이터 | 운영 부하 시험 안 함 | 격리된 합성 환경에서 10/50/100 동시 요청 단계 시험, 429·지연·단절 주입. 실제 AI 비용은 제한된 표본으로 별도 측정 |

이미 갖춘 장점도 유지해야 한다. 공통 데이터 인증, API 소유권 재확인, 일기 단일 문서와 저장 트랜잭션, 제한된 백업 이전, 화면 캐시·진행 중 읽기 공유, 관리자 count·페이지 조회는 좋은 기반이다. 유지보수 구조는 이 기반 위에 요청 검증·AI 호출·작업 처리·관측을 공통 모듈로 나누는 정도로 개선하고, 지금 마이크로서비스나 Kubernetes부터 도입할 필요는 없다.
