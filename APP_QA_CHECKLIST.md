# Dreamary 앱 핵심 기능 점검표

웹 기능을 앱에서 다시 구현하는 것이 아니라, 같은 소스가 Capacitor 정적 앱에서도 같은 화면과 데이터를 사용하는지 확인한다.

## 코드·빌드 자동 점검

- 인증/소유권 수정 및 서버 배포 전 `SECURITY_DEPLOYMENT.md`의 차단 항목을 먼저 확인한다. 현재 Firestore 전체 공개 규칙 문제는 미해결이며, 앱 빌드 성공을 배포 승인으로 해석하지 않는다.
- `npm run test:security`: 일기·비로그인 기기키·백업/이전 인증 회귀 테스트. 키 연결 기간 종료, 토큰 없는 구버전 앱 거부, iOS/Android/웹 동일 헤더를 확인한다.
- `npm run test:rules`: `firebase.test.json`으로 시작한 demo 전용 로컬 에뮬레이터에서만 규칙을 검증한다. 운영 규칙 반영과는 별개다.
- 로그인 전/후/로그아웃 시 UI 인증과 별도 DB 세션이 같은 데이터 소유자를 가리키고, 이전 계정 캐시/늦은 응답이 표시되지 않는지 확인한다. 관리자 재로그인, 선톡/채팅 저장, 캐릭터 삭제, 중단된 이전 후 회원 탈퇴도 포함한다.

- `npm run check:app`: TypeScript 검사 후 앱 정적 빌드
- `npm run test:performance`: 캐시 경합·저장 응답·사진 로딩·알림 후처리 회귀 테스트(실기기 확인은 별도)
- 개발 웹·개발 앱 테스트 전에는 `ENVIRONMENT_GUIDE.md`를 확인하고 개발 Firebase·개발 Netlify 값을 사용한다.
- 개발 빌드에서는 왼쪽 상단에 노란색 `개발` 딱지가 표시되고, 운영 빌드에서는 표시되지 않아야 한다.
- `npx cap sync`: 최신 웹 결과물을 iOS·Android 프로젝트에 복사
- 동적 상세 화면 이동은 `buildStaticEntityRoute()`를 사용하고 실제 ID query는 `useSearchParams()`로 반응형 조회한다. 렌더 시점의 `window.location.search`만 읽어 빌드용 ID `1`로 조회하면 안 된다.
- `/api` 요청은 `apiFetch()` 또는 단발성 JSON용 `apiPostJson()`을 사용한다.
- 캐릭터 쓰기 후 `invalidateCharacterStore()`를 호출한다.
- `CapacitorHttp.enabled`가 `false`인지 확인한다. Firestore 요청에 전역 네이티브 HTTP 패치를 사용하지 않는다.
- 새 Firestore/Firebase 조회를 추가했다면 예상 읽기 수·호출 빈도·반복 진입 여부를 확인한다. 캐시·count 집계·단일 문서 ID·페이지네이션으로 대체할 수 있으면 전체 목록 조회를 금지한다.

## 실제 기기 핵심 시나리오

아래 항목은 iOS와 Android에서 각각 확인한다.

1. 비로그인 첫 실행 → 일기·홈·채팅·마이페이지 탭 이동
2. 캐릭터 생성 → 홈에 새 캐릭터 노출 → 채팅 목록 → 해당 채팅 상세 진입
3. 캐릭터 생성 입력 중 키보드 표시 → CTA가 키보드 바로 위에 있고 잘리지 않음
4. 홈 설정·페어명·드림캐·유저 수정 진입 및 저장 후 변경 내용 즉시 반영
5. 일기 작성·답장·과거 일기 상세 및 이전/다음 이동
6. 채팅 전송·재진입·수정·삭제·광고 잠금해제
   - 새 채팅방 1번째·2번째 전송은 광고 없이 AI 답장이 표시되어야 함
   - 3번째 전송은 광고 웹페이지가 열린 뒤 앱 복귀 시 AI 답장이 표시되어야 함
   - 광고가 열리지 않거나 서버가 실패하면 입력값은 채팅 입력칸에만 보존되고 일기 입력란에는 자동 입력되지 않아야 함
7. 비로그인 데이터 생성 → 로그인 → 데이터 이전 → 앱 재실행 후 로그인 유지
8. 로그아웃 → 기존 로그인 데이터가 잠깐 노출되지 않고 비로그인 UUID 데이터 노출
9. 키보드가 열린 모든 화면에서 앱 하단 탭 미노출
10. 네트워크 지연·차단 시 무한 로딩 없이 캐시 또는 재시도 화면 노출
11. 캐릭터 저장 중 Logcat에 `WebChannelConnection RPC 'Write' ... transport errored`가 반복되지 않음
12. 캐릭터 생성 완료 → 홈·채팅 상세에서 30초 이내 선톡 1건 노출 → 앱 재실행 후 중복 선톡 없음
13. 선톡 API 1회 실패 후 채팅이 0건이면 홈 또는 채팅 상세 재진입 시 자동 재시도
14. iOS 채팅 상세에서 하단 탭 미노출, 키보드가 닫힌 입력창은 홈 인디케이터 바로 위에 배치
15. iOS 채팅 상세 입력 중 입력창이 키보드 위에 보이고, 1줄에서 최대 6줄까지 확장된 뒤 내부 스크롤되는지 확인
16. iOS 일기 작성 중 키보드가 열린 상태에서도 배경 일기 화면과 textarea 위 드래그가 스크롤되고, 입력란은 내용 길이에 따라 커지는지 확인
   - 500자까지 연속 입력해도 마지막 입력 줄과 커서가 키보드 위에 보여야 함
   - 긴 글의 중간 문장을 수정할 때 textarea 맨 아래로 강제 이동하면 안 됨
   - 일기 화면을 나간 뒤 채팅·프로필 입력 화면의 키보드 위치가 기존과 동일해야 함(iOS resize mode 복원 확인)
17. 광고 대상 턴에서 광고 웹페이지가 열리면 채팅/일기 답장이 제공되고, 열리지 않으면 입력값이 보존되며 `dreamary_ad_diagnostics`에 `ad_open_failed`가 남는지 확인
18. Android 체크 배경 화면에서 노치·상태바 영역이 흰색 단색으로 끊기지 않고 화면 배경과 이어지는지 확인
19. iOS와 Android에서 왼쪽 가장자리 스와이프 시 이전 화면으로 이동하는지 확인
20. Android 시스템 뒤로가기: 홈에서는 앱 종료, 일기·채팅·마이페이지 탭 최상위에서는 홈 이동, 상세·수정 화면에서는 이전 화면 이동 확인
21. 광고 노출 모달 확인 버튼을 누르면 즉시 진한 색상으로 바뀌고, 광고 로딩 중 버튼 텍스트 대신 작은 스피너가 표시되는지 확인
22. 새 캐릭터 선톡 생성 중 홈 메시지 카드에 입력중 점 3개 애니메이션이 보이고, 누르면 채팅 상세로 이동하며, 선톡 완료 후 실제 메시지로 교체되는지 확인
23. 앱 비로그인 마이페이지에서 UUID 원문 영역이 보이지 않고, 캐릭터가 있을 때만 채팅내용 백업 버튼과 로그인 안내 문구가 보이는지 확인
24. 비로그인 상태에서 채팅내용 백업 버튼 → 8자리 백업 코드 모달 → 로그인 후 백업코드 입력 → 기존 캐릭터·채팅·일기 데이터가 로그인 계정에 노출되는지 확인
25. 로그인 상태에서 마이페이지 → 유저 아이디 → 비밀번호 확인 → 개인정보 수정 → 회원 탈퇴 버튼 → 취소하기 선택 시 개인정보 수정 화면으로 돌아오는지 확인
26. 같은 화면에서 회원 탈퇴 확정 시 해당 계정의 캐릭터·대화·일기·계정 정보가 삭제되고 비로그인 홈으로 이동하는지 확인
27. 일기 답변 완료 후 `신고` 버튼 → 사유 미선택 시 신고하기 비활성 → 사유 1개 이상 선택 시 활성 → 기타는 30자 제한인지 확인
28. 채팅방에서 사용자 메시지에는 수정 버튼이 없고, AI 캐릭터 답변에만 사이렌 신고 아이콘이 보이는지 확인
29. 신고 제출 후 Firestore `reports`에 `status: new` 문서가 생성되고, 미처리 신고가 5건 단위가 될 때만 알림 발송 조건이 동작하는지 확인
30. 웹 `/account-deletion`에서 아이디·비밀번호 본인 확인 → 취소 시 계정 유지 → 재시도 후 탈퇴 확정 시 앱 탈퇴와 같은 범위의 데이터와 Firebase Auth 계정이 삭제되는지 확인
31. 앱 채팅 목록 → 실제 캐릭터 채팅 상세, 일기 모아보기 → 실제 일기 상세로 진입했을 때 빌드용 ID `1`로 잘못 조회되어 1초 이내 목록으로 복귀하지 않는지 확인
32. 일기 작성 완료 → 캐릭터 답변 완료 후 교환일기 푸시 바텀시트가 노출되는지 확인
33. 비로그인 상태에서 푸시 알림받기를 선택하면 로그인 안내가 뜨고, 로그인/회원가입 후 `/diary`로 돌아와 알림 설정을 이어가는지 확인
34. 로그인 상태에서 푸시 알림받기를 선택하면 OS 권한 요청 후 `pushDevices`, `diaryPushTargets` 문서가 생성되는지 확인
35. 푸시 권한을 거절하면 7일간 같은 유저에게 바텀시트가 반복 노출되지 않는지 확인
36. 회원 탈퇴 시 `pushDevices`, `diaryPushTargets`의 해당 uid 데이터가 같이 삭제되는지 확인
37. 마이페이지 → 앱 정보 → 알림설정 진입 시 풀페이지로 열리고 하단 탭이 보이지 않는지 확인
38. 알림설정 → 교환일기 알림 토글 ON/OFF 시 `diaryPushTargets.enabled`와 현재 기기의 `pushDevices.diaryPushEnabled`가 같이 변경되는지 확인
   - 알림설정 진입 직후 토글은 로컬 캐시값으로 즉시 표시되고, 서버 응답이 늦어도 화면 전체가 로딩 상태로 묶이지 않아야 함
39. 기기 OS에서 드림어리 알림을 OFF하거나 아직 허용 전인 상태로 알림설정 화면 진입 시, 앱 내부 `교환일기 알림` 토글이 OFF여도 `설정 > 알림 > 드림어리 > 알림을 켜주세요` 안내와 `알림설정 바로 가기` 버튼이 노출되는지 확인
40. `알림설정 바로 가기` 선택 시 Android는 앱 알림 설정 화면, iOS 16 이상은 앱 알림 설정 화면으로 이동하는지 확인
41. 웹 버전 마이페이지 앱 정보 영역과 일기 작성 완료 화면에 알림설정 버튼·알림 유도 바텀시트가 노출되지 않는지 확인
42. Android에서 OS 드림어리 알림을 OFF한 뒤 앱 알림받기를 선택했을 때, 완료 바텀시트가 뜨지 않고 설정 안내로 이동하는지 확인
43. iOS 최초 설치 후 시스템 알림 허용을 선택했을 때 APNs 토큰 등록이 성공하고, 시스템 설정에서 드림어리 알림이 켜진 상태로 보이는지 확인
44. 교환일기 푸시 대상 계정에 페어가 여러 개 있으면 `diaryPushTargets.candidates` 중 오늘 일기를 쓰지 않은 페어만 후보가 되고, 그중 `nextTopicOrder`가 가장 높은 페어의 `{n}번째 주제가 도착했어요!` / `{주제}` 문구가 한국어 푸시로 발송되는지 확인
45. 관리자 테스트 푸시 `/api/admin/test-diary-push`는 `PUSH_TEST_SECRET` 또는 `ADMIN_PASSWORD` 없이는 실패하고, 계정 아이디 최대 10개까지만 발송되는지 확인
46. 관리자 테스트 푸시에서 `title`/`body`를 보내면 커스텀 문구가 발송되고, 둘 다 생략하면 운영 교환일기 후보 기준 한국어 문구가 발송되는지 확인
47. 한국어 주제·서사 템플릿의 `{유저}`, `{캐릭터}` 뒤에 `이/가`, `을/를`, `은/는`, `으로/로`, `과/와`, `아/야`가 붙어도 실제 이름 받침에 맞는 조사로 표시·전송되는지 확인
48. 같은 유저·같은 캐릭터·같은 날짜에 일기 작성 요청이 반복되어도 Firestore `diaries` 문서가 1개만 생성되고, 두 번째 요청은 기존 일기를 반환하는지 확인
49. 푸시 알림 수신 후 앱을 열면 앱 아이콘 뱃지와 알림센터에 남은 드림어리 알림이 정리되는지 확인
50. 캐릭터·유저 설정의 긴 입력칸에서 키보드를 열어도 하단 입력 영역까지 스크롤해 확인할 수 있는지 확인
51. 홈 설정 배경 사진 업로드 중 버튼 스피너가 회전하고, 업로드 중 뒤로가기·앱 비활성화 후 이전 업로드 완료 모달이 뒤늦게 뜨지 않는지 확인
52. 배경화면 변경 완료 후 기본 alert가 아니라 `홈으로`/`편집 계속` 2버튼 앱 모달이 표시되는지 확인
49. Clarity에서 Dreamary 사용자 ID는 기본 `Clarity ID`가 아니라 `Custom user ID` 또는 `dreamary_user_id` custom tag 필터로 조회되는지 확인
50. 웹 개발 서버에서 광고 대상 일기 작성 시 광고는 새창에서만 열리고, 기존 드림어리 탭이 광고 URL로 이동하지 않으며, 광고 실패 시 일기 입력값이 보존되는지 확인
51. 한국어 일기 답변에 영어 알파벳·일본어·중국어 한자·기타 외국어 문자가 섞인 경우 답변이 저장되지 않고 1회 재생성되는지 확인
52. 앱/웹 하단탭 배경은 `rgba(255, 255, 255, 0.7)`처럼 비치되, 아이콘·텍스트 가독성이 흐려지지 않는지 확인
53. 어드민 대시보드는 `characters`, `diaries`, `chatMessages` 전체 문서를 직접 읽지 않고 count 집계로 개수만 표시하는지 확인
54. 어드민 사용자 목록은 20명 단위 `더보기`로 조회되고, 사용자 상세·페어 상세 본문 로그도 20개씩 추가 조회되는지 확인
55. 사용자 일기 모아보기는 페어별 최신 10개만 먼저 노출하고, `10개 더보기` 선택 시 다음 10개가 추가되는지 확인
56. 작성 완료한 일기의 유저 포스트잇 하단 우측 수정 아이콘 → textarea 수정 → 저장/취소가 동작하고, 키보드가 열린 상태에서도 전체 화면이 스크롤되는지 확인
57. 작성 완료한 일기의 AI 답변 하단 좌측 신고/수정/삭제 아이콘 → 신고 모달, 수정 저장, 삭제 확인 모달이 각각 동작하는지 확인
58. 일기 수정·삭제는 현재 일기 1건만 API로 변경하며, 목록 전체 재조회나 전체 컬렉션 fallback이 추가되지 않았는지 확인
59. 홈에서 페어를 빠르게 바꿔도 배경이 흰 화면으로 깜빡이지 않고, 새 배경이 준비되기 전에는 이전 배경이 유지되는지 확인
60. 홈 배경 사진 업로드 후 생성된 이미지 URL이 과도하게 큰 원본이 아니라 앱에서 압축된 이미지로 저장되는지 확인
61. 홈 배경 속도 개선을 위해 페어 전환 시 추가 Firestore 재조회가 발생하지 않는지 확인
62. 정상 프로필·홈 배경을 한 번 표시한 뒤 ImgBB를 차단해도 IndexedDB 캐시로 복구되고, 캐시가 없는 최종 실패에서는 깨진 이미지 대신 기본 프로필·기본 배경이 표시되는지 확인
63. 이미지 최종 실패 시 Clarity `image_load_failed`가 동일 URL·이미지 종류별 세션 1회만 기록되며 URL·UID·이름이 전송되지 않고 Firestore 추가 조회도 발생하지 않는지 확인
64. 일기 작성과 채팅 전송 버튼을 빠르게 두 번 누르거나 Enter 입력과 버튼 선택을 연속 수행해도 요청·사용자 데이터가 1건만 생성되고, 응답 완료 전까지 버튼 스피너와 비활성 상태가 유지되는지 확인
65. iOS·Android에서 일기 작성 완료 → 광고 종료 후 앱 복귀 → 교환일기 알림 바텀시트 노출 시 헤더·캐릭터 선택 영역·본문·하단 탭 전체가 같은 dim 아래에 있고, 바텀시트만 최상단에 있으며 dim 뒤 하단 탭을 누를 수 없는지 확인
66. iOS·Android에서 비로그인 첫 실행 → 기존 페어가 있는 계정으로 로그인 → 다른 탭을 거치지 않고 일기 탭 첫 진입 시 비로그인 dummy 안내가 노출되지 않고 로그인 계정의 페어·일기가 즉시 표시되는지 확인
   - 로그인 직후 알림 설정 재개로 `/diary`에 바로 이동하는 경로도 동일하게 확인
   - 로그아웃 후에는 직전 로그인 계정의 페어가 남지 않고 비로그인 UUID 데이터만 표시되는지 확인

## 판정 기준

### 성능 수정 회귀 확인 (2026-09-09 추가)

- 같은 페어 프로필을 여러 화면이 동시에 요구해도 읽기를 공유하며, 프로필 수정/로그아웃 후 이전 캐시가 다시 살아나지 않는지 확인한다.
- 일기 목록 진입 시 페어 캐시를 재사용하고, 더보기에서 유효한 프로필 캐시를 다시 읽지 않는지 확인한다.
- 서버 저장 응답 직후 답변이 표시되고, Firestore 구독 연결이 늦거나 실패해도 저장된 답변이 사라지지 않는지 확인한다. 서버 배포 전에는 구버전 구독 방식이 유지될 수 있다.
- 다른 페어/계정으로 전환한 뒤 이전 작성 응답이 새 화면을 덮지 않는지 확인한다.
- 사진 캐시 없음/캐시 있음/CORS 차단/404/빠른 페어 전환을 구분하여, 정상 사진·기본 이미지와 복구 동작을 확인한다. 새 프로필 512px 압축 시도와 기존 GIF 보존도 확인한다.
- 알림 OFF 일기 완료 시 대상 문서 외의 페어·주제·일기·프로필 조회가 없는지, ON이면 작성한 페어만 갱신하고 다른 후보는 보존하는지 확인한다.
- 알림 후보 계산 실패/계산 중 OFF 전환에서 개발·운영 모두 기존 후보를 보존하고 알림이 다시 켜지지 않는지 확인한다.
- 같은 기기·계정·네트워크에서 첫 진입/재진입을 나누어 기록한다. `dreamary_performance`와 서버 `[dreamary_performance]`로 API 왕복·AI 생성·DB 저장 시간을 구분하고 본문·UID·토큰이 추가 기록되지 않는지 확인한다.

### 공통 판정

- 웹과 앱이 같은 Firestore 문서·API 결과를 사용해야 한다.
- 앱만 다른 화면으로 이동하거나 홈으로 되돌아가면 정적 경로 변환을 먼저 확인한다.
- 로그인 전후 목록이 비거나 이전 목록이 남으면 데이터 소유자 ID와 공용 캐시 무효화를 먼저 확인한다.
- 기기에서만 확인 가능한 키보드·안전영역·외부 브라우저·세션 복원은 네이티브 빌드 후 실제 기기에서 판정한다.
- Firestore 조회는 필요한 범위만 읽어야 한다. 전체 컬렉션 조회, 실패 시 전체 목록 fallback, 앱 시작·탭 이동마다 반복되는 원격 상태 조회는 출시 전 차단한다.
- 푸시 발송 스케줄러는 전체 유저·전체 일기 컬렉션을 조회하지 않고 `diaryPushTargets`와 해당 uid의 `pushDevices`만 조회해야 한다.
- 푸시 문구 개인화에 필요한 페어별 후보는 알림 등록·일기 작성 완료 시 갱신하고, 스케줄러 실행 시점에 전체 `characters`·`diaries`·`topics`를 재조회하지 않는다.
