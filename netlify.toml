[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_PATHS = ".netlify,.next/static,**/*.pack,android/**,ios/**"

[functions]
  external_node_modules = ["firebase-admin"]
