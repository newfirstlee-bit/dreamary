# Dreamary 모바일 디버깅 기록

같은 장애가 반복되지 않도록 실제 기기에서 확인된 원인과 금지 설정을 기록한다.

## 2026-09-10 테스트 앱과 운영 앱 혼동 방지용 마이페이지 버전 표기

### 확인 내용

- 마이페이지 버전은 이전에 개발 실행에서 `dev`, 앱 정적 빌드에서 `app-dev` 또는 `app-prod`로 표시되어 테스트 브랜치 앱인지 운영 앱인지 한눈에 구분하기 어려웠다.
- 실제 코드·서버 동작을 나누는 변경이 아니라 설치된 앱 빌드 출처를 표시하는 UI 문제다. 표시값 때문에 API 주소, 인증, DB, 캐시 동작이 바뀌면 안 된다.

### 해결 방법 및 재발 방지

- 앱 빌드의 기본 라벨을 `branch`로 통일해 마이페이지에 `v버전 · branch`가 표시되도록 했다. 웹 개발은 `dev`, 배포 웹은 `web` 표시를 유지한다.
- 정식 앱 배포 파이프라인은 `NEXT_PUBLIC_APP_CHANNEL=release`처럼 명시된 승인 라벨을 주입해 `branch`와 구분할 수 있다. 코드에 운영 여부를 추측하는 별도 분기를 추가하지 않는다.
- 브랜치 테스트 앱은 기존 앱을 삭제한 뒤 같은 브랜치에서 다시 빌드·설치한다. 마이페이지 표시만 바뀌며 로그인/비로그인 데이터나 Firestore 읽기·쓰기는 초기화하지 않는다.

### 회귀 확인

1. `NEXT_PUBLIC_BUILD_TARGET=app` 빌드의 마이페이지가 `v1.0.0 · branch` 형식인지 확인한다.
2. 웹 개발 실행은 `dev`, 웹 배포는 `web`로 표시되는지 확인한다.
3. 정식 앱 배포 시 승인된 `NEXT_PUBLIC_APP_CHANNEL=release`가 `branch`를 덮어쓰는지 확인한다.
4. 라벨 변경 후에도 API 주소·인증 상태·기존 캐릭터/일기/채팅 데이터가 변하지 않는지 확인한다.

## 2026-09-10 채팅 목록에는 선톡이 보이나 상세 진입 시 빈 화면

### 증상·근본 원인

- 테스트 웹에서 채팅 목록 미리보기에는 캐릭터의 `오렌지, 지금 바빠?` 메시지가 보였지만, 같은 캐릭터의 채팅 상세에는 안내문만 있고 메시지가 보이지 않았다.
- 목록은 `getLatestChatMessage()`의 단건 조회 결과를 `lastMessages`에 즉시 표시한다. 반면 상세 초기화는 `getChatMessages()`로 기록을 읽은 뒤에도 `setMessages(history)`를 호출하지 않고 `subscribeChatMessages()`의 첫 실시간 스냅샷만 기다렸다.
- 첫 WebView 진입·인증 전환·Firestore WebChannel 지연 또는 구독 오류로 첫 스냅샷이 늦거나 전달되지 않으면, 이미 읽기에 성공한 기록을 화면에 사용하지 못해 빈 화면이 되었다. 목록과 상세가 서로 다른 데이터 소유자를 조회한 문제나 메시지 저장 실패로 단정할 증거는 없었다.

### 해결 방법 및 재발 방지

- 상세 화면은 `getChatMessages()`의 소유자·캐릭터 필터가 적용된 결과를 읽은 즉시 `setMessages(history)`로 표시한다. 이후 실시간 구독은 새 메시지 동기화 역할만 맡는다.
- 초기 일회성 읽기와 실시간 구독을 둘 다 유지하되, 구독을 기다리기 위해 이미 읽은 화면 표시를 지연하지 않는다. 전체 컬렉션 fallback이나 추가 조회는 만들지 않는다.
- 채팅 목록과 상세는 같은 `userId + characterId` 조건을 사용해야 하며, 다른 계정 캐시·dummy 메시지를 실제 상세 데이터로 사용하지 않는다. 인증 상태 전환 시 이전 구독의 늦은 응답이 새 캐릭터 화면을 덮지 않는지도 함께 확인한다.

### 회귀 확인

1. 캐릭터의 `chatMessages`에 선톡 1건이 있는 계정에서 채팅 목록의 미리보기와 상세 화면을 연속으로 확인한다.
2. 상세 진입 시 실시간 구독을 지연시켜도 일회성 조회 결과가 먼저 표시되는지 확인한다.
3. 새 사용자 메시지·AI 답장이 구독으로 기존 기록 뒤에 추가되고, 삭제 후 실제 빈 대화방도 빈 상태로 표시되는지 확인한다.
4. iOS·Android·웹에서 같은 `userId + characterId`로 조회하고, 개발/운영이 동일 공통 코드 경로를 사용하는지 확인한다.

## 2026-09-10 guest/session 500 원인 확정 — 서버의 require(ESM) 제한과 인증 하위 의존성 충돌

### 증상·근본 원인과 증거

- 사용자 재현 뒤 진단 배포 `6aa226950210950009dd697b`의 03:41~03:43 UTC 로그에서 `stage: account-check`, `code: ERR_REQUIRE_ESM`, HTTP 500을 확인했다. 브라우저 입력/DB quota 오류가 아니라 Firebase Auth 로딩 단계의 실패다.
- 실제 의존성은 `firebase-admin@14.1.0 → jwks-rsa@4.1.0 → jose@6.2.8`. `jwks-rsa/src/utils.js`는 ESM 전용 jose 6을 CommonJS `require()`로 불러온다. Node 22.23.2에 `--no-experimental-require-module`을 주고 `import('firebase-admin/auth')`를 실행하면 같은 `ERR_REQUIRE_ESM`이 재현된다. 바깥 import만 동적으로 바꿔도 내부 require는 그대로다.
- AWS Lambda는 해당 실험적 기능을 기본 차단한다. 기존 로컬 검사는 기본 허용된 Node로 실행되어 통과했고, `[build.environment] NODE_OPTIONS`만으로 실제 함수 런타임에서도 활성화되었다고 가정한 것이 검증의 빈틈이었다. 실제 실패 런타임 전체 환경값을 출력/수정하지 않았다.
- 최초 기기 연결뿐 아니라 `data-session` 토큰 발급, Auth SDK를 쓰는 탈퇴/비밀번호 관련 경로도 같은 의존성에 영향을 받는다. 인증 검증 자체를 삭제하는 것은 해결책이 아니다.
- 근거: [AWS Node.js 런타임 문서](https://docs.aws.amazon.com/lambda/latest/dg/lambda-nodejs.html), [Firebase 저장소 동일 의존성 문제 보고](https://github.com/firebase/firebase-admin-node/issues/3181). 근본 원인 판정은 원격 로그와 로컬 제한 조건 재현을 함께 사용했다.

### 해결 방법·재발 방지

- `package.json`의 `overrides.jwks-rsa.jose = "$jose"`로 해당 하위 모듈만 프로젝트에서 이미 사용하는 CJS/ESM 겸용 `jose@4.15.9`로 통일했다. 잠금 파일에서 중첩 jose 6만 제거했다. Firebase Admin 자체/인증 로직/DB 권한/서명키/조회 횟수는 바꾸지 않는다. 개발·테스트·운영이 동일 잠금 파일과 공통 코드를 사용한다.
- 초기 `npm install/update`는 기존 중첩 잠금을 유지했다. 따라서 `npm ls`만이 아니라 깨끗한 임시 폴더의 `npm ci --ignore-scripts`와 실제 제한 조건 테스트로 잠금 파일 재현성을 확인한다. 불필요한 전체 의존성 업데이트는 하지 않는다.
- `test:server-runtime`은 실험적 require(ESM)을 명시적으로 끄고 실제 Firebase Auth의 require/import, 합성 서비스 계정의 custom-token 서명/클레임, 실제 JWKS 공개키 변환을 확인한다. 네트워크/실제 인증키/DB 읽기·쓰기 모두 0건이다.
- `prebuild`에서 해당 검사를 필수 실행해 개발 PC에서만 우연히 통과하는 배포를 차단한다. 모의 SDK 보안 테스트나 Next 컴파일만으로 인증 런타임 검증을 대체하지 않는다. 진단 로그의 민감정보 금지는 유지한다.

### 회귀 확인

1. 변경 전 Node 22 제한 조건에서 SDK import 실패, 변경 후 같은 조건에서 런타임 테스트 2개 통과를 대조한다. 깨끗한 설치에서도 반복한다.
2. 보안 19개/성능 15개/Netlify 구조 3개, 타입 검사·웹 빌드 및 실제 Netlify nft 패키징을 확인한다. 생성된 패키지에서도 Auth 로딩/서명을 같은 제한 조건으로 확인한다.
3. 테스트 PR 새 배포 후 동일 브라우저/비로그인 기기키를 유지한 채 캐릭터 생성을 재시도한다. `guest/session`과 이어지는 `data/session` 성공 및 실제 저장을 확인한다. 사용자 기기키 삭제/전체 DB 초기화로 우회하지 않는다.
4. 운영 서버·DB 규칙은 별도 승인 전 변경하지 않는다. 라이브러리 호환성 해결은 `SECURITY_DEPLOYMENT.md`의 공개 규칙 차단 항목 해결을 의미하지 않는다.

- 로컬 검증 완료: 위 회귀 39개와 타입 검사/웹 빌드 통과. 깨끗한 임시 설치 및 실제 nft `guest-session.zip` 추출본에서 Node 22.23.2 제한 조건의 SDK 로딩/서명/JWKS 테스트 모두 통과했다. 함수 23개 모두 API v2 유지. 테스트 PR 반영 뒤 원격 캐릭터 생성 성공 여부는 사용자의 재시도로 최종 확인한다.

## 2026-09-10 테스트 배포 캐릭터 생성 전 guest/session HTTP 500 — 원인 조사 중

### 증상·확인된 사실

- PR #2 커밋 `5f2571d413f9d97fc3bbdda22457c6ddfa1a8d9c`, dreamary 배포 `6aa21f96b583840007b6ce49`는 ready 상태이나 사용자가 캐릭터 생성 시 `/api/guest/session` HTTP 500을 보고했다. 캐릭터 저장보다 앞선 기기 인증 단계의 실패다.
- 해당 배포 함수 로그(2026-09-10 03:12~03:16 UTC)에는 실행 시간만 있다. 핸들러가 예외를 `securityErrorResponse()`에 전달하면서 예상하지 못한 예외를 기록하지 않아 실제 실패 단계/코드가 유실된다. **500의 근본 원인은 아직 미확정**이며 로그 누락은 확정된 진단상의 결함이다.
- 현재 deploy-preview/Functions 범위의 Firebase 서비스 계정 값 존재와 게스트 서명키 형식은 정상이다. 비밀값은 출력·저장하지 않았다. 해당 설정으로 로컬에서 가상 ID의 단일 자격 문서 조회, Auth 계정 존재 확인(`auth/user-not-found`는 정상), 기존 데이터 3종 `limit(1)` 포함 읽기 전용 트랜잭션이 통과했다. 총 Firestore 읽기는 최대 5건, DB 쓰기/권한/환경 변경은 0건이다. 이 결과로 과거 실패 시점의 원격 환경까지 정상이라고 단정하지 않는다.
- 실제 Netlify 로컬 패키지의 Firebase Auth import 및 Node 22.23.2에서 실제 패키지 핸들러 호출(합성 키, DB/Auth 작업 대역)은 통과했다. 원격 데이터 저장/실제 캐릭터 생성의 성공을 검증한 것은 아니다. 과거 4KB 업로드 오류와 이번 런타임 500을 구분한다.

### 이번 변경·재발 방지

- `guestSessionDiagnostics.ts`에서 설정/요청검증/토큰서명/자격조회/계정확인/자격트랜잭션 단계를 구분한다. 예상하지 못한 실패는 서버 로그에 고정 단계, 허용 목록의 오류 코드/종류, HTTP 상태만 남긴다. 오류 원문·stack·UUID·비밀키·해시·토큰·요청 본문은 남기지 않는다. 정상 처리와 일반적인 4xx 인증 거부는 오류 로그를 추가하지 않는다.
- 기존 인증 판정/응답/DB 조회·쓰기 경로는 유지한다. 개발/Next·Netlify가 동일 핸들러를 사용하며 환경별 우회는 추가하지 않는다. 추가 Firestore 읽기/쓰기 0건. 진단 추가를 버그 해결 완료로 표현하지 않는다.
- 기능 테스트만으로 런타임 장애 원인을 추측하지 않는다. 같은 배포/시각의 서버 실패 단계와 오류 코드를 확보한 뒤 수정한다. 임의로 Firestore 규칙을 풀거나 인증을 제거하지 않는다.

### 회귀 확인·다음 단계

1. 보안 회귀에서 각 실패 단계의 500/무저장/민감정보 미노출, 정상 발급, 다른 키 403 유지 및 임의 오류 코드 차단을 확인한다.
2. 사용자가 승인한 테스트 PR #2에 진단 변경만 반영한다. 운영 배포·DB 규칙·서명키 설정은 변경하지 않는다.
3. 새 테스트 배포에서 사용자가 같은 캐릭터 생성을 한 번 재시도한 뒤 해당 배포의 `guest-session` 로그를 확인한다. 오류 코드로 근본 원인을 확정하고 이 기록을 갱신한다. 이 단계가 끝나기 전 장애 해결 완료로 안내하지 않는다.

- 로컬 확인: 보안 19개/성능 15개/Netlify 구조 3개 회귀 통과. Next 빌드와 별도 타입 검사 통과. 서버 진단 전용 변경으로 네이티브 앱 코드는 변경하지 않았다.

## 2026-09-10 Netlify 함수 업로드 HTTP 400 — 환경변수 4KB 제한 (로컬 수정·패키징 확인 완료)

### 증상·원인과 증거

- 사용자가 제공한 11:59:39 배포 로그에서 함수 생성 단계가 HTTP 400 및 AWS Lambda 환경변수 총용량 4KB 초과로 거부됐다. 이 로그의 직접 원인은 컴파일 오류나 Firestore 읽기 한도/권한 오류가 아니다.
- 현재 소스를 Netlify `@netlify/zip-it-and-ship-it`의 `listFunctions(..., {parseISC: true})`로 읽기 전용 검사했다. 총 25개 중 실제 함수 23개는 `runtimeAPIVersion: 2`, 보조파일 `netlify/functions/cors.ts`와 `netlify/functions/push-shared.mts`만 `runtimeAPIVersion: 1`로 발견된다. 보조파일이 함수 디렉터리 직속에 있어 불필요한 Lambda 호환 함수로 배포되는 구조다.
- 기존 8월 로컬 배포 manifest에도 두 보조파일이 별도 함수로 포함되어 있다. 다만 이번 원격 실패 로그에는 실제 함수명이 없으므로 두 파일 중 어느 업로드가 먼저 실패했는지는 미확정이다. 원격 환경변수 원문/총 바이트 수는 조회하지 않았다.
- 공식 안내상 최신 Netlify Functions 런타임에는 총용량 4KB 제한이 없고 Lambda 호환 모드에는 남아 있다. 참고: https://docs.netlify.com/build/functions/environment-variables/ 및 https://developers.netlify.com/guides/migrating-to-the-modern-netlify-functions/

### 해결 방향·재발 방지

- 두 보조파일을 `netlify/shared/cors.ts`, `netlify/shared/push-shared.mts`로 이동하고 모든 함수 import 및 테스트 대역 경로를 갱신했다. 원본과 비교해 안내 주석 외 공통 로직이 동일함을 확인했다. 실제 API 경로/인증키/DB 규칙/조회 수는 바꾸지 않았다. Netlify 환경 변경·재배포는 하지 않았다.
- `scripts/check-netlify-functions.cjs`를 `npm run build`의 `prebuild`에 연결했다. 함수 폴더에 default export 없는 보조파일이나 구형 named handler가 추가되면 빌드 전에 거부한다. 구조 검사는 Netlify 실제 패키징 검사를 대체하지 않는다. `npm run test:netlify`로 보조파일/구형 핸들러 거부 및 정상 함수 허용을 회귀 확인한다.
- 함수 디렉터리 직속에는 실제 진입점만 둔다. Next 빌드 성공뿐 아니라 Netlify 함수 검색/패키징 목록에서 불필요한 함수와 API v1 잔존 여부를 확인한다.
- 환경변수 삭제·인증키 길이 축소·비밀키의 NEXT_PUBLIC 접두사 변경으로 우회하지 않는다. `NEXT_PUBLIC_FIREBASE_API_KEY`는 일부 서버 인증 함수에서도 사용하므로 공개 접두사만 보고 Functions scope를 일괄 제거하지 않는다.
- 환경변수 추가 전 범위와 런타임 용량 제한을 확인한다. 실제 설정값은 로그·문서·채팅에 남기지 않는다. 로컬 앱/Next 빌드는 원격 함수 생성 제한을 검증하지 못한다.

### 수정 후 회귀 절차

1. 함수 검색 목록에서 `cors`, `push-shared`가 사라지고 실제 진입점만 API v2로 남는지 확인한다. Next 어댑터가 생성하는 서버 함수도 별도로 검사한다.
2. 타입 검사·보안/성능 테스트 및 Netlify 패키징을 확인한다. CORS preflight와 공통 푸시 코드의 import가 유지되는지 확인한다.
3. 승인된 테스트 배포에서 HTTP 400이 사라지고 인증/일기/알림 동작이 유지되는지 확인한다. 배포 성공은 별도 DB 보안 규칙 검증의 대체가 아니다.

### 로컬 확인 결과

- Netlify 실제 `listFunctions`와 `zipFunctions`(`nodeBundler: nft`, Node 22 대상 설정) 모두 함수 23개/API v2만 반환했다. `cors`, `push-shared`의 독립 함수 패키지는 생성되지 않았다. 의존 모듈로는 정상 포함된다. 임시 패키지는 `/private/tmp/dreamary-netlify-package-2a1x07`에 생성했으며 업로드하지 않았다.
- `npm run test:netlify` 3개, `npm run test:security` 18개, `npm run test:performance` 15개 통과. `npm run build` 및 빌드 후 `npx tsc --noEmit` 검증을 수행했다. `.next/types`를 사용하는 타입 검사는 Next 빌드와 동시에 실행하지 않는다(동시 실행 시 생성 파일이 교체되어 TS6053 오탐 발생).
- 이번 수정은 서버 모듈 위치/배포 검사만 변경하므로 앱 재설치가 필요하지 않다. 실제 Netlify 업로드 성공, 최신 Next 어댑터 생성 함수, 실제 환경값을 포함한 원격 동작은 다음 테스트 배포에서 확인한다. 과거 로컬 어댑터 5.15.13 산출물의 서버 함수는 최신 방식이었으나 이번 원격 산출물을 확인한 것은 아니다.
- 수정된 코드가 배포 소스에 반영되어야 한다. 예전 실패 배포의 같은 커밋을 단순 Retry하면 로컬 수정이 포함되지 않는다. 사용자 환경변수 삭제/축소나 DB 권한 초기화는 하지 않는다.

## 2026-09-10 Firestore 전체 공개 규칙·서버/클라이언트 인증 연동 보완 — 운영 미반영

### 증상·근본 원인

- 배포 전 읽기 전용 점검에서 운영 Firestore가 모든 경로에 `allow read, write: if true`를 허용함을 확인했다. API 인증을 우회한 직접 접근과 신규 비로그인 자격 문서 변조가 가능한 설정이었다. 침해 발생 여부를 확인한 것은 아니다.
- 단순히 이 규칙을 닫으면 비로그인 앱은 Firebase 인증이 없어 DB 접근이 막히고, 채팅/일부 관리자/비밀번호 초기화 서버의 Web SDK 경로도 막힌다.
- 관리자 레이아웃은 `admin_auth=true` 값만 확인하고 일부 관리자 API는 별도 세션 검증이 없었다. DB 규칙과 관리자 서버 인증을 함께 점검해야 했다.

### 해결 방법과 재발 방지

- 로컬 `firestore.rules`에 소유권 검증·소유자 변경 금지·일기 API 우회 금지·서버 전용 컬렉션 접근 금지를 작성했다. **실제 규칙 배포는 하지 않았다.**
- 기존 UI Firebase 로그인은 유지하며, 별도 `dreamary-data` Firebase 앱의 메모리 세션으로 데이터에 접근한다. `/api/data/session`은 검증된 사용자/기기 증명에서만 Firebase custom token을 발급한다. `dreamaryOwner`는 기존 UID/UUID를 유지하고 비로그인 가상 Auth ID는 `guest:{UUID}`다. UI를 임의로 로그인 상태로 만들지 않는다.
- 클라이언트 DB 원격 호출은 `dataFirestore.ts`를 사용하여 세션 준비·동시 요청 공유·소유자 변경 시 늦은 응답 폐기를 중앙화한다. 기존 소유자별 앱 캐시는 유지하며, 다른 계정의 SDK 캐시 스냅샷은 노출하지 않는다. 공개 주제 조회는 인증을 준비하지 않는다.
- 채팅 저장·중복 조회·캐릭터 삭제·신고 대상 확인·관리자 주제/페어 작업을 인증된 Admin SDK 경로로 옮겼다. 선톡 네이티브 POST에도 동일 인증 헤더를 보낸다. 서버 함수에서 클라이언트 `db.ts`의 읽기/쓰기 함수를 재사용하지 않는다.
- 관리자 쿠키는 `ADMIN_SESSION_SECRET`으로 서명한 1시간 토큰만 허용한다. IP 제한이나 쿠키의 문자열 `true`만으로 권한을 부여하지 않는다.
- 캐릭터/회원 탈퇴는 제한된 20개 페이지로 자식 데이터를 정리한다. 탈퇴는 중단된 게스트 이전 원본·새 백업/시도 기록·가상 Auth 사용자까지 처리하며 기기 자격 문서는 재연결 방지용 retired 표시만 남긴다.
- 환경값·동시 전환 순서·추가 읽기 예산·미완료 검증은 `SECURITY_DEPLOYMENT.md`를 따른다. 기본 ID 토큰 검증의 즉시 취소 미확인, 고비용 API 전역 요청 제한, 실제 Netlify/기기 검증은 별도 남은 항목이다.

### 검증 및 남은 작업

- 실제 JWT 서명과 메모리 DB로 보안 회귀 18개 통과, 기존 성능 회귀 15개 통과.
- 로컬 Firestore 에뮬레이터에서 실제 보안 규칙 테스트 6개 통과: 무인증/타인 거부, 소유자 쿼리만 허용, 인증 문서 변조 금지, 비로그인 정상 작성/이전 후 쓰기 금지, 프로필 부모 소유권, 사용자 AI 답변 위조 금지, 계정/주제 접근 제한.
- 에뮬레이터는 가짜 demo 프로젝트/합성 데이터만 사용했다. 운영 사용자/본문/권한은 변경하지 않았다.
- 최신 코드의 타입 검사·앱/웹 빌드 통과, iOS/Android 동기화 완료, iOS Simulator Debug 빌드 통과. Android는 최초 도구 승인 처리 한도로 실행이 막혔으나 사용자 재시도 승인 후 2026-09-10 11:42 KST 앱 검사·Android 동기화·`assembleDebug`까지 모두 통과했다. 실기기 테스트와 Netlify 실제 런타임 검증은 남아 있다. 상세 상태와 APK 체크섬은 `SECURITY_DEPLOYMENT.md`를 따른다.
- 운영은 여전히 기존 규칙/서버다. 새 서버 API·서명키·규칙이 반영되기 전 새 앱을 정상 테스트 가능 상태로 안내하면 안 된다.

## 2026-09-09 일기 API 본인 확인·비로그인 키 연결·데이터 이전 보완 — 배포 보류

### 증상·근본 원인과 증거

- 일기 API가 요청 본문의 `userId`를 본인 증명으로 취급했다. 일기 ID/UID를 아는 호출자가 인증 없이 수정·삭제하거나 중복 작성 응답으로 기존 내용을 받을 수 있는 코드 경로였다. 로컬 테스트 대역으로 확인했으며 실제 피해 발생을 단정하지 않는다.
- 비로그인은 로컬 UUID만 있고 별도 소유 증명이 없었다. 백업 코드 발급도 원본 UUID만 받았고, 이전은 입력 UID를 신뢰하거나 앱에서 Firestore `userId`를 직접 변경했다.
- **추가 확정:** Firebase Rules REST API에서 현재 Firestore 규칙이 `match /{document=**} { allow read, write: if true; }`임을 읽기 전용으로 확인했다. 일기 API 인증만으로 앱 전체 데이터를 보호할 수 없고 신규 인증 문서도 직접 변조 가능하다. 규칙/운영 데이터/배포는 변경하지 않았다.

### 로컬 코드 해결 범위

- Firebase 로그인 토큰과 본문 UID, 실제 일기·캐릭터 소유자를 검증한다. 무인증/다른 UID 요청은 DB/AI 처리 전에 거부한다. 수정/삭제 및 최종 저장 트랜잭션에서도 소유자·이전 상태를 재확인한다.
- 비로그인은 256-bit 기기키의 서버 해시 최초 연결과 15분 서명 인증증을 사용한다. 다른 키로 기존 연결을 덮어쓰지 않는다. 사용자가 승인한 출시 전 전제 아래 `GUEST_LEGACY_CLAIM_UNTIL` 종료 시각까지만 기존 데이터의 최초 연결을 허용한다. 최초 연결은 과거 소유권의 증명이 아니라 한시적 신뢰 전환이다.
- 백업 발급/이전도 같은 인증을 사용하며, 이전 대상 계정을 고정하고 일기→채팅→캐릭터 순서로 20개씩 옮겨 중단 후 재개한다. 캐릭터를 마지막에 옮겨 이전 도중 동일 날짜 일기의 중복 생성을 방지한다. 앱의 직접 소유자 ID 변경 경로는 제거했다.
- 삭제 시 클라이언트가 보내는 오늘 문자열 대신 서버 시각과 범위를 제한한 기기 시간대를 사용한다. 개인 일기 응답은 `no-store` 처리한다.
- Next 개발/웹 서버와 Netlify 함수가 같은 핸들러를 사용하고, iOS/Android/web 요청은 같은 인증 헤더 구성 함수를 사용한다. `CapacitorHttp.enabled=false`와 Firestore long-polling 설정은 유지했다.

### 재발 방지 / 출시 차단

- 상세 설정·조회 예산·남은 작업은 `SECURITY_DEPLOYMENT.md`를 먼저 대조한다. **DB 직접 접근 규칙, 비로그인의 Firestore 직접 접근 인증, 새 인증 메타데이터 탈퇴 정리가 끝나기 전에는 보안 해결/배포 가능으로 판정하지 않는다.**
- API 인증이 있어도 Admin SDK 밖의 클라이언트 DB 직접 호출과 백업/이전 우회 경로를 반드시 확인한다. UUID, CORS, 앱 UI 미노출은 소유권 증명이 아니다.
- 토큰이 없거나 검증 실패하면 본문 UID를 믿는 호환 fallback을 만들지 않는다. 기존 데이터 키 연결은 상용 배포 전에 닫는다.
- 로그인 토큰 검증 자체는 Firestore 0회지만, 비로그인 수정/삭제는 자격 상태 1회 추가(합계 2), 신규 작성은 캐릭터 2회 및 비로그인 자격 상태 2회가 추가된다. 기기 인증증 갱신은 실제 동작 시 캐시 만료 뒤 1문서만 읽는다. 자세한 계산은 배포 문서 표를 따른다.
- 새 앱과 서버는 함께 전환해야 한다. 인증 헤더 없는 구버전 앱과 `/api/guest/session` 없는 구버전 서버를 혼합해 기능 완료로 판정하지 않는다.

### 회귀 확인

- `npm run test:security`: 무인증·서명 위조·다른 프로젝트·다른 UID·만료·다른 기기키·키 최초 연결 경합·과거 날짜 위조·코드 제한·이전 중단/재개·계정 바꿔 이전 시도·기기별 헤더·로그아웃 경합을 확인한다.
- `npm run test:performance`, 타입 검사, 앱/서버 빌드와 iOS/Android 네이티브 빌드를 수행한다. 최종 실행 결과는 작업 보고와 배포 문서의 검증 상태를 확인한다.
- 실제 기기·DB Rules Emulator·Netlify 런타임 검증은 아직 별도로 남아 있다. 운영 DB 쓰기/서버 배포는 하지 않았다.

## 2026-09-09 일기·프로필 지연 1차 개선 — 코드 원인 확인, 실기기 속도 측정 대기

### 증상과 증거 수준

- 일기 불러오기, 프로필 사진, 일기 수정·작성·캐릭터 답변 표시가 간헐적으로 느리다는 사용자 제보.
- 아래 중복 호출·응답 미사용·환경별 처리 차이는 소스와 로컬 자동 테스트로 확인했다. 운영 DB 자체가 느리다거나 iOS에서 몇 초가 걸린다는 실측 결과는 아직 없다.
- 이번 변경은 로컬 코드와 네이티브 빌드까지 완료했다. 서버 배포·스토어 배포·운영 데이터 수정은 하지 않았다.

### 확인된 원인과 해결 방법

1. **프로필·주제·페어 중복 조회:** 화면별 프로필 effect와 초기화가 같은 문서를 재조회했고, 공용 스토어는 완료된 값만 공유했다. 진행 중 요청도 공유하도록 변경했다. 프로필은 사용자/페어별 60초·최대 100개, 주제는 5분·1묶음 메모리 캐시를 사용한다. 프로필 저장·로그아웃·화면 캐시 무효화 시 읽기 캐시도 지우며, 무효화 이전 요청은 새 캐시를 덮어쓰지 못한다. 일기 목록도 공용 페어를 사용하고 페어·주제를 동시에 준비한다. 캐릭터/주제 조회 실패를 정상 빈 목록으로 반환하지 않는다.
2. **저장 성공 뒤 실시간 통신을 다시 기다림:** 서버는 저장 후 답변을 반환했지만 작성 화면이 그 답변을 사용하지 않고 Firestore 구독을 기다렸다. 신규/중복 요청 모두 실제 저장된 `diary`를 응답하고, 화면은 소유자·페어·날짜·ID가 일치하면 바로 표시한다. 구독은 계속 유지하되 캐시의 빈 결과나 구독 오류로 저장된 화면을 지우지 않는다. 구버전 서버가 `diary`를 보내지 않으면 기존 구독 방식으로 동작하며 저장 내용을 임의로 만들어내지 않는다.
3. **사진 표시·캐시용 다운로드가 동시에 실행:** `ResilientImage`를 캐시 확인 → 단일 다운로드 → Blob 표시 순서로 변경했다. IndexedDB 저장 완료를 화면 표시 조건으로 두지 않는다. 캐시 열기/읽기는 각각 1초 제한, 네트워크 다운로드는 최대 2회·각 8초(재시도 간격 0.5초), 404 등 영구 오류는 재시도하지 않는다. CORS 차단·Blob 디코딩 실패에는 원본 img 경로를 한 번 시도한다. 이 원본 fallback 시간은 위 다운로드 제한과 별개다. 화면 이탈 후 응답은 무시하고 컴포넌트가 만든 Blob URL만 해제한다. 새 프로필 업로드는 최대 변 512px 압축을 시도한다(배경 1920px 유지, GIF/압축 실패/더 커지는 결과는 원본 유지). 기존 업로드 파일은 변경하지 않았다.
4. **일기 완료 때 모든 페어의 알림 후보 재계산:** 알림 OFF 확인보다 후보 조회가 먼저였다. 이제 OFF/미등록이면 대상 문서 1회 확인 후 종료한다. ON이면 작성한 페어 1개만 소유권을 확인하여 계산하고 나머지 후보는 보존한다. 계산 도중 OFF가 되어도 다시 켜지지 않도록 트랜잭션 안에서 재확인한다.
5. **개발/운영 후처리 불일치:** 개발 Next 경로는 후보 생성 실패를 삼키고 빈 후보로 진행했지만 Netlify 경로는 실패 응답했다. 양쪽 모두 `src/lib/server/diaryPushComplete.ts`의 동일 함수를 호출하도록 통합했다. 실패 시 기존 후보를 유지하고 실패를 반환한다. 다른 API 전체의 환경 동등성을 검증했다는 뜻은 아니다.
6. **“AI 생성”과 “DB 저장” 시간 구분 불가:** 공통 `measurePhase()`로 일기 API 왕복, 서버 조회·AI 생성·언어 재생성·DB 저장·삭제 시간을 나눠 기록한다. 클라이언트는 `dreamary_performance` localStorage 최근 100건, 서버는 `[dreamary_performance]` 로그에서 확인한다. 고정 단계명·성공 여부·시간만 기록하고 본문·UID·토큰·사진 URL은 추가하지 않는다. AI 모델/품질 검수와 저장 성공 기준은 바꾸지 않았다.

### Firestore 조회 영향·재발 방지

- 프로필: 캐시 유효 구간에 같은 사용자/페어 동시·반복 조회는 문서 읽기 최대 1회. 수정/로그아웃 후 첫 조회나 TTL 만료 후에는 다시 읽는다.
- 주제: 같은 앱 런타임에서 5분 동안 한 번만 T개 주제를 읽는다. 기존 주제 전체 조회 범위를 늘리지 않았다. 주제 작성·삭제 시 무효화한다.
- 알림 완료: OFF이면 문서 1회. ON이면 일반적으로 T개 주제 + 대상/페어/오늘 일기/프로필/트랜잭션 확인 약 5문서 + count 집계 1회다(빈 결과 과금·트랜잭션 재시도는 별도). 전체 페어 수에 비례하는 반복 조회를 제거했으며, 주제 T개 읽기는 아직 남아 있다.
- 일기 작성·수정에 추가 DB 조회를 넣지 않았다. 서버 저장 응답을 재사용하며 저장 성공 전 UI를 완료 처리하지 않는다.
- 캐시 도입 시 TTL·상한·진행 중 요청 공유·소유자 분리·쓰기 후 무효화를 함께 구현한다. 오류를 빈 정상 데이터로 바꾸거나 전체 컬렉션 fallback으로 우회하지 않는다.
- 수정 시 이 문서를 먼저 읽는 `AGENTS.md` 규칙을 유지한다. `npm run test:performance`와 `npm run check:app`을 회귀 기준으로 사용한다.
- `CapacitorHttp.enabled=false`와 Firebase long-polling 자동 감지 설정은 변경하지 않는다. 성능 개선을 위해 전역 fetch/XHR 패치를 다시 켜지 않는다.

### 검증 결과와 남은 확인

- `npm run test:performance`: 15개 통과. 실제 모듈을 사용하되 DB·네트워크·React hooks는 테스트 대역으로 검증한다. 캐시 경합/무효화/TTL, 서버 저장/중복 응답, 이미지 단일 다운로드/늦은 응답 폐기, 알림 OFF/도중 OFF/오류 보존/개발·운영 공통 연결을 포함한다. 실제 브라우저·기기 E2E 테스트를 대체하지 않는다.
- `npm run check:app`: 타입 검사 및 정적 앱 빌드 통과. `npx cap sync`: iOS·Android 완료.
- iOS Simulator Debug `xcodebuild`: BUILD SUCCEEDED. Android `assembleDebug`: BUILD SUCCESSFUL.
- 기존 Next `allowedDevOrigins`/정적 export 경고, Gradle deprecated 설정 경고는 남아 있다. 전체 `git diff --check`에는 이번에 수정하지 않은 기존 UI 줄의 공백 경고가 남아 있다.
- iOS·Android에서 같은 계정·페어 수·네트워크로 캐시 없는 첫 진입/재진입, 로그인 직후 일기, 프로필 수정, 일기 작성·수정·중복 제출, 사진 서버 차단, 알림 ON/OFF를 확인해야 한다. 실측 전에는 “몇 배 빨라짐”을 확정하지 않는다.
- 서버와 앱 양쪽에 반영해야 저장 응답 즉시 표시와 후처리 개선이 모두 적용된다. 실제 광고 복귀·OS 권한 바텀시트는 실기기 확인이 남아 있다.
- 후속: 일기 목록 최신순 정렬/인덱스·과거 날짜 필드 확인, 목록→상세 일기 캐시, 채팅 페이지네이션, 이미지 저장소/프리로드 메모리 상한, 일기 API 인증 강화, 실제 환경 설정값 대조와 속도 측정. 이번 1차 작업 완료를 앱 전체 문제 해결로 해석하지 않는다.

## 2026-09-09 iOS 로그인 직후 일기 탭에 비로그인 안내 노출

> 증거 수준 보충: 아래 상태 경합은 코드에서 확인한 위험 경로다. 실제 iOS의 콜백 지연 로그는 수집하지 않았으므로, 해당 기기에서 발생한 유일한 원인으로 단정하지 않는다. 수정 후 같은 실기기 재현 순서 확인이 필요하다.

### 증상

- iOS에서 비로그인 상태로 앱을 처음 연 뒤 로그인하고 일기 탭에 진입하면, 로그인 계정의 페어 대신 비로그인 사용자용 dummy 안내 화면이 노출됨.
- 다른 탭에 갔다가 일기 탭으로 돌아오면 별도 조작 없이 정상 페어와 일기가 표시됨.
- Android에서는 같은 순서로 정상 동작했으며, 계정 데이터 자체는 삭제되거나 이전되지 않았음.

### 근본 원인

- `signInWithEmailAndPassword()`가 성공하면 Firebase SDK의 `auth.currentUser`는 즉시 로그인 UID로 바뀌지만, React `AuthContext`의 `user/status`는 `onAuthStateChanged` 콜백과 다음 렌더를 거쳐 반영된다.
- iOS WKWebView에서는 이 반영이 탭 이동보다 늦어지는 경우가 있어, 한 렌더 동안 `useUserId()`는 로그인 UID를 반환하면서 `status`는 여전히 `guest`인 불일치 상태가 만들어졌다.
- 공용 캐릭터 스토어는 캐시 키로 UID만 사용했다. 따라서 같은 로그인 UID를 `isAuthenticated=false`로 최초 조회해 얻은 빈 결과를 “조회 완료”로 캐시하면, 뒤이어 `status=authenticated`가 되어도 같은 UID라는 이유로 빈 목록을 그대로 재사용했다.
- 일기 초기화 effect에는 이전 인증 상태에서 시작한 비동기 요청을 취소하는 guard가 없었고, 일기 캐시에도 생성 당시 인증 모드가 없었다. 늦게 도착한 이전 응답이나 guest 모드의 dummy-only 캐시가 새 인증 상태의 화면을 다시 비로그인 안내로 덮을 수 있었다.
- 다른 탭을 방문하면 인증 상태가 이미 `authenticated`로 안정된 뒤 공용 캐릭터 스토어를 다시 채우므로, 일기 탭 재진입 시 정상으로 보였다. 개발/운영 데이터나 API 차이가 아니라 iOS에서 더 잘 드러난 공통 클라이언트 상태 경합이었다.

### 해결 방법 및 재발 방지 규칙

- 로그인·회원가입 성공 직후 Firebase가 반환한 사용자를 `AuthContext`에 명시적으로 동기화한 뒤 계정 화면으로 이동한다. 화면 전환이 `onAuthStateChanged`의 React 반영 시점에 우연히 의존하지 않게 한다.
- 캐릭터 공용 캐시는 `userId`와 `authenticated/guest` 모드를 함께 키로 사용한다. 같은 UID라도 인증 모드가 달라졌으면 이전 빈 목록을 재사용하지 않는다.
- Firebase SDK의 현재 사용자 UID와 조회 UID가 같으면 React context가 한 렌더 늦더라도 로그인 소유자 조회로 처리한다.
- 캐릭터 요청마다 증가하는 sequence를 부여하고, 최신 sequence·소유자·인증 모드가 모두 일치하는 응답만 공용 캐시를 갱신한다.
- 인증 상태가 `checking`인 동안 일기 원격 초기화를 시작하지 않는다. 인증 상태가 바뀌어 effect가 다시 실행되면 이전 비동기 작업은 `cancelled` guard로 화면·로컬 캐시를 수정하지 못하게 한다.
- 일기 캐시에도 생성 당시 `authenticated/guest` 모드를 기록하고 현재 모드와 일치할 때만 사용한다. 이전 버전의 모드 없는 dummy-only 캐시는 한 번 새로 확인하되, 인증된 사용자에게 실제 페어가 없는 정상 빈 상태는 `authenticated` 캐시로 재사용한다.
- 로그인 전후 화면 이상을 고칠 때 탭 재진입이나 임의 timeout으로 상태를 새로고침하지 않는다. 데이터 소유자 ID, 인증 모드, 캐시 키, 진행 중 요청의 폐기 조건을 하나의 전환 단위로 점검한다.
- 이 로직은 개발/운영 및 iOS/Android에서 동일한 공통 코드 경로를 사용한다. 플랫폼별 지연을 가리는 별도 분기나 운영 전용 우회 코드를 만들지 않는다.

### Firestore 조회 영향

- 신규 Firestore 조회는 추가하지 않았다.
- 정상 상태에서는 기존 UID 캐시를 그대로 재사용한다. 로그인 전환 순간에 인증 모드가 달라진 잘못된 캐시만 무효화하여 페어 목록을 1회 정상 조회한다.
- 실패 시 전체 컬렉션 조회 fallback은 추가하지 않았다.

### 회귀 확인

1. iOS에서 앱 데이터가 없는 첫 실행 상태로 앱을 열고 비로그인 안내 화면을 확인한다.
2. 기존 페어가 있는 계정으로 로그인한 직후 다른 탭을 거치지 않고 일기 탭을 선택한다.
3. 첫 진입부터 로그인 계정의 페어·주제·오늘 일기가 표시되고 비로그인 dummy 안내가 한 프레임 이상 노출되지 않는지 확인한다.
4. 로그인 직후 마이페이지 대신 알림 설정 재개 흐름으로 `/diary`에 바로 이동해도 동일하게 정상인지 확인한다.
5. 로그아웃 후 일기 탭에서는 이전 로그인 계정의 페어가 남지 않고 비로그인 UUID 데이터만 표시되는지 확인한다.
6. iOS·Android의 개발 앱과 운영 앱에서 같은 순서를 반복하고, 인증 전환 시 페어 목록 조회가 무한 반복되지 않는지 확인한다.

## 2026-09-08 iOS 광고 복귀 후 알림 바텀시트 위에 헤더·하단바 노출

### 증상

- 일기 작성 완료 → 광고 노출 → 광고 종료 후 앱 복귀 → 알림 미설정 상태에서 교환일기 알림 유도 바텀시트가 표시됨.
- 바텀시트의 dim 배경이 일기 본문에만 적용되고, 일기 헤더·캐릭터 선택 영역·앱 하단 탭은 밝은 상태로 바텀시트보다 위에 보였음.

### 근본 원인

- `DiaryPushOptInSheet`가 일기 페이지의 스크롤 `<main class="diary-content">` 내부에서 렌더링되고 있었음.
- `.diary-content`는 `position: relative`, `overflow-y: scroll`, `-webkit-overflow-scrolling: touch`를 사용하는 iOS 스크롤/합성 영역이므로, 자식의 `position: fixed`와 큰 `z-index`만으로는 앱 전역 최상위 레이어가 되지 않았음.
- 헤더와 캐릭터 선택 영역은 해당 `<main>`의 바깥 형제이고, 하단 탭은 `ClientLayoutWrapper`가 페이지 바깥에 렌더링하는 형제여서 `<main>` 내부 overlay의 dim 및 stacking 범위에 포함되지 않았음.
- 개발/운영 환경 차이가 아니라 같은 DOM 구조에서 발생하는 레이어 소유권 문제였음. 앱 빌드에서만 알림 유도 UI가 노출되어 개발 웹에서는 놓치기 쉬웠음.

### 해결 방법 및 필수 규칙

- 전역 모달·바텀시트는 페이지의 스크롤 컨테이너 안에 직접 렌더링하지 않고 React portal로 `document.body` 직속에 렌더링한다.
- 공통 `.global-overlay`는 viewport 전체에 `position: fixed; inset: 0`으로 배치하고, 헤더·하단 탭·개발 표시·기존 앱 모달보다 높은 공통 z-index를 사용한다.
- dim 배경과 바텀시트는 하나의 전역 overlay 안에 둔다. 헤더나 하단 탭을 별도 예외 처리하여 밝게 남겨두지 않는다.
- 숫자가 큰 `z-index`만 추가해 해결하지 않는다. 먼저 해당 요소가 스크롤 컨테이너, transform, filter, opacity, isolation 등 별도 stacking context 안에 갇혔는지 확인한다.
- 포털 코드는 개발/운영 공통 컴포넌트 경로에 둔다. `NEXT_PUBLIC_BUILD_TARGET=app`에 따른 의도된 노출 여부 외에는 환경별 레이어 구현을 나누지 않는다.

### 회귀 확인

1. iOS와 Android 앱에서 일기 작성 완료 → 광고 확인 → 외부 광고 종료 → 앱 복귀 순서로 알림 유도 바텀시트를 연다.
2. 헤더, 캐릭터 선택 영역, 일기 본문, 하단 탭, 상태바 뒤 WebView 배경 전체가 동일한 dim 아래에 있고 바텀시트만 밝게 보이는지 확인한다.
3. 하단 탭이 시트 또는 dim 위로 올라오지 않고, dim을 통과해 탭을 누를 수 없는지 확인한다.
4. 작은 화면과 홈 인디케이터가 있는 기기에서 시트 버튼이 안전영역에 가리지 않고 내부 스크롤되는지 확인한다.
5. 개발 앱과 운영 앱 빌드에서 동일한 portal 및 `.global-overlay`가 사용되는지 확인한다.

## 2026-09-02 API 작업 버튼 중복 호출 방어 1차 적용

### 적용 범위

- 중복 저장 영향이 큰 일기 작성과 채팅 전송부터 공통 `useAsyncActionLock()`을 적용한다.
- 잠금용 `ref`를 React 상태보다 먼저 동기적으로 설정해 같은 렌더 프레임의 더블클릭·Enter 연속 입력을 차단한다.
- pending 상태는 버튼 비활성화와 로딩 스피너 표시에만 사용하고, 실제 중복 방어를 상태 업데이트 시점에 의존하지 않는다.
- 일기와 채팅의 기존 `requestId` 서버 중복 방지는 유지한다. 클라이언트 잠금만으로 네트워크 재시도·다른 기기 요청까지 막을 수 있다고 가정하지 않는다.

### 필수 규칙

- 잠금은 성공·실패 모두 `finally`에서 해제한다.
- 클라이언트 timeout만 발생시키고 실제 요청을 취소하지 않은 상태에서 잠금을 먼저 풀지 않는다. 늦게 완료되는 첫 요청과 재요청이 겹칠 수 있다.
- 광고 대상 요청은 광고 모달과 외부 브라우저 복귀 대기까지 하나의 잠금 구간으로 취급한다.
- 채팅은 첫 응답 조각이 아니라 전체 스트림이 끝날 때 잠금을 해제해 답변 순서가 섞이지 않게 한다.

### 회귀 확인

1. 일기 작성 버튼을 빠르게 두 번 눌러도 `/api/diary` 요청과 당일 일기 문서가 각각 1개인지 확인한다.
2. 채팅 전송 버튼과 키보드 Enter를 연속 입력해도 사용자 메시지와 `/api/chat` 요청이 각각 1개인지 확인한다.
3. 광고 대상 턴에서 앱을 나갔다 돌아오기 전까지 전송 버튼이 다시 활성화되지 않는지 확인한다.
4. 서버 실패 후 입력값이 복원되고 버튼 잠금과 스피너가 해제되는지 확인한다.

## 2026-09-02 iOS 긴 일기 작성 중 커서·본문 가림

### 원인

- 확정 원인은 플랫폼별 키보드 리사이즈 방식 불일치다. `capacitor.config.ts`의 `KeyboardResize.None`은 iOS에서 WebView를 줄이지 않지만 Android는 `windowSoftInputMode`에 의해 WebView가 키보드 높이만큼 축소된다.
- 따라서 Android에서는 브라우저의 기본 포커스 추적이 작동하지만, iOS에서는 키보드가 WebView 위를 덮어 브라우저가 textarea의 실제 가시 영역을 알지 못했다.
- 일기 textarea는 내용에 따라 계속 높아지고 내부 스크롤을 사용하지 않지만, 기존 포커스 시점의 `scrollIntoView()`는 최초 한 번만 실행됐다.
- 이후 입력으로 textarea가 커져도 현재 커서가 키보드 위 가시 영역에 있는지 다시 확인하지 않아 마지막 문장이 키보드 뒤로 내려갔다.
- iOS는 `KeyboardResize.None`이라 WebView 높이가 줄지 않으므로 하단 padding만 확보해도 현재 커서 위치가 자동으로 이동하지 않는다.
- iOS WKWebView에서는 flex 자식의 자연 높이가 남아 있으면 `overflow-y: scroll`이어도 해당 영역을 실제 스크롤 뷰로 만들지 않는 경우가 있었다. 이때 textarea는 늘어나지만 본문 `scrollTop`은 움직이지 않아 커서가 화면 상단에 고정된 것처럼 보였다.
- 캐릭터 선택 영역은 본문 scrollTop이 생긴 뒤 축소되어 자동 이동과 동시에 레이아웃이 한 번 더 변할 수 있었다.

### 수정 및 필수 규칙

- iOS 일기 화면에 진입한 동안에만 Capacitor Keyboard를 `KeyboardResize.Native`로 전환하여 Android와 동일하게 WebView 자체가 키보드 위 영역으로 축소되게 한다.
- 일기 화면을 벗어나면 진입 전 resize mode로 복원한다. 채팅 등 기존 `KeyboardResize.None` 전제 화면에는 영향을 주지 않는다.
- 네이티브 리사이즈 적용 중에는 iOS 키보드 높이를 CSS와 커서 계산에서 다시 더하거나 차감하지 않는다.
- `.diary-content` 하나만 일기 본문의 세로 스크롤 주체로 사용하고 `flex: 1 1 0`, `height: 0`, `min-height: 0`, `overflow-y: scroll`, `touch-action: pan-y`를 유지한다.
- textarea는 기존처럼 내용 길이에 맞춰 확장하고 내부 스크롤로 바꾸지 않는다.
- 무조건 중앙으로 옮기는 `scrollIntoView()`를 사용하지 않는다. textarea의 실제 커서 위치를 계산해 키보드에 가린 경우에만 부모 컨테이너의 `scrollTop`을 최소 거리만 직접 조정한다.
- 타이핑 중 스크롤은 iOS의 textarea 높이와 flex 레이아웃이 모두 반영되도록 두 번의 `requestAnimationFrame` 뒤 실행하고, 애니메이션 없이 이동해 입력 화면이 흔들리지 않게 한다.
- iOS는 키보드 높이를 가시 경계에서 제외하고, Android는 이미 축소된 WebView 높이를 사용해 키보드 높이를 이중 차감하지 않는다.
- 입력 포커스 시 캐릭터 선택 영역을 먼저 축소하고 키보드 애니메이션이 끝난 뒤 커서 가시성을 다시 확인한다.

### 회귀 확인

1. iPhone에서 500자까지 연속 입력할 때 마지막 입력 줄과 커서가 키보드 위에 계속 표시되는지 확인한다.
2. 입력 중 손가락으로 일기 배경과 textarea 위를 위아래로 움직여 질문·작성 내용·버튼을 확인할 수 있는지 확인한다.
3. 긴 글의 중간 문장을 선택해 수정할 때 화면이 textarea 맨 아래로 강제 이동하지 않는지 확인한다.
4. Android에서는 키보드 높이가 이중 적용되어 과도한 빈 공간이나 스크롤 점프가 생기지 않는지 확인한다.

## 2026-08-31 Android 광고 복귀 후 광고 차단 오탐·인증 복원 지연

### 증상

- Samsung 기기에서 광고 페이지가 유해 사이트로 차단된 뒤 앱을 재실행하면 앱이 느리고 비로그인처럼 보였다.
- 재로그인 시 비밀번호가 틀렸다는 안내 뒤 광고 차단 해제 모달이 앱 전체에 표시됐다.

### 실제 기기 확인 결과

- Samsung SM-A102N 로그에서 Samsung Internet의 `anti_tracking` 서비스가 활성화된 기록을 확인했다. 별도 광고 차단 앱이 없어도 Samsung Internet의 추적 방지·유해 사이트 보호가 광고 도메인을 차단할 수 있다.
- 같은 시간대에 Google Play 프로세스에서도 `ERR_NAME_NOT_RESOLVED`가 반복됐다. 드림어리 단일 도메인이나 Firebase 인증만의 장애가 아니라 기기 DNS/네트워크가 전반적으로 불안정했던 상태다.
- 기기의 Private DNS 설정값은 별도 지정 상태가 아니었다. 따라서 별도 광고 차단 앱 설치 여부만으로 원인을 단정하면 안 된다.
- 수정 APK 설치 후 앱 강제 종료·재실행 전후 마이페이지 캡처의 SHA-256이 동일했고 로그인 계정 화면이 유지됐다. 앱 재실행 자체가 Firebase 세션이나 비밀번호를 삭제하지는 않았다.

### 원인

- 전역 `AdBlockModal`이 앱 시작 때 Google 광고 스크립트에 요청하고, 연결 실패 한 번을 광고 차단으로 단정해 전체 앱을 덮었다.
- `navigator.onLine`은 DNS 해석이나 특정 호스트 접근 가능 여부를 보장하지 않으므로, 온라인 상태에서 발생한 DNS 실패가 광고 차단으로 잘못 분류됐다.
- 인증 복원 fallback이 2초로 짧아 네트워크가 느린 기기에서 로그인 세션 복원 전에 guest 화면이 먼저 보일 수 있었다.
- 로그인 화면이 Firebase 네트워크 오류, 요청 제한, 실제 자격 증명 오류를 모두 같은 로그인 실패 문구로 표시해 네트워크 장애를 비밀번호 오류처럼 보이게 했다.

### 수정 및 필수 규칙

- 네이티브 앱에서는 전역 광고 차단 probe와 전역 광고 차단 모달을 실행하지 않는다. 광고 접근 확인은 사용자가 실제 광고 턴을 시작한 순간에만 `src/lib/ads.ts`에서 수행한다.
- 웹에서도 광고 호스트 요청 실패 한 번만으로 광고 차단 모달을 표시하지 않는다. 오프라인과 광고 열기 실패를 구분한다.
- Firebase 인증 복원 제한시간은 5초로 늘리되, listener는 계속 유지해 늦게 복원된 로그인 세션이 guest 상태를 자동으로 덮어쓰게 한다.
- 로그인 오류는 `auth/network-request-failed`, `auth/timeout`, `auth/too-many-requests`를 실제 비밀번호 오류와 다른 문구로 표시한다.
- 광고 Custom Tab이 차단됐다는 사실만으로 일기 작성 완료나 로그인 세션을 취소·삭제하지 않는다.

### 회귀 확인

1. Android에서 앱 강제 종료·재실행 후 마이페이지 로그인 계정이 유지되는지 확인한다.
2. DNS가 느리거나 광고 도메인만 차단된 상태에서 앱 시작 시 전역 광고 차단 모달이 나타나지 않는지 확인한다.
3. 로그인 중 네트워크를 끊었을 때 비밀번호 오류 대신 네트워크 확인 안내가 나오는지 확인한다.
4. 광고 턴에서는 광고가 실제로 열리지 않으면 입력을 보존하고 AI 요청을 호출하지 않는 기존 규칙이 유지되는지 확인한다.

## 2026-08-31 iOS 유저 프로필 수정 스크롤 재수정

### 원인

- 이전 수정은 공통 `.fixed-cta-page`와 `.fixed-cta-content`를 붙였지만 `.fixed-cta-page`가 풀페이지의 `overflow: hidden`을 `visible`로 다시 덮었다.
- 본문의 인라인 `padding: 20px`도 공통 클래스의 하단 CTA 스크롤 여백을 덮어 WKWebView에서 body와 main 사이의 스크롤 소유권이 불안정했다.

### 수정 및 검증

- 이 화면은 `.user-profile-edit-page`와 `.user-profile-edit-scroll` 전용 구조로 교체했다.
- 풀페이지 shell은 고정하고 `main` 하나만 `flex: 1 1 0`, `min-height: 0`, `overflow-y: scroll`, `touch-action: pan-y`를 갖는다.
- iPhone 17 Pro 시뮬레이터의 실제 WKWebView에서 화면 상단과 최대 scroll 위치를 캡처했다. 최대 scroll에서 `추가 설정` textarea 전체와 고정 저장 버튼이 함께 표시되는 것을 확인했다.
- 화면 이동과 scroll 검증에 사용한 AppDelegate 테스트 훅은 검증 후 제거했다.
- 이후 유저 프로필 하단의 `캐릭터에게 느끼는 감정`, `추가 설정` textarea에 한해서, 키보드와 고정 CTA가 자리를 잡은 뒤 실제로 가려진 거리만큼 전용 스크롤 컨테이너를 이동하도록 보완했다. 무조건 중앙으로 이동하는 `scrollIntoView()`는 사용하지 않는다.

## 2026-08-26 개발 앱 일기 작성 타임아웃·당일 삭제 무반응

### 증상

- iOS와 Android 개발 앱에서 일기 전송 후 약 20초 동안 로딩하다 재시도 안내가 표시됐다.
- 일기 삭제 확인 모달에서 삭제를 눌러도 진행 상태가 보이지 않고 일기가 남아 있었다.

### 원인

- 일기 함수는 AI 답변과 한국어 검수 재생성이 모두 끝날 때까지 응답 바이트를 보내지 않았다. 네이티브 앱과 함수 프록시 사이의 유휴 응답 제한에 걸릴 수 있는 구조였고, 클라이언트가 같은 요청을 세 번 반복해 실패 시간이 더 길어졌다.
- 채팅 함수에는 이미 주기적인 응답 바이트가 있었지만 일기 함수에는 같은 연결 유지 처리가 없었다.
- 삭제는 서버 응답을 기다리는 동안 버튼 피드백과 중복 입력 차단이 없었고, 서버도 당일 일기 여부를 검증하지 않았다.
- 유저 프로필 수정 본문은 공통 고정 CTA 스크롤 컨테이너를 사용하지 않아 iOS WKWebView의 flex 영역에서 스크롤 높이가 안정적으로 계산되지 않았다.

### 수정 및 필수 규칙

- 일기 함수는 AI 생성 중 2초 간격으로 공백을 전송하는 JSON 스트림을 사용해 연결을 유지한다. 최종 JSON 앞의 공백은 표준 JSON 파서에서 허용된다.
- AI 공급자 라우팅과 서버의 한국어 재생성이 이미 있으므로 클라이언트에서 전체 일기 요청을 반복하지 않는다. `requestId` 중복 방지는 계속 유지한다.
- 삭제 버튼은 처리 중 스피너를 표시하고 중복 입력을 막는다. 삭제 API는 현재 문서 1건만 읽고 소유자와 클라이언트 현지 당일 문자열이 일치할 때만 삭제한다.
- 과거 일기 상세에는 삭제 버튼을 노출하지 않는다. 당일 삭제 성공 직후 로컬 상태를 작성 가능 상태로 바꾸고, 다음 주제용 count 조회만 1회 허용한다.
- 유저 프로필 수정은 전용 `.user-profile-edit-page`, `.user-profile-edit-scroll`, `.fixed-cta-footer` 조합을 사용한다. 하단 textarea 외 입력 포커스에는 자동 스크롤을 추가하지 않는다.

### 회귀 확인

1. iOS와 Android 개발 앱에서 한국어 검수 재생성이 발생해도 일기 요청 연결이 유지되고 한 번의 요청으로 저장되는지 확인한다.
2. 당일 일기 삭제 버튼에 즉시 스피너가 표시되고, 성공 후 같은 화면이 일기 작성 상태로 바뀌는지 확인한다.
3. 과거 일기 상세에는 삭제 버튼이 없고 서버에 과거 일기 삭제 요청을 보내도 거부되는지 확인한다.
4. iPhone 유저 프로필 수정 화면에서 키보드 유무와 관계없이 손가락으로 끝까지 스크롤되는지 확인한다. 하단 textarea가 이미 보이면 자동 이동하지 않고, 가려진 경우에만 입력란이 보이는 최소 거리만 이동해야 한다.

## 2026-08-15 ImgBB 프로필·홈 배경 이미지 깨짐

### 증상

- `dreamary` 로그인 계정에서 캐릭터·유저 프로필 사진과 홈 배경 일부가 운영 웹·앱에서 불규칙하게 깨져 보임.

### 원인

- 계정의 이미지 URL 8개 중 ImgBB 원본 3개가 반복해서 HTTP 404를 반환함.
- 나머지 정상 이미지도 최대 약 9초 지연 또는 10초 타임아웃이 관찰되어 ImgBB CDN 상태에 따라 실패 대상이 달라 보였음.
- 기존 코드는 배경 프리로드 실패 후에도 실패 URL을 적용하고 프로필 이미지에는 오류 복구가 없어 외부 호스트 장애가 그대로 노출됨.

### 수정 및 필수 규칙

- 정상 다운로드한 외부 이미지는 `src/lib/imageCache.ts`를 통해 IndexedDB에 URL 기준으로 최대 30일 캐시한다.
- 프로필 이미지는 `src/components/ResilientImage.tsx`를 사용해 원본 실패 시 로컬 캐시 → 제한된 네트워크 재시도 → 기본 프로필 순으로 복구한다.
- 홈 배경은 `src/lib/imagePreload.ts`에서 로컬 캐시 또는 정상 다운로드가 확인된 Blob URL만 화면에 적용한다.
- Netlify `/_next/image`가 정상 ImgBB 원본에도 404를 반환할 수 있으므로 `next.config.mjs`의 `images.unoptimized`는 웹·앱 모두 `true`로 유지한다.
- 이미지 캐시 때문에 Firestore를 다시 조회하지 않는다. 화면이 이미 받은 URL만 사용한다.
- 최종 실패에만 Clarity `image_load_failed` 이벤트를 기록하며 `image_failure_kind`, `image_failure_host`, `image_failure_reason` 외 URL·UID·이름 등 식별 정보는 전송하지 않는다.
- 같은 세션에서 동일 URL·이미지 종류의 최종 실패 이벤트는 한 번만 전송한다.
- ImgBB에서 이미 404인 원본은 코드로 복구할 수 없다. 기기 캐시가 없으면 기본 이미지를 표시하고 원본 확보 후 재업로드한다.

### 회귀 확인

1. 정상 이미지를 한 번 표시한 뒤 ImgBB 요청을 차단하고 같은 프로필·배경이 IndexedDB 캐시로 표시되는지 확인한다.
2. 캐시가 없는 404 URL에서는 깨진 아이콘 대신 기본 프로필 또는 기본 홈 배경이 표시되는지 확인한다.
3. 최종 실패 시 Clarity에 `image_load_failed`가 한 번 기록되고 URL·사용자 식별 정보가 포함되지 않는지 확인한다.
4. 이미지 복구 과정에서 Firestore 추가 조회가 발생하지 않는지 확인한다.

### 동일 원본 재업로드 시 기존 404 URL 재사용

- 삭제된 ImgBB URL의 원본 파일을 다시 올렸는데도 클라이언트가 기존 404 URL을 계속 요청하는 사례가 확인됐다.
- 저장 성공 이벤트 뒤에도 URL이 바뀌지 않았다면 ImgBB가 동일 이미지 업로드를 중복 처리해 삭제된 기존 URL을 다시 반환했을 가능성이 있다.
- 업로드 성공 응답만 신뢰하지 않고 반환 URL을 실제 이미지 로딩으로 검증한다.
- 검증에 실패하면 우측 하단 픽셀을 육안으로 구분되지 않게 변경한 이미지로 한 번만 재업로드하고, 두 번째 URL도 검증된 경우에만 Firestore에 저장한다.
- 이 재시도는 이미지 업로드 요청만 추가하며 Firestore 조회를 추가하지 않는다.

## 2026-08-13 iOS 풀페이지 입력 화면 키보드 스크롤 여백 부족

### 증상

- iOS 캐릭터 수정 화면에서 키보드가 열린 상태로 `추가 설정`, `드림주/나 와의 서사` 영역까지 스크롤하기 어려움.
- iOS 유저 프로필 수정 화면에서도 `캐릭터에게 느끼는 감정`, `추가 설정` 영역이 키보드와 고정 CTA에 가려짐.

### 원인

- 고정 CTA가 있는 풀페이지의 본문 스크롤 영역이 키보드 높이를 충분히 반영하지 못했다.
- `.full-page .content`의 키보드 상태 padding 규칙이 `.fixed-cta-content`의 더 큰 하단 여백 규칙을 덮어써 마지막 입력 영역까지 내려갈 공간이 부족했다.
- Android는 WebView가 키보드만큼 줄어들고, iOS는 CTA를 직접 키보드 위로 올려야 하므로 두 플랫폼을 같은 값 하나로 처리하면 한쪽에서 CTA가 과하게 이동할 수 있다.

### 수정 및 필수 규칙

- `--keyboard-height`는 CTA 위치 조정용, `--keyboard-scroll-height`는 본문 스크롤 여백용으로 분리한다.
- iOS는 CTA를 키보드 높이만큼 올리고, Android는 CTA를 중복으로 올리지 않는다.
- 단, 본문 스크롤 영역은 iOS/Android 모두 키보드 높이만큼 추가 여백을 확보한다.
- 입력칸 선택 시 무조건 실행하는 `scrollIntoView()`는 사용자가 의도하지 않은 위치로 화면을 튀게 만들 수 있으므로 사용하지 않는다.
- 기본값은 스크롤 컨테이너의 하단 여백을 키보드 높이만큼 확보하는 것이다. 예외적으로 유저 프로필 하단 textarea는 키보드 표시가 끝난 뒤 입력란이 실제로 가려졌을 때만 최소 거리 `scrollBy()`를 허용한다.

## 2026-08-13 Firestore 조회 한도 소진 방지

### 증상

- Firestore에서 `RESOURCE_EXHAUSTED: Quota exceeded`가 발생해 일기·채팅·관리자 조회가 실패할 수 있음.

### 원인

- 어드민 대시보드와 사용자 목록에서 `characters`, `diaries`, `chatMessages` 전체 컬렉션을 직접 읽어 프론트에서 개수를 계산하면 문서 수만큼 읽기가 발생한다.
- 사용자 상세·페어 상세에서 모든 일기·채팅 본문을 한 번에 읽으면 헤비 유저에서 조회량이 급증한다.
- 최적화 쿼리 실패 시 전체 목록으로 fallback하는 로직도 숨은 한도 소진 원인이 될 수 있다.

### 수정 및 필수 규칙

- 개수만 필요한 어드민 대시보드는 전체 문서 조회 대신 Firestore count 집계를 사용한다.
- 어드민 사용자 목록은 가입 계정 기준 20명씩 불러오고, 각 사용자의 개수 정보만 count로 조회한다.
- 어드민 페어 상세의 실제 일기·채팅 본문은 각각 20개씩만 불러오고 `더보기`로 추가 조회한다.
- 사용자 일기 모아보기는 페어별 최신 일기 10개씩만 불러오고 `더보기`로 추가 조회한다.
- 최적화 쿼리가 실패했을 때 전체 일기·채팅 목록으로 자동 fallback하지 않는다.
- 일반 채팅 상세는 아직 대화방 전체 메시지를 읽는 구조이므로, 채팅량이 커지면 별도 페이지네이션 대상이다.

### 2026-08-13 일기 수정·삭제 조회 원칙

#### 목적

- 사용자가 작성 완료한 일기의 유저 입력과 AI 답변을 수정하거나 일기 1건을 삭제할 수 있게 한다.

#### 필수 규칙

- 일기 수정·삭제는 현재 보고 있는 `diaries/{diaryId}` 단일 문서만 대상으로 한다.
- 클라이언트에서 전체 일기 목록을 다시 읽어 수정·삭제 여부를 판단하지 않는다.
- 앱 보안 규칙 차이로 직접 Firestore 쓰기가 실패할 수 있으므로 `netlify/functions/diary-edit.ts`를 통해 서버에서 소유자 `userId`를 확인한 뒤 수정·삭제한다.
- 삭제 후 오늘 일기 화면은 우선 로컬 상태를 작성 가능 상태로 돌리고, 다음 주제 계산은 해당 페어의 count 조회 1회만 허용한다.
- 수정 중 textarea는 키보드가 열린 상태에서도 주변 안내문구와 하단 버튼을 스크롤로 확인할 수 있도록 `--keyboard-scroll-height` 하단 여백을 반영한다.

### 2026-08-13 채팅 목록 최신 메시지 fallback

#### 증상

- 채팅 상세에는 선톡·기존 메시지가 있는데 채팅 목록에는 `새로운 메시지가 있어요`만 표시될 수 있음.
- 홈 선톡 입력중 애니메이션이 잠깐 보였다가 최신 메시지 카드로 교체되지 않는 것처럼 보일 수 있음.

#### 원인

- 조회 한도 보호를 위해 최신 메시지 최적화 쿼리 실패 시 전체 채팅 목록 fallback을 제거했다.
- 하지만 최적화 쿼리가 인덱스·네트워크·일시 오류로 실패하면 목록에서는 실제 메시지가 있어도 `null`로 처리됐다.

#### 수정 및 필수 규칙

- 최신 메시지 쿼리가 실패해도 전체 채팅을 읽지 않는다.
- 대신 같은 `userId + characterId` 조건에서 최대 30개만 읽는 bounded fallback을 사용하고, 클라이언트에서 최신 1건만 선택한다.
- 이 fallback도 실패하면 Firebase 한도·권한·네트워크 문제로 보고 에러 로그를 확인한다.

## 2026-08-13 한국어 일기 답변에 비한국어 토큰 삽입

### 증상

- 한국어 교환일기 AI 답변 중간에 `C`, `HP`, `SHLLP`처럼 의미 없는 영어 알파벳 조각이 섞여 출력됨.

### 원인

- 일기 저장·중복 방지 코드는 OpenRouter가 반환한 `charReply`를 후처리 없이 저장·반환한다.
- 따라서 문장 중간의 영어 조각은 DB 저장 로직이 끼워 넣은 값이 아니라 AI 모델 출력 자체로 보는 것이 맞다.
- 최근 `{유저}`, `{캐릭터}` 조사 처리로 프롬프트 문자열이 일부 포맷팅되도록 바뀌었기 때문에 모델 출력 품질에 간접 영향을 줄 수는 있다.

### 수정 및 필수 규칙

- 한국어 일기 답변은 저장 전 `src/lib/aiReplyGuard.ts`로 비한국어 문자 검수를 수행한다.
- 영어 알파벳, 일본어, 중국어 한자, 기타 외국어 문자 스크립트가 감지되면 답변을 저장하지 않고 한 번 재생성한다.
- 재생성 후에도 비한국어 문자가 남아 있으면 해당 답변은 저장하지 않는다.
- 일기·채팅 OpenRouter temperature는 `0.7`을 기준으로 사용한다.
- 사용자 일기 본문은 민감하므로 비한국어 감지 로그에 그대로 남기지 않는다. 로그에는 characterId, dateString, requestId, 감지된 짧은 segment만 남긴다.

## 2026-08-12 교환일기 중복 생성

### 증상

- 사용자가 같은 날짜·같은 캐릭터의 교환일기를 작성했는데 Firestore `diaries`에 2개 문서가 생성됨.
- 화면에서는 저장 중 버튼이 비활성화되어 있어 일반적인 중복 클릭으로는 설명이 어려웠음.

### 확인 결과

- 실제 Firestore에서 `userId + characterId + dateString`이 같은 중복 그룹이 확인됨.
- 최근 케이스는 두 문서의 `requestId`가 서로 달라, 같은 버튼 클릭의 단순 재시도라기보다 사용자가 답장이 안 온 것으로 보고 다시 전송했거나 화면 복귀·재시도 과정에서 새 요청이 들어간 것으로 판단됨.
- 과거 케이스는 `requestId`가 같은 중복도 있었으므로 서버 타임아웃·재시도·동시 처리도 방어해야 함.

### 원인

- 기존 일기 저장 API가 `crypto.randomUUID()`로 매번 새 문서 ID를 만들었음.
- UI에서 버튼을 막아도 서버/DB에는 “같은 유저·같은 캐릭터·같은 날짜 일기는 1개만 저장”이라는 강제 규칙이 없었음.

### 수정 및 필수 규칙

- 일기 문서 ID는 `src/lib/db.ts`의 `getDiaryDailyDocId(userId, characterId, dateString)`으로만 만든다.
- 같은 날짜의 같은 캐릭터 일기는 Firestore 문서 ID를 고정하여 1개만 저장한다.
- `/api/diary`는 AI 답변 생성 전 기존 일기를 먼저 확인하고, 저장 시에도 Firebase Admin 트랜잭션으로 이미 저장된 문서가 있으면 기존 일기를 반환한다.
- Netlify 함수·Next 로컬 API route 모두 같은 `netlify/functions/diary.ts` 핸들러를 사용한다. `npm run dev` 로컬 테스트에서도 `/api/diary`가 404가 되면 안 된다.
- 서버 함수에서는 클라이언트 Firestore SDK(`src/lib/db.ts`)로 일기를 저장하지 않는다. 비로그인 UUID·로그인 uid 모두 보안 규칙에 막히지 않게 Firebase Admin으로 저장한다.
- `requestId`는 네트워크 재시도 추적용으로만 사용하고, 일기 중복 방지 기준으로 단독 의존하지 않는다.
- 새 일기 저장 로직에서 `crypto.randomUUID()`로 `diaries` 문서 ID를 만들지 않는다.

### 회귀 확인

1. 같은 유저·같은 캐릭터·같은 날짜로 `/api/diary`를 2번 호출해도 Firestore `diaries` 문서가 1개만 생성되는지 확인한다.
2. 두 번째 호출은 기존 일기의 `savedId`와 답장을 반환하는지 확인한다.
3. 앱에서 일기 작성 완료 후 화면을 나갔다가 같은 날짜에 다시 전송해도 새 일기가 추가 생성되지 않는지 확인한다.

## 2026-08-12 Clarity 사용자 ID 매칭

### 증상

- Firestore/Firebase의 Dreamary `userId`로 Microsoft Clarity에서 사용자를 검색해도 세션이 나오지 않음.

### 원인

- Clarity의 기본 사용자 ID는 Clarity가 쿠키·기기 기준으로 만든 별도 ID다.
- Dreamary `userId`를 Clarity에서 검색하려면 클라이언트에서 `clarity('identify', userId)`로 Custom user ID를 별도 전송해야 한다.
- 기존 코드는 `language` custom tag만 보내고 Dreamary `userId`를 Clarity custom user id로 보내지 않았음.

### 수정 및 필수 규칙

- `src/app/ClientLayoutWrapper.tsx`에서 로그인 `uid` 또는 비로그인 UUID를 Clarity `identify`로 보낸다.
- 같은 값을 `dreamary_user_id` custom tag로도 남겨 추후 필터링을 쉽게 한다.
- 이메일, 이름 등 직접 개인정보는 Clarity custom id/tag로 보내지 않는다.
- Clarity에서 찾을 때는 기본 `Clarity ID`가 아니라 `Custom user ID` 또는 `dreamary_user_id` custom tag 필터를 사용한다.

## 2026-08-12 웹 광고 후 일기 작성 상태 유실

### 증상

- 개발 서버 웹 브라우저에서 일기 작성 중 광고 모달 확인을 누르면 광고 새창이 열림과 동시에 기존 일기 브라우저 탭도 광고 URL로 이동할 수 있었음.
- 사용자가 광고 새창을 닫고 기존 탭에서 뒤로가기로 돌아오면 일기 작성 전 상태가 되고, 작성 중이던 내용이 사라짐.
- 다시 같은 일기를 작성하면 `잠시 후 다시 시도해주세요` 모달이 뜨며 작성이 막힐 수 있었음.

### 원인

- 웹 광고 열기 함수가 `window.open(..., 'noopener,noreferrer')`를 사용했는데, 브라우저에 따라 새창이 열려도 반환값이 `null`이 될 수 있음.
- 기존 코드는 반환값이 `null`이면 팝업 차단으로 오판하고 `window.location.href = 광고 URL`로 현재 드림어리 탭을 직접 이동시켰음.
- 일기 광고 카운트를 답장 저장 성공 전, 전송 시도 시점에 먼저 증가시켜 실패한 광고·실패한 작성도 광고 턴으로 소모될 수 있었음.

### 수정 및 필수 규칙

- 웹 광고는 현재 드림어리 탭을 절대 광고 URL로 이동시키지 않는다.
- 웹 광고는 사용자 클릭 직후 `about:blank` 새창을 먼저 열고, 광고 네트워크 접근 확인 후 그 새창만 광고 URL로 이동한다.
- 새창이 열리지 않거나 광고 차단이 의심되면 현재 입력값을 보존하고 답장 API를 호출하지 않는다.
- 일기 광고 카운트는 채팅과 동일하게 AI 답장 저장 성공 후에만 증가시킨다.
- 광고 대상 일기 전송 전에는 `diary` scope draft를 저장해, 브라우저 새로고침·탭 복귀가 발생해도 입력값을 복구할 수 있게 한다.

### 회귀 확인

1. 웹 개발 서버에서 광고 대상 일기 작성 → 광고 확인 선택 시 기존 드림어리 탭 URL이 광고 URL로 바뀌지 않는지 확인한다.
2. 광고 새창을 닫고 원래 탭으로 돌아와도 작성 중 입력값이 보존되거나 정상 답장이 완료되는지 확인한다.
3. 광고 차단·팝업 차단 상태에서는 `잠시 후 다시 시도해주세요` 대신 광고 열기 실패 안내가 나오고, 입력값이 일기 입력란에 남는지 확인한다.

## 2026-08-12 App Store 업로드 Invalid bundle / app icon

### 증상

- TestFlight/App Store Connect 업로드 시 `Invalid bundle` 코드 90474가 발생함.
- `UISupportedInterfaceOrientations`가 Portrait만 있는데 iPad 멀티태스킹을 지원하려면 모든 방향을 포함해야 한다는 오류가 표시됨.
- 동시에 `Invalid large app icon` 코드 90717이 발생하고, 큰 앱 아이콘이 투명하거나 alpha channel을 포함할 수 없다는 오류가 표시됨.

### 원인

- Xcode 프로젝트의 `TARGETED_DEVICE_FAMILY`가 `"1,2"`로 설정되어 iPhone+iPad 앱으로 판정됨.
- 실제 서비스는 iPhone만 대상으로 하지만 iPad 대상이 포함되어 Apple이 iPad 멀티태스킹 방향 규칙을 요구함.
- iOS 1024x1024 앱 아이콘 PNG가 눈으로는 불투명해 보여도 파일 형식이 RGBA라 alpha channel을 포함하고 있었음.

### 수정 및 필수 규칙

- iPad를 지원하지 않는 동안 `ios/App/App.xcodeproj/project.pbxproj`의 Debug/Release `TARGETED_DEVICE_FAMILY`는 `1`로 유지한다.
- iPhone 전용 앱에서는 `Info.plist`에 `UISupportedInterfaceOrientations~ipad`를 추가하지 않는다.
- iOS 앱 아이콘 `ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`는 반드시 `1024x1024 RGB PNG`이고 `hasAlpha: no`여야 한다.
- 아이콘 검증은 `sips -g hasAlpha ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`로 확인한다.

## 2026-08-11 한국어 이름 조사 처리

### 필수 구조

- 한국어 이름 조사는 `src/lib/koreanJosa.ts`의 공통 유틸만 사용한다.
- 지원 조사는 `이/가`, `을/를`, `은/는`, `으로/로`, `과/와`, `아/야`다.
- `{유저}`, `{캐릭터}` 템플릿은 `formatKoreanNameTemplate()`로 치환한다.
- `{유저}가`, `{유저}는`, `{캐릭터}를`처럼 이름 바로 뒤에 조사가 붙은 경우에도 공통 유틸로 처리한다.
- 화면 표시용 주제, 일기 API로 전달되는 주제, 푸시 후보 문구, AI 채팅·일기 프롬프트는 같은 조사 규칙을 사용한다.
- 일본어 문구는 조사 변환을 적용하지 않고 `{ユーザー}`, `{キャラクター}` 단순 치환만 허용한다.
- 영문·숫자로 끝나는 이름은 현재 첫 번째 조사를 사용한다. 영문 발음 기반 조사는 별도 규칙 확정 전까지 추가하지 않는다.

## 2026-08-09 교환일기 푸시 알림 구현 원칙

### 목적

- 사용자가 요청한 기능성 알림으로, 광고 푸시가 아니다.
- 매일 한국시간 20시에 교환일기를 쓸 수 있음을 알려준다.

### 필수 구조

- 비로그인 사용자는 서버 푸시 대상에 등록하지 않고, `dreamary_diary_push_pending_opt_in`으로 로그인 후 이어가기만 저장한다.
- 로그인 후 Firebase `uid` 기준으로 `pushDevices`와 `diaryPushTargets`를 저장한다.
- 앱 내부 `교환일기 알림` ON은 기기 OS 알림 권한과 분리한다. OS에서 드림어리 알림이 꺼져 있으면 먼저 기기 설정 화면으로 보낸 뒤, 앱 복귀 시 권한을 재확인하고 서버 등록을 이어간다.
- Android는 `NativeSettingsPlugin.openAppNotificationSettings()`로 앱 알림 설정 화면을 연다. iOS 최초 1회는 시스템 권한 팝업을 사용하고, 이후 거절 상태에서는 iOS 16 이상 `UIApplication.openNotificationSettingsURLString`로 앱 알림 설정 화면을 연다. iOS 16 미만은 공개 API 제한으로 앱 설정 화면으로 fallback한다.
- iOS의 `NativeSettingsPlugin`은 `CAPBridgeViewController.capacitorDidLoad()`에서 `registerPluginInstance(NativeSettingsPlugin())`로 등록한다. `registerPluginType()`은 자동 플러그인 등록 모드에서 무시되어 JS 호출이 네이티브로 전달되지 않을 수 있으므로 사용하지 않는다.
- 알림설정 화면의 기기 권한 안내는 앱 내부 `교환일기 알림` ON/OFF와 별개로, `PushNotifications.checkPermissions().receive !== 'granted'`이면 노출한다. iOS/Android가 `denied`가 아닌 `prompt`, `prompt-with-rationale`, `unknown`을 반환해도 안내가 사라지면 안 된다.
- 알림설정 화면의 `교환일기 알림` 토글은 진입 즉시 `dreamary_diary_push_enabled_{uid}` 로컬 캐시값을 먼저 보여주고, `/api/push/status` 응답이 오면 서버의 `diaryPushTargets/{uid}.enabled` 값으로 보정한다. 토글 초기 렌더가 서버 응답을 기다리게 만들면 앱에서 느린 화면처럼 보인다.
- 푸시 저장 구조는 계정 설정과 기기 설정을 분리한다. `diaryPushTargets/{uid}.enabled`는 계정 단위 일기 알림 ON/OFF이고, `pushDevices/{uid}_{deviceId}`는 기기별 토큰·플랫폼·`diaryPushEnabled`를 저장한다.
- 로그인 사용자가 앱을 열었을 때 계정의 일기 알림이 ON이고 현재 기기 OS 권한이 허용 상태라면, 현재 기기의 푸시 토큰을 다시 등록한다. 로그인만 하고 토큰 재등록을 하지 않으면 오래된 Android FCM 토큰이 `NotRegistered`로 실패해 iOS에만 푸시가 가는 것처럼 보일 수 있다.
- 알림 토글 ON은 푸시 토큰 저장과 `diaryPushTargets.enabled` 저장을 기준으로 성공 처리한다. 페어별 다음 주제 후보 캐시 재계산이 실패해도 알림 설정 자체를 실패시키지 않는다.
- 푸시 등록 시 후보 캐시 재계산은 Firestore 읽기 한도·인덱스·일시 오류에 취약할 수 있으므로 실패 시 로그만 남기고, 일기 작성 완료 또는 스케줄러 후보 보정에서 다시 갱신한다.
- Capacitor 권한값만으로 완료 처리하지 않는다. Android는 `NotificationManagerCompat.areNotificationsEnabled()`와 `diary` 채널 차단 여부를 함께 확인하고, iOS는 `UNUserNotificationCenter.getNotificationSettings()`의 실제 authorization status를 함께 확인한다.
- iOS 푸시 등록에는 `App.entitlements`의 `aps-environment`와 Xcode Push Notifications capability가 필요하다. 이 설정이 없으면 사용자가 알림 허용을 눌러도 APNs 토큰 등록에서 실패해 `알림 설정에 실패했어요`로 떨어질 수 있다.
- iOS Bundle Identifier는 Apple Developer에 등록된 `com.repov.dreamary`를 사용한다. Firebase iOS 앱도 같은 Bundle ID로 새로 등록하고 `ios/App/App/GoogleService-Info.plist`를 교체해야 한다.
- 웹 버전에는 교환일기 푸시 알림 UI를 노출하지 않는다. 마이페이지 알림설정 버튼과 일기 완료 후 알림 유도 바텀시트는 `NEXT_PUBLIC_BUILD_TARGET=app`에서만 동작한다.
- 일기 작성 완료 후에는 기존 전체 일기 목록을 읽지 않고 `diaryPushTargets/{uid}`의 `nextNotifyAt`만 다음날 20시로 갱신한다.
- 알림 등록·일기 작성 완료 시 `diaryPushTargets/{uid}.candidates`에 페어별 다음 주제 후보를 미리 저장한다.
- 후보는 `lastDiaryDate`, `nextTopicOrder`, `nextTopicContent`, `nextTopicId`를 포함한다.
- 여러 페어가 있는 사용자는 스케줄러 발송 시 후보 중 `lastDiaryDate !== 오늘`인 페어만 대상으로 보고, 그중 `nextTopicOrder`가 가장 높은 후보를 선택한다.
- 한국어 푸시는 후보가 있으면 `{nextTopicOrder}번째 주제가 도착했어요!` / `{nextTopicContent}`로 발송한다. 일본어 푸시는 별도 번역 확정 전까지 기존 고정 문구를 유지한다.
- Netlify 스케줄러는 `diaryPushTargets`에서 `enabled == true`이고 `nextNotifyAt <= now`인 문서만 조회하며, 발송 시점에 전체 캐릭터·전체 일기를 다시 훑지 않는다.
- 전체 `accounts`, `characters`, `diaries` 컬렉션을 매일 훑는 방식은 Firestore 조회 한도를 빠르게 소모하므로 금지한다.
- 즉시 푸시 확인은 `/api/admin/test-diary-push` 관리자 테스트 함수만 사용한다. 이 함수는 `PUSH_TEST_SECRET` 또는 `ADMIN_PASSWORD` 없이는 호출되지 않아야 하며, 최대 10개 계정 아이디만 대상으로 한다.
- 테스트 푸시 함수는 `title`과 `body`를 받으면 해당 문구를 그대로 보내고, 둘 다 없으면 운영 교환일기 후보 로직으로 한국어 `{n}번째 주제가 도착했어요!` / `{주제}` 문구를 만든다. 푸시 문구만 바꿔 테스트할 때는 재배포하지 않는다.
- 푸시 테스트 함수는 전체 유저 발송 기능을 제공하지 않는다. 전체 발송이 필요해 보이더라도 별도 승인과 설계 없이 테스트 함수를 확장하지 않는다.
- 회원 탈퇴 시 해당 uid의 `pushDevices`와 `diaryPushTargets`도 함께 삭제한다.
- iOS에서 시스템 알림 권한 팝업을 한 번도 본 적 없는 상태(`UNAuthorizationStatus.notDetermined`)는 설정앱 이동이 아니라 `PushNotifications.requestPermissions()`로 먼저 처리한다. 이 상태에서는 iOS 설정 앱의 알림 목록에 드림어리가 아직 보이지 않을 수 있다.
- Android 앱 아이콘은 적응형 아이콘 마스크 때문에 원본 1024px 이미지를 foreground에 그대로 넣으면 삼성 런처에서 로고가 잘릴 수 있다. Android 아이콘 리소스는 중앙 로고를 72% 이하로 축소하고 `ic_launcher_background` 색상과 맞춘다.

### 회귀 확인

1. 비로그인 일기 작성 완료 후 알림 바텀시트 → 로그인 안내 → 로그인 후 알림 설정 이어가기를 확인한다.
2. 로그인 상태에서 알림 허용 후 Firestore `pushDevices`, `diaryPushTargets` 문서가 생성되는지 확인한다.
3. 같은 날 일기 작성 완료 후 `nextNotifyAt`이 다음날 한국시간 20시로 갱신되는지 확인한다.
4. Netlify Functions에서 `diary-push-scheduler`를 수동 실행해 대상 uid의 기기에만 푸시가 발송되는지 확인한다.
5. 탈퇴 후 해당 uid의 푸시 문서가 남지 않는지 확인한다.

## 2026-08-09 일기 탭 작성 가능 화면 깜빡임

### 증상

- iOS에서 페어가 하나뿐이고 오늘 일기를 이미 작성한 계정이 `/diary`에 들어오면, 작성 가능 화면이 잠깐 보인 뒤 `/diary/history`로 이동했다.

### 원인

- `/diary` 진입 시 로컬 캐시가 먼저 적용되는데, 캐시에 `todayDiary: null`이 남아 있으면 서버의 오늘 작성 여부 확인이 끝나기 전에 작성 화면이 먼저 렌더될 수 있었다.

### 방지 규칙

- 실제 페어가 있고 오늘 일기 상태가 확정되지 않은 캐시는 작성 화면으로 표시하지 않는다.
- 단일 페어이고 캐시에 오늘 일기가 이미 있으면 `/diary/history`로 즉시 이동한다.
- 하단탭 활성 상태는 `src/lib/navigation.ts`의 `getBottomNavActivePath()` 한 곳에서만 계산해 한 번에 하나의 탭만 선택되게 한다.

## 2026-08-08 정적 앱 상세 화면이 즉시 목록으로 복귀

### 증상

- 채팅 목록에서 실제 채팅방을 열면 잠깐 로딩된 뒤 채팅 목록으로 돌아감
- 일기 모아보기에서 일기를 선택하면 상세 대신 모아보기 목록으로 돌아감

### 원인

- Capacitor 정적 빌드는 물리 페이지 `/chat/1`, `/diary/history/1`을 재사용하고 실제 문서 ID는 `entityId` query에 담음
- 상세 컴포넌트가 렌더 중 `window.location.search`를 직접 읽어, Next.js 소프트 이동에서 주소 갱신 전 빌드용 ID `1`을 사용함
- Firestore에서 ID `1` 문서를 찾지 못하자 기존 보호 로직이 목록으로 이동시킴

### 수정 및 필수 규칙

- 상세 페이지는 `useSearchParams()`로 `entityId` 변경을 구독한다.
- 정적 출력에서 `useSearchParams()`를 쓰는 상세 페이지는 `Suspense` 경계 안에서 렌더한다.
- 실제 문서를 확인하기 전에는 빌드용 ID `1`로 Firestore를 조회하거나 목록으로 리다이렉트하지 않는다.
- 같은 물리 상세 경로에서 이전·다음 항목으로 이동할 때도 query 변경으로 데이터를 다시 불러와야 한다.

### 회귀 확인

1. 앱 채팅 목록에서 서로 다른 캐릭터 채팅방을 차례로 열어 각각 유지되는지 확인한다.
2. 일기 모아보기에서 일기 상세로 진입한 뒤 이전·다음 일기로 이동한다.
3. 어떤 상세 화면도 진입 직후 목록으로 자동 복귀하지 않는지 확인한다.

## 2026-08-06 Android 캐릭터 저장 시간초과

### 증상

- 비로그인 상태에서 캐릭터 생성을 완료하면 `캐릭터 저장 시간이 초과되었습니다.`가 표시됨
- 15초 제한시간을 늘려도 해결되지 않음
- 저장 버튼 재시도 시에도 같은 오류 반복

### 실제 저장 여부

- 오류가 발생한 비로그인 UUID로 Firestore `characters`를 직접 조회한 결과 문서 수는 0개였음
- 서버가 쓰기를 확정하기 전 통신 스트림이 끊겼으므로 캐릭터는 저장되지 않았음

### 확인 로그

```text
CapacitorHttp ... firestore.googleapis.com/.../Firestore/Write/channel ... status 200
@firebase/firestore: WebChannelConnection RPC 'Write' ... transport errored
Error: 캐릭터 저장 시간이 초과되었습니다.
```

HTTP 200은 저장 완료가 아니라 Firestore 스트리밍 채널의 최초 연결 응답이다. 그 뒤에 와야 하는 쓰기 완료 응답을 받지 못했다.

### 원인

- `capacitor.config.ts`에서 `CapacitorHttp.enabled: true`로 전역 `fetch`와 `XMLHttpRequest`를 네이티브 HTTP로 교체함
- Firestore Web SDK는 여러 응답을 이어 받는 WebChannel 스트림을 사용함
- CapacitorHttp는 응답을 한 번에 버퍼링하고 종료된 일반 HTTP 응답으로 반환하여 Firestore 스트림이 매번 끊김
- Firestore가 내부 재시도를 계속하므로 `setDoc()` Promise가 끝나지 않았고 앱의 15초 제한시간이 먼저 종료됨

### 수정 및 필수 규칙

- `CapacitorHttp.enabled`는 iOS와 Android 모두 `false`로 유지한다.
- Firestore는 WebView 기본 네트워크와 `experimentalAutoDetectLongPolling`을 사용한다.
- 단순 HTTP 요청에 네이티브 HTTP가 필요하면 전역 패치가 아닌 명시적인 `CapacitorHttp.request()`만 검토한다. Firestore와 스트리밍 채팅 API에는 사용하지 않는다.
- 캐릭터 저장 재시도는 같은 문서 ID를 재사용한다. 타임아웃 직후 원래 요청이 늦게 완료되어도 중복 캐릭터가 생기지 않게 한다.
- 이 설정을 바꾼 뒤에는 반드시 `npm run build:app`, `npx cap sync`, 실제 기기 재설치 순서로 확인한다.

### 회귀 확인

1. Android 실제 기기에서 비로그인 캐릭터를 생성한다.
2. 저장 완료 후 홈·채팅 탭에 같은 캐릭터가 한 번만 노출되는지 확인한다.
3. Firestore에서 해당 UUID의 `characters` 문서와 캐릭터 ID 기반 `users` 문서를 확인한다.
4. Logcat에서 `Firestore/Write/channel` 요청이 CapacitorHttp로 전달되지 않는지 확인한다.
5. `WebChannelConnection RPC 'Write' ... transport errored`가 반복되지 않는지 확인한다.

## 2026-08-06 iOS/AOS 캐릭터 생성 후 선톡 미수신

### 증상

- 캐릭터 저장은 성공하지만 홈과 채팅방에 캐릭터의 첫 메시지가 나타나지 않음
- 앱을 껐다 켜거나 채팅방에 다시 들어가도 재시도되지 않음
- 실제 iPhone 비로그인 데이터 조회에서도 캐릭터 1건, 채팅 메시지 0건으로 확인됨

### 원인

- 네이티브 앱 origin은 `https://localhost`인데 운영 `/api/chat`의 OPTIONS 응답에 `Access-Control-Allow-Origin`이 없어 WebView가 POST 전에 요청을 차단함
- 기존 코드는 API 결과를 받기 전에 `hasPinged_{characterId}=true`를 저장함
- 실제 Firestore 채팅 문서는 0건인데 로컬 플래그만 성공으로 남아 홈·채팅 상세의 재시도를 막음
- 동일 선톡 호출이 온보딩·홈·채팅 상세 세 곳에 중복 구현되어 실패 처리 방식도 서로 달랐음

### 확인 로그 및 운영 응답

```text
Access to fetch at 'https://dreamary.netlify.app/api/chat' from origin
'https://localhost' has been blocked by CORS policy
```

운영 서버에 OPTIONS 요청을 직접 확인했을 때 HTTP 204였지만 CORS 허용 헤더가 없었다.
같은 운영 API에 저장 없는 진단 POST를 직접 보냈을 때는 HTTP 200과 정상 답장이 반환되어 AI 생성 서버 자체는 정상임을 확인했다.

### 수정 및 필수 규칙

- 선톡은 `src/lib/initialPing.ts`의 `ensureInitialPing()`으로만 요청한다.
- 네이티브 선톡은 단발성 JSON 요청이므로 명시적 `CapacitorHttp.post()`를 사용한다. 전역 `CapacitorHttp.enabled`는 계속 `false`다.
- 웹은 동일 출처 fetch를 사용한다.
- `hasPinged_*`는 API가 성공하고 답장 본문을 받은 뒤에만 기록한다.
- 로컬 플래그와 관계없이 Firestore 채팅이 0건이면 선톡을 다시 요청한다.
- 같은 캐릭터의 동시 호출은 하나의 Promise를 공유하며, `requestId`를 고정하여 재시도 중복을 방지한다.
- Netlify `/api/chat`은 `netlify/functions/chat`으로 명시적으로 연결하고 함수가 OPTIONS와 실제 응답 모두 CORS 헤더를 반환하게 한다.

### 회귀 확인

1. 기존에 `hasPinged_*`만 남고 채팅이 없는 캐릭터로 홈 또는 채팅 상세에 진입한다.
2. 선톡 1건이 Firestore `chatMessages`에 저장되고 화면에 표시되는지 확인한다.
3. 홈과 채팅 상세를 빠르게 왕복해도 선톡이 2건 생기지 않는지 확인한다.
4. iOS와 Android에서 각각 새 캐릭터를 만들고 앱 재실행 후에도 선톡이 1건만 유지되는지 확인한다.

## 2026-08-06 iOS 채팅 입력창 하단 공백

### 증상과 원인

- 채팅 상세 입력창이 화면 하단에서 크게 떠 있고 그 아래에 불필요한 빈 영역 또는 하단 탭이 보임
- `.full-page`의 공통 viewport 높이와 인라인 `max-height: -webkit-fill-available`이 동시에 적용되어 WKWebView에서 콘텐츠 높이가 이중으로 제한됨
- 구형 iOS 번들에서는 채팅 상세가 풀페이지라는 중앙 하단 탭 규칙도 반영되지 않아 공백이 더 크게 보였음

### 수정 및 필수 규칙

- 채팅 상세는 `.full-page`의 `--app-viewport-height`만 사용하고 `-webkit-fill-available`을 사용하지 않는다.
- 입력 영역은 `.chat-input-area` 공통 클래스로 하단 안전영역을 한 번만 반영한다.
- 채팅 상세는 풀페이지이므로 하단 탭을 절대 생성하지 않는다.

## 2026-08-06 앱 광고 후 에러 모달

### 증상

- Android에서 광고 웹페이지가 열리지 않음
- iOS에서 광고 웹페이지는 열렸지만 드림어리로 돌아온 뒤 `잠시 후 다시 시도해주세요` 모달이 표시됨
- 재시도 시 두 플랫폼 모두 같은 에러 모달이 반복될 수 있음

### 원인

- 광고 모달 확인 직후 답장 생성 API와 광고 브라우저 호출이 동시에 진행되어, 광고 성공 여부와 드림어리 서버 실패가 같은 에러 흐름으로 섞였음
- 기존 광고 완료 기준이 `window.open()` 또는 `Browser.open()` 호출 시점에 가까워 실제 네이티브 브라우저 열림/복귀 상태를 구분하기 어려웠음
- Netlify 설정에 `/api/chat` 연결만 있고 `/api/diary` 연결이 없어 앱의 일기 답장 API 호출이 배포 환경에서 흔들릴 수 있었음

### 수정 및 필수 규칙

- 광고 대상 턴은 광고 웹페이지가 실제로 열렸을 때만 채팅·일기 답장 API를 호출한다.
- 광고 웹페이지가 열리면 광고는 성공으로 보고 답장을 제공한다. 별도의 클라이언트 잠금해제 저장 실패 때문에 사용자에게 에러 모달을 띄우지 않는다.
- 광고 웹페이지가 열리지 않으면 답장 API를 호출하지 않고 입력값을 보존한다.
- 광고 도메인 접근이 DNS·광고 차단 앱·콘텐츠 차단으로 막힌 것으로 의심되면 `ad_block_suspected`로 기록한다. 이 로그는 서버 전송이 아니라 기기 로컬 `dreamary_ad_diagnostics`에만 남긴다.
- 광고 호출 실패는 `ad_open_failed`, 드림어리 서버/API 실패는 `app_server_request_failed`, 기존 잠금 콘텐츠 해제 실패는 `ad_unlock_failed`로 `dreamary_ad_diagnostics` 로컬 로그에 남긴다.
- Netlify는 `/api/chat`과 `/api/diary` 모두 함수 리다이렉트를 명시한다.

### 회귀 확인

1. 광고 대상 채팅 턴에서 광고 웹페이지가 열리면 앱 복귀 후 답장이 표시되는지 확인한다.
2. `dns.adguard.com` 같은 개인 DNS 차단 환경에서는 사용자 입력이 보존되고 답장 API가 호출되지 않는지 확인한다.
3. iOS와 Android에서 `localStorage.dreamary_ad_diagnostics`에 광고 차단 의심, 광고 열기 실패, 서버 실패가 다른 타입으로 기록되는지 확인한다.

## 2026-08-06 앱 채팅 첫 전송 실패와 일기 임시입력 오염

### 증상

- iOS와 Android에서 첫 채팅 전송부터 `잠시 후 다시 시도해주세요`가 표시되고 답장이 오지 않음
- 일기는 같은 기기에서 정상 동작함
- 채팅 전송에 실패한 문장이 이후 일기 입력란에 자동으로 들어갈 수 있음

### 원인

- 채팅은 광고 턴이 아닐 때 네이티브 앱에서도 웹용 스트리밍 `fetch`를 사용하고 있었음
- 일기는 이미 단발성 JSON API를 사용해 네이티브 CORS/스트리밍 병목을 피하고 있었기 때문에 정상 동작했음
- 채팅 광고 카운트를 API 성공 전에 증가시켜 실패한 전송도 광고 턴 계산에 포함될 수 있었음
- 채팅과 일기가 같은 `dreamary_draft_{characterId}` 임시저장 키를 공유하여 실패한 채팅 입력이 일기 draft로 복원될 수 있었음
- 채팅 메시지를 API 호출 전에 Firestore에 저장한 뒤 실패 시 원격 문서를 삭제하지 않아 구독으로 실패 메시지가 다시 나타날 수 있었음

### 수정 및 필수 규칙

- 네이티브 앱의 일반 채팅 전송은 `preferJsonResponse: true`가 포함된 `apiPostJson('/api/chat')`로 처리한다.
- 웹 채팅은 기존 스트리밍 응답을 유지한다.
- 채팅 광고는 성공한 AI 답장 수 기준으로 3번째, 6번째, 9번째에만 노출한다.
- 광고가 열리지 않거나 서버/API가 실패한 채팅은 광고 카운트를 증가시키지 않는다.
- 채팅 실패 입력은 `chat` scope draft에만 저장하고, 일기는 `diary` scope draft만 읽는다.
- 전송 실패 시 API 호출 전에 저장한 사용자 채팅 문서를 삭제하여 채팅방에는 실패 메시지가 남지 않고 입력칸에만 복원되게 한다.

### 회귀 확인

1. 새 채팅방에서 1번째와 2번째 채팅은 광고 없이 전송되고 AI 답장이 표시되는지 확인한다.
2. 3번째 채팅에서 광고 웹페이지가 열리고, 앱 복귀 후 AI 답장이 표시되는지 확인한다.
3. 3번째 채팅에서 광고가 차단되면 답장 API가 호출되지 않고 입력값이 채팅 입력칸에 남는지 확인한다.
4. 채팅 실패 후 일기 탭에 진입해도 실패한 채팅 문장이 일기 입력란에 자동 입력되지 않는지 확인한다.

## 2026-08-13 일기 작성·히스토리 조회 불가와 Firestore 한도 초과

### 증상

- 운영 배포 후에도 일기 작성 또는 일기 히스토리 조회가 실패함
- `netlify logs --source functions --function diary --since 30m --level error`에는 새 `diary` 함수 에러가 없을 수 있음
- 전체 함수 로그에는 `RESOURCE_EXHAUSTED: Quota exceeded`가 `push-status`, `push-register`, `___netlify-server-handler` 등에서 반복됨

### 원인

- 이전 `diary` 함수의 `ERR_REQUIRE_ESM`은 배포로 해결됐더라도, Firestore 일일 읽기/쓰기 한도가 이미 초과되면 클라이언트 Firestore 조회와 서버 Admin Firestore 조회가 모두 실패할 수 있음
- 일기 히스토리 조회는 Netlify `diary` 함수가 아니라 앱/웹 클라이언트가 Firestore를 직접 읽으므로 `diary` 함수 로그에 남지 않는다.
- 앱 시작 시 푸시 토큰 갱신 흐름이 원격 `/api/push/status`를 호출하면 사용자 화면 진입만으로도 Firestore 읽기가 추가 발생한다.

### 수정 및 필수 규칙

- `ERR_REQUIRE_ESM` 여부는 `diary` 함수 로그에서 확인하고, Firestore 한도 여부는 전체 함수 로그의 `RESOURCE_EXHAUSTED`로 확인한다.
- 앱 시작 시 푸시 상태 확인을 위해 `/api/push/status`를 자동 호출하지 않는다. 로컬에 푸시 ON 기록이 있는 경우에만 기기 권한 확인 후 토큰 갱신을 시도한다.
- 사용자가 알림설정 화면에 직접 들어간 경우에만 서버의 푸시 ON/OFF 상태를 확인한다.
- Firestore 한도가 이미 초과된 상태는 코드 배포만으로 즉시 복구되지 않는다. 한도 리셋을 기다리거나 Firebase Blaze 플랜 전환이 필요하다.

### 회귀 확인

1. 배포 후 `netlify logs --source functions --function diary --since 30m --level error`에 새 `ERR_REQUIRE_ESM`이 없는지 확인한다.
2. 전체 함수 로그에 새 `RESOURCE_EXHAUSTED`가 계속 쌓이는지 확인한다.
3. 앱 재실행만으로 `/api/push/status`가 반복 호출되지 않는지 확인한다.
4. Firestore 한도 리셋 후 일기 작성과 히스토리 10개 조회가 정상 동작하는지 확인한다.
